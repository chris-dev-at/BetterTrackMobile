<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG-ONLY network security policy (V5 S1).

  Android blocks cleartext HTTP by default (targetSdk >= 28). The holiday-sprint
  dev stack is served over plain http on the Mac and reached from the phone
  through `adb reverse`, so a debug build must be allowed to talk to it — the
  runtime origin override in Settings → Developer → Dev backend is useless
  otherwise (`java.net.UnknownServiceException: CLEARTEXT communication to
  localhost not permitted by network security policy`).

  Scoped deliberately to the loopback/dev hosts rather than opening cleartext
  globally, so even a debug build cannot silently downgrade a real backend:
    - localhost / 127.0.0.1 / ::1  → `adb reverse` tunnels (API 3000, web 6771)
    - 10.0.2.2                     → the emulator's alias for the host machine
    - 192.168.0.114                → the Mac's LAN address (platform's documented
                                     fallback when adb reverse is unavailable)

  This file lives in `app/src/debug/` and is referenced by the debug-only
  manifest overlay, so it is NOT part of any release artifact: a release build
  keeps the platform default (all cleartext refused).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">::1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">192.168.0.114</domain>
    </domain-config>
</network-security-config>
