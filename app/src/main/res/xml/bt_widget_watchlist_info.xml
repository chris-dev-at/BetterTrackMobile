<?xml version="1.0" encoding="utf-8"?>
<!--
  Watchlist widget (at.bettertrack.app.widget.BtWatchlistWidget).

  2x2 by default, resizable up to a tall list — the row count follows the
  height. Same initialLayout / updatePeriodMillis / preview reasoning as
  bt_widget_net_worth_info.xml.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:configure="at.bettertrack.app.widget.BtWatchlistWidgetConfigActivity"
    android:description="@string/bt_widget_watchlist_description"
    android:initialLayout="@layout/bt_widget_loading"
    android:minHeight="110dp"
    android:minResizeHeight="110dp"
    android:minResizeWidth="140dp"
    android:minWidth="140dp"
    android:previewImage="@drawable/bt_widget_preview_img_watchlist"
    android:previewLayout="@layout/bt_widget_preview_watchlist"
    android:resizeMode="horizontal|vertical"
    android:targetCellHeight="2"
    android:targetCellWidth="2"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen"
    android:widgetFeatures="reconfigurable|configuration_optional" />
