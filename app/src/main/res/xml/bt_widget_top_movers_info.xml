<?xml version="1.0" encoding="utf-8"?>
<!--
  Top-movers widget (at.bettertrack.app.widget.BtTopMoversWidget).

  See bt_widget_net_worth_info.xml for why initialLayout points at Glance's own
  loading layout and why updatePeriodMillis is 0.

  Defaults to 4x3 rather than 2x2: this is a LIST, and a list that opens showing a
  single row asks every user to resize it before it is useful (same reasoning as
  the watchlist widget).
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/bt_widget_top_movers_description"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:minHeight="110dp"
    android:minResizeHeight="110dp"
    android:minResizeWidth="140dp"
    android:minWidth="140dp"
    android:previewLayout="@layout/glance_default_loading_layout"
    android:resizeMode="horizontal|vertical"
    android:targetCellHeight="3"
    android:targetCellWidth="4"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen" />
