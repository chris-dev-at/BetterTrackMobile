<?xml version="1.0" encoding="utf-8"?>
<!--
  Portfolio-stats widget (at.bettertrack.app.widget.BtPortfolioStatsWidget).

  See bt_widget_net_worth_info.xml for why initialLayout points at Glance's own
  loading layout and why updatePeriodMillis is 0.

  Opens at 2x2 (net worth + two percents); resizeMode lets the user stretch to the
  wide and tall breakpoints BtPortfolioStatsWidget draws.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/bt_widget_portfolio_stats_description"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:minHeight="110dp"
    android:minResizeHeight="110dp"
    android:minResizeWidth="140dp"
    android:minWidth="140dp"
    android:previewLayout="@layout/glance_default_loading_layout"
    android:resizeMode="horizontal|vertical"
    android:targetCellHeight="2"
    android:targetCellWidth="2"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen" />
