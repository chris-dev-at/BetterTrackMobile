# BetterTrack App — Build Progress Tracker

Checklist from `HandOffPlan.md`. Check off `[x]` each finished step and add a one-line completion note under it.

---
## THE CHECKLIST

### [x] Step 1 — Project skeleton & docs

New Android project: Kotlin, single module to start, minSdk 26+, latest stable target, Jetpack Compose. Package `at.bettertrack.app`. Set up: build-config fields for `API_ORIGIN`, `WEB_ORIGIN`, `OAUTH_CLIENT_ID` (debug ⇒ `http://localhost:3000` / `http://localhost:8090` — use `10.0.2.2` for the emulator; release ⇒ `https://api.bettertrack.at` / `https://web.bettertrack.at`), version catalog for dependencies, `.gitignore`, and `docs/TODO.md` (see "RIGHT NOW" above). App shows a placeholder screen with the app name.
**Done when:** project builds and runs on an emulator; `docs/TODO.md` is saved.
> ✅ Done — Kotlin via AGP 9.2.1 built-in Kotlin (KGP 2.2.10) + Compose compiler plugin 2.2.10, Compose BOM 2026.06.01; BuildConfig `API_ORIGIN`/`WEB_ORIGIN`/`OAUTH_CLIENT_ID` with debug overrides via `-PbtApiOrigin`/`-PbtWebOrigin`/`-PbtOauthClientId`; compileSdk bumped 36→37 because androidx.core 1.19 / lifecycle 2.11 (latest stable) require API 37 (targetSdk stays 36, minSdk 28); placeholder screen verified on a physical device.

### [x] Step 2 — Design system (spec §3, all of it)

Implement the brand as a Compose theme + component library: color tokens (§3.3 table, dark-only), typography (system font, weights/tracking per §3.4, tabular digits for money), shapes (6–8px cards, §3.5), and reusable components: `Wordmark` (two-color + "App" edition, §3.2), `MoneyText` (EUR formatting, gain/loss coloring), stat card, list card, primary/secondary buttons, chip/badge, loading skeleton, empty state, error state with retry. Integrate the supplied `BT_AppIcon.png` as adaptive icon (§3.1). Add a hidden debug "gallery" screen rendering every component for visual checking.
**Done when:** gallery screen shows all components in correct brand styling; launcher icon is the BT mark.
> Built — `BtColors` tokens (CompositionLocal) + Material3 dark scheme mapping, dark-only; system-font typography with tight tracking and tabular figures (`tnum`) on all money styles; 6–8dp card shapes with 1px #262626 borders; components: Wordmark (+App edition), MoneyText (EUR locale-aware, gain/loss), BtCard/StatCard/ListCard, BtPrimary/SecondaryButton, BtChip/BtBadge, BtSkeleton (shimmer, reduced-motion aware), BtEmptyState/BtErrorState; adaptive launcher icon from BT_AppIcon.png (full-bleed #111315 foreground = seam-free by construction, monochrome layer via PIL) replacing all template icons; Android 12+ splash (BT glyph on near-black); debug gallery behind long-press on the top-bar wordmark.
> ✅ Verified on device (Samsung Note20 Ultra, M2) + design-elevation pass. Every component self-inspected in the debug gallery. Adaptive launcher icon confirmed **SEAM-FREE** on-device (background `#111315` == full-bleed foreground tile — the mask only ever cuts uniform dark). Live splash captured (BT glyph on `#0b0e14`). Craft elevation (2026 bar): reusable empty/error states now carry an icon-in-badge (neutral surface vs red-tinted); tactile reduced-motion-aware press-scale shared by buttons/cards/chips; cleaner translucent-gold selection for chips + bottom-nav (deep amber-tint kept only for large highlighted cards — a two-tier selection system); larger confident money numeral (36sp). Files: `ui/components/BtStates.kt`, `BtMotion.kt` (new), `BtButtons.kt`, `BtCards.kt`, `BtChip.kt`, `ui/shell/AppShell.kt`, `ui/theme/BtTypography.kt`.

### [x] Step 3 — App shell & navigation

Bottom navigation with 4 tabs: **Portfolio · Assets · Social · Workboard** placeholder screens, top app bar with wordmark, notification-bell slot, and settings entry; navigation graph wired for all future destinations (placeholders); global offline-banner scaffold ("Offline — showing data as of …", static for now); pull-to-refresh scaffold on tab screens.
**Done when:** you can tab around the empty app and it looks like a BetterTrack shell.
> Built — 4-tab bottom navigation (Portfolio · Assets · Social · Workboard, Material outlined icons, gold selection on amber-tinted pill), top bar (plain wordmark + inert bell slot + settings), typed kotlinx-serialization routes for ALL future destinations (login, app-lock, holding detail, transactions/form, cash, custom assets, asset page, search, watchlists, conglomerate list/builder/detail, chat list/thread, notifications, settings account/security/notifications/language/about, pending-sync, gallery) with brand placeholder screens; global offline-banner scaffold (§7.4 style, hidden, gallery debug toggle); pull-to-refresh scaffold (no-op) on all 4 tabs.
> ✅ Verified on device (M2): all 4 tabs (Portfolio · Assets · Social · Workboard) render the brand shell — top bar (wordmark + bell + settings), gold-selected bottom nav, per-tab empty states, back-nav to shell. Fixed a copy defect found in QA: the Portfolio empty state read "Log in to see your holdings…" while the user is logged **in** → corrected to "Your holdings, cash and net worth appear here." (strings.xml).

### [x] Step 4 — Login with BetterTrack (spec §4) + API client

OAuth 2.0 Authorization Code + PKCE via Chrome Custom Tab against `{WEB_ORIGIN}/oauth/authorize`, deep-link callback, token exchange at `POST {API_ORIGIN}/api/v1/oauth/token`, encrypted token storage, proactive refresh, logout with server-side revocation + full local wipe. Login screen per brand (wordmark + edition, tagline, single "Login with BetterTrack" button; "Need an account?" / "Forgot password?" open the web). Build the API client layer: bearer header, JSON mapping, error mapping (401→refresh→re-login, 403 `PASSWORD_CHANGE_REQUIRED`→ send to web, disabled account message), auth-gated navigation (logged out ⇒ login screen only).
**Done when:** real login round-trip works against the dev stack; kill/restart keeps the session; logout wipes and returns to login.
> ✅ App side verified on device (M2, against **production** `api/web.bettertrack.at`). OAuth 2.0 Authorization-Code + PKCE round-trip WORKS: Login → Chrome Custom Tab → web login + 4-digit PIN (test account) → `bettertrack://oauth/callback` deep link → `POST /api/v1/oauth/token` = **HTTP 200**, access+refresh tokens stored (EncryptedSharedPreferences). Done-when checks: (1) returns into the 4-tab shell ✓; (2) force-stop + relaunch keeps the session (no re-login) ✓; (3) logout → full local wipe → login screen, stays on login after another relaunch ✓. No crashes.
> ⚠️ **Two endpoint groups are session-cookie-only BY DESIGN** (confirmed against platform source `bearerAuth.ts` + production probes; coordinator holds authoritative platform state): (a) `/auth/me` → **403 `API_KEY_FORBIDDEN`** under a valid bearer ⇒ username/email can't display yet (Settings shows "—"; app degrades gracefully to a placeholder user); (b) `/oauth/*` + `/settings/oauth-grants` ⇒ logout revocation is local-wipe-only (best-effort, logged). **The OAuth bearer IS accepted on all module/data routes** — `/portfolios`, `/assets`, `/search`, `/custom-assets`, `/conglomerates`, `/workboard`, `/backtest`, `/social`, `/notifications`, `/settings` — read scope for GET, write scope for mutations (probe: garbage bearer on `/portfolios`/`/search` → `401 API_KEY_INVALID`, i.e. live bearer middleware, not a policy 403). **⇒ Steps 5–13 are fully buildable AND verifiable with real data now.** The session-only auth/oauth endpoints plus the not-yet-granted `social:write`/chat/notification scopes gate **Steps 14–18** verification only. (Note: an earlier M2 diagnosis claimed the bearer was rejected on ALL routes — that was WRONG, caused by `/openapi.json` per-route security metadata being stale (docs bug: it declares `sessionCookie`-only everywhere). Runtime middleware is the truth; treat `/docs` route-security annotations as unreliable.)

### [x] Step 5 — Local database & sync engine core (spec §7.1, §7.3)

Room database: entities for portfolios, holdings, transactions, cash sources & movements, custom assets & value points, watchlists, conglomerates (read models), plus the **outbound operation queue** (client-UUID key, type, payload, status: pending/in-flight/needs-attention, error). Repository pattern: screens read ONLY from Room; network refreshes Room. Sync worker (WorkManager): connectivity-triggered + foreground-triggered FIFO queue drain with idempotency keys and exponential backoff; refetch-and-reconcile after drain; account-keyed storage wiped on logout/account-switch. Wire the offline banner to real connectivity + data-age. No new visible features this step — include a debug screen showing queue contents and a manual "drain now".
**Done when:** with airplane mode on, a manually-enqueued test op persists across app restarts and drains exactly once when connectivity returns (verify via debug screen + API).
> ✅ Built — Room DB (`data/db/`): entities for portfolios (+embedded server totals), holdings, transactions, cash sources+movements, custom assets+value points, watchlists+items, conglomerates+positions (all read models), the **outbound `sync_ops` queue** (autoinc FIFO id + client-UUID key, opType, portfolioId, payloadJson, status pending/in_flight/needs_attention/done, attemptCount, nextAttemptAtMs backoff gate, serverError, serverResultJson, accountKey), and a `meta` KV (owner key + last-sync-at). DB is single-account; `AccountDataManager` owner-gate wipes on account switch, keeps on same-user re-login / unknown-identity expiry. Repository (`data/repo/PortfolioRepository`) exposes Flow reads + network→Room refresh paths (portfolios list, detail+totals, tx page, cash). Sync engine (`sync/`): pure `SyncEngine` FIFO state machine (unit-tested) + `ApiOpExecutor` op→API mapping for the §7.2 ledger set (buy/sell, cash deposit/withdraw, value-point; transfer parked unsupported), `RoomOpStore`, `SyncScheduler`/`SyncWorker` (WorkManager, CONNECTED-constrained), `ConnectivityMonitor` (NetworkCallback). Triggers: connectivity-restored (in-process callback + parked WM job) + app-foreground (ProcessLifecycleOwner) + manual drain-now. Crash-mid-drain safety: op marked in_flight BEFORE send; ambiguous outcome → next drain RECONCILES via a `[bt:<clientId>]` note marker before any resend (exactly-once). Offline banner wired to real connectivity + cached-data age. Debug queue screen (gallery → "Open sync queue debug screen").
> **Idempotency-key finding (platform gap):** the API has **NO** idempotency-key / client-reference field — `POST /portfolios/{id}/transactions`, `/cash/deposit|withdraw` all use `additionalProperties:false` and reject unknown fields; `grep -ci idempotent openapi.json` = 0. Interim exactly-once: client UUID persisted locally + embedded as a ` [bt:<uuid>]` suffix in the REAL `note` field, so an ambiguous send is proven landed/absent by finding the marker in the ledger. **Platform owner action: add an idempotency key on portfolio mutations** so the marker hack can be dropped.
> **Unit tests:** 16 (`SyncEngineTest`) + 6 (`OwnerGateTest`) green — enqueue→in_flight→done/needs-attention, FIFO blocking, backoff (10s·20s·40s… cap 30m), ambiguous-reconcile (crash mid-drain never double-submits), auth-failure keeps queue, account-key wipe gate.
> ✅ **On-device E2E** (Note20 Ultra, prod API, bearer token). Bearer WORKS on module routes (docs `security` bug confirmed — spec claims sessionCookie-only, runtime accepts bearer). Stages (screens in scratchpad `m3/`): created throwaway **"ZZ App Test"** via bearer API (`07b`); WiFi off → banner flips **Offline/NO NETWORK** (`08`); enqueue buy → **PENDING** (`10`); force-stop+relaunch → **still PENDING**, same client id, 0 attempts (`12`) + live offline banner on shell (`11`); WiFi on → **auto-drain via connectivity trigger** (no manual tap) → **DONE** (`13`), logcat shows exactly ONE POST + `Drained(completed=1)`; live API GET → **exactly 1 transaction**, note carries matching `[bt:c963cc59…]` marker (`14`); **manual drain** + **needs-attention** demo: invalid oversell op → **NEEDS ATTENTION** with server error surfaced + **Retry/Discard** (queue NOT blocked — prior op stays DONE) (`15`/`16`), Discard verified. Cleanup: test transaction hard-deleted (API GET → 0), **ZZ App Test archived** (platform has no portfolio hard-DELETE — archive is its removal semantic) (`17`). Logout → **local wipe incl. sync queue** (logcat "Local account data wiped (caches + sync queue)"), app left at login screen, relaunch stays logged out (`18`). `svc power stayon` restored false; WiFi restored on; airplane untouched.
> **Other platform gaps noted:** no named cash sources / no cash-transfer endpoint (single cash pot per portfolio — synthesized as "Main cash"; transfer op parked as needs-attention "unsupported"); no custom-asset LIST endpoint (identities ride on holdings); single unnamed workboard watchlist (stored under synthetic id). Build: `./gradlew :app:assembleDebug` clean (KSP/Room; added `android.disallowKotlinSourceSets=false` for AGP-9 built-in Kotlin × KSP).

### [x] Step 6 — Portfolio read: switcher, overview, graph (spec §6.1)

Fetch → Room → UI for real portfolio data: portfolio switcher (list/create/rename/archive per §6.1), the **Portfolio tab overview** — Net Worth/total value, performance %, cash line, holdings list with P/L and weights, allocation donut — and the **history graph** with ranges 1D·1W·1M·3M·1Y·Max in the §3.6 area style. All values from the API, rendered from cache when offline with the as-of label.
**Done when:** your real portfolio renders correctly online; airplane mode ⇒ same screen from cache with offline banner.
> ✅ Built — Overview (`ui/portfolio/PortfolioOverviewScreen` + VM): switcher trigger (name+chevron) → bottom-sheet switcher (per-portfolio value, Default badge, ⋮ rename/archive, Archived+Restore section, New portfolio; online-gated with a "Requires a connection — you're offline" hint + disabled controls offline); Net-Worth hero (36sp, day-change line); §3.6 area chart (hand-rolled Compose Canvas, `ui/charts/BtAreaChart`): gold 2dp line, gradient fade, 3 recessive gridlines, scale-consistent muted axis labels (compact ≥10k, zero-clamped for all-positive series), first/last date labels, morph-animated range transitions (resample+lerp, reduced-motion snaps), horizontal-drag scrub with value+date readout + guide/dot; range chips **1M·6M·1Y·Max** — the FULL set the platform serves (`GET /portfolios/{id}/history?range=1M|6M|1Y|MAX`, day-granular; **no server-side 1D/1W/3M window — platform gap**, the web ships the same subset; client-side windowing would mean re-deriving performance % = forbidden §7.1); server `performance[]` last point = headline range % (green/red); Holdings/Cash roll-up cards; allocation donut (Canvas, `BtDonutChart`) with by-asset/by-category toggle, 5-slot CVD-validated muted ramp (#3987E5 #1D9DBF #6D5BD0 #C25B8E #B58840 — gold reserved), Other fold + silver Cash slice, 2dp gaps + labeled legend with %; skeletons (first load + fresh portfolio), create-first-portfolio empty state, error+retry; pull-to-refresh + throttled refetch-on-focus. Selection persisted in the account-scoped Room `meta` KV (deliberate DataStore substitution: identical observe semantics, account-wipe for free). Data layer: `portfolio_history` cache table (verbatim server JSON), DB v1→v2 **real migration** (destructive fallback removed — the sync queue must survive updates), history/rename(PATCH)/restore endpoints + repo mutations.
> ✅ Device E2E (Note20 Ultra, prod API, real account) — numbers vs live-API GETs: cash **exact** (€5.438,01), Bayer **exact** (€914,76), portfolio "second" **exact on every digit** (35.504,58 / 35.301,30 / 203,28 / −1,44 / +47,53%); live-ticking values match within seconds-of-skew; range perf exact on all 4 chips (+9,77/+9,30/+10,12/+344,59 = API 9.7699/9.3/10.12/344.59); scrub 22 Juni → €5.007,73 = API 5007.73; donut %s exact (incl-cash denominator; holdings-row weight = invested-only — both mirror the web reference). Switcher E2E: create "ZZ Switcher Test" (API-verified, auto-selected) → rename "… R2" (API-verified) → archive (API-verified; **selection auto-fell back to Main**) → restore (API-verified) → re-archived (left archived; app reconciled the external archive on refresh). Selection persisted across force-stop/relaunch (reopened on "second"). Offline (WiFi off): overview/donut/holdings fully from cache under "Offline — showing data as of 19:38", zero spinners; switcher actions disabled + hint. WiFi on → banner clears, refresh pulls fresh totals. Screens in scratchpad `m4/`. QA fixes shipped mid-run: single axis format per scale, zero-clamp, unambiguous long-span dates ("Juli 2026").

### [x] Step 7 — Transactions & holding detail, read-only (spec §6.2)

Per-portfolio transaction history (filterable by asset/type), holding detail screen (position value, P/L, its transactions, link-out spot for the asset page later).
**Done when:** every existing transaction from the web shows correctly; works offline from cache.
> ✅ Built — Transactions (`ui/portfolio/TransactionsScreen` + VM): cached-ledger list (Buy/Sell tinted badges, symbol, date · qty × price, notional = qty×price of the same server row, fee subline only when >0, `[bt:…]` sync markers stripped from display notes); **filters: type chips All/Buys/Sells + asset picker bottom sheet** (distinct cached assets; the API has no server-side ledger filters — cursor+limit only — so these are client display filters over cache, matching the web); cursor paging: refresh loads page 1 (limit 200) + auto/manual **load-older** appends via `nextCursor` (offline → requires-connection note); skeletons, no-transactions and no-matches (+Clear filters) states; offline as-of banner in-screen. Holding detail (`HoldingDetailScreen` + VM): position hero (marketValue + day change), 2×2 server stats (Amount, Avg cost, Unrealized P/L €+%, Today €+%), **visually-reserved "Asset page · Coming soon" link-out row (`// TODO(step 11)`)**, that asset's transactions via `transactionsForAsset`; not-found + skeleton states; offline banner. Routes wired (`HoldingDetailRoute.holdingId = assetId`, `TransactionsRoute.portfolioId` with switcher-selection fallback).
> ✅ Device E2E: **all 10 ledger transactions match the API row-for-row** (dates, sides, qty × price, notionals, fees — e.g. BAYN 4×€53,36=€213,44 no-fee; Anthropic 0,0333×€6.010,00=€200,00 Fee €20,00; newest-first order); Sells filter → exactly the 1 sell; Sells+BTC-USD → no-matches state + Clear filters works; asset sheet lists all 6 assets alphabetically. Holding detail (Zoetis): ZTS·NYSE header, €2.203,45 position, +42,11% P/L green, avg cost €53,70 (server), exactly its 2 buys; AMD offline: full render from cache incl. its 1962-day-old buy. Offline: transactions + holding detail fully cached under the as-of banner, zero spinners. **Unverified:** load-older paging couldn't fire live (whole ledger = 10 rows < one page); filter/pager logic is unit-tested instead. Tests: 39/39 green (16 new: history parse/verbatim-%/corrupt-blob/range-map + side/asset filters, distinct-assets, notional, selection-fallback rule, weight guards, note scrub). `assembleDebug` clean.

### [x] Step 8 — Transaction writes + offline queue end-to-end (spec §6.2, §7)

Buy/sell form: asset (from held assets for now), price + amount, date, **pay-from-cash / add-proceeds-to-cash** with live cash-after preview and no silent negatives; edit/delete (online-direct for synced items; queued items just edit the queue); FAB on Portfolio tab (≤2 taps rule). Full offline path: instant pending row + badge, queue drain, reconcile, **needs-attention** flow with edit-and-retry/discard, and the **Pending sync** screen (§7.4) reachable from the offline banner.
**Done when:** a buy recorded in airplane mode shows as pending, syncs on reconnect, and appears on the web; a deliberately-invalid queued op lands in needs-attention with working retry/discard.
> ✅ Built — **TransactionFormScreen** (3 modes: create / edit-queued / edit-synced): emerald/red Buy|Sell toggle (money-direction accents, gold CTA), held-assets picker + "Held in other portfolios" fallback (`// TODO(step 11)` search-buy), comma/dot-tolerant qty/price/fee fields, no-future date picker, cash-coupling card with LIVE "Cash after: €…" vs the CACHED balance + three-way cash check *(evolved 2026-07-09: hard block only when the buy overdraws the CURRENT balance; a backdated buy short only on its past date shows a non-blocking amber advisory — cash settles TODAY via the server's `settleCashAsOfToday` flag (#378) while the stock keeps its backdated date)*, soft amber oversell warning, invisible `[bt:uuid]` note marker preserved through edits, pinned Total+CTA. **All writes run through the M3 queue** (submit = enqueue → in-process drain online; exactly-once machinery around every write); rejection stays inline with the server reason and resubmits bind to the SAME op (same client UUID) via new `SyncEngine.updateOp` (refuses IN_FLIGHT/DONE). Synced edit = delta-only PATCH; synced delete = confirm+DELETE; offline shows "Requires a connection" with controls disabled (§7.2). **Pending rows** decoded straight from sync_ops (no migration): ledger "Waiting to sync" tray, holding-detail rows, overview strip → **Pending-sync screen** (§7.4: sections, per-item Edit&retry/Retry/Discard, Sync now, empty state) reachable from tappable offline banner + strip + debug screen. FABs on overview + holding detail. Sticky per-portfolio cash default in meta KV, seeded from server `defaultPayFromCash`.
> ✅ Device E2E (prod, "ZZ App Test" + €1.000 seed): (1) online FAB buy 1×€100+€5 pay-from-cash → API tx w/ marker + linked movement **−91,79** — server converts asset-currency price+fee to EUR (USD rate 0,8742; client preview best-effort per §7.3); (2) offline buy → instant pending row/strip/badge → survives force-stop → auto-drains on reconnect → **exactly one** new tx (API); (3) two queued oversells → needs-attention "You only hold 3 shares." (queue unblocked) → Edit&retry 999→1 syncs w/ SAME uuid, second op discarded; (4) 100×€100 vs €908,21 → red "-€9.091,79" + inline error + CTA disabled; (5) sticky flip persists reopen+restart, "Main" independent (server default ON); (6) pending screen all states + banner/strip tap-through; (7) synced note-PATCH (marker preserved verbatim), synced delete (cash movement reversed, balance 995,63), offline requires-connection. Screens `m5/00–44`; tests **56/56** (17 new); `assembleDebug` clean.
> **Platform findings:** PATCH tx = side/qty/price/fee/executedAt/note (NO assetId/cash flags); **PATCH rejects economic changes on cash-coupled tx** ("delete and re-add") → app sends delta-only PATCHes; DELETE reverses the coupled cash movement; cash coupling converts to EUR server-side; still no idempotency key (marker stays); **NEW: cash sources/transfer/preview endpoints live** (`/cash/sources`, `/cash/transfer`, `/cash/preview`, movements carry sourceId/transferId, `cashSourceId` on tx create) — unblocks Step 9.

### [x] Step 9 — Cash & cash sources (spec §6.3)

Cash screen per portfolio: Main + named sources with balances and typed labels, movement history per source, **deposit / withdraw / transfer** (source pickers, live previews), create/rename/archive sources. All three movement types work offline via the queue.
**Done when:** an offline transfer between two sources syncs correctly and Net Worth stays consistent with the web.
> ✅ **Platform shipped the cash endpoints since M3** (the gap is CLOSED): `GET /cash/sources`, POST create, PATCH rename/relabel, POST archive/restore, `POST /cash/transfer` (atomic paired legs), `POST /cash/preview`; `GET /cash` now returns `sources[]` + per-movement `sourceId`/`transferId`/`counterpartSourceId`, and `cashSourceId` is accepted on tx create + deposit/withdraw. Wired all of it — the old "Main cash" synthesis is gone.
> ✅ Built — **CashScreen** (`ui/cash/`): total-cash hero (server roll-up §7.1), Deposit · Withdraw · Transfer actions, Sources list (Main-badged first, per-source balance + typed label, tap = filter movements, ⋮ rename/archive — Main's archive hidden), New-source dialog (name + type chips bank/retirement/cash/custom), Archived section + restore, movement stream (kind-labeled incl. transfer legs resolving the counterpart source name, signed gain/loss coloring), pending-cash tray (§7.4). Deposit/withdraw/transfer **all enqueue through the M3 queue** (offline-capable) with live balance-after previews vs CACHED balances (§7.3) — withdrawal/transfer hard-block on overdraw, transfer blocks same-source. Source management (create/rename/archive/restore) is online-only per §7.2 (server rejects Main-archive + non-zero-balance archive; surfaced inline). Queued cash ops render as pending rows with Edit&retry/Discard on the Pending-sync screen (deep-links back into the cash sheet, same client UUID). DB v2→v3 **real migration** (transfer columns on cash_movements; queue preserved). New `OpType.CASH_TRANSFER` executor path (was parked-unsupported in M3). Entry points: overview Cash roll-up card → cash screen.
> ✅ Device E2E (prod, ZZ App Test): created "Savings" source (type cash, API-verified balance 0, Main-first ordering) → **Transfer** enabled once ≥2 sources; **OFFLINE** transfer €200 Main→Savings with dual live preview "Main after €795,63 · Savings after €200,00" → instant "Waiting to sync · Transfer Main → Savings · Pending €200,00" → WiFi on → **auto-drained**, banner cleared, API shows **paired transfer_out −200 / transfer_in +200 sharing one transferId**, sources Main €795,63 + Savings €200, **total cash unchanged €995,63 (Net Worth consistent)** — done-when met. Movement stream shows "Transfer to Savings −€200,00" (counterpart name resolved). Screens `m5/50–57`. Tests **62/62** (6 new: entry/transfer previews, overdraw + same-source validation, active-sources ordering, pending-cash decode). `assembleDebug` clean.
> **Note:** offline WiFi-toggle E2E focused on the transfer (the done-when); deposit/withdraw source-routing + preview covered by unit tests + the shared queue path already E2E-proven in Step 8. `// TODO(step 10)` custom assets next.

### [x] Step 10 — Custom assets (spec §6.4)

Custom asset management: create/edit (name, category), value-point history with step-line chart, edit/delete points, and the **quick "update value now"** action from the holding row. Value points work offline via the queue.
**Done when:** updating a custom asset's value offline lands on the web after sync; step-line renders.
> ✅ Built (Step-10 code inherited code-complete) + **device-verified** (Note20 Ultra, prod API, ZZ App Test). Custom-asset identities ride on portfolio holdings (`isCustom`) + a create/edit cache — the platform has **NO custom-asset LIST endpoint** (§6.3 gap; workaround verified holding). **Create**: "ZZ Test House" (Real estate) → `POST /custom-assets` **201**, appears in list with correct name+category. **Value points**: read-merge-write full-replace `PUT /custom-assets/{id}/value-points` (idempotent by construction — no `[bt:]` marker needed); recorded 1/4/8 Juli (€400k/410k/425k) → **§3.6 step-line renders** (gold step line holding flat then jumping, observation dots, gradient fill). **Quick "update value now" from the holding row** confirmed (Anthropic held in Main → row opens the custom detail). **Offline done-when MET**: value point (7 Juli €420k) recorded **offline** → instant **PENDING** (zero network) → survived **force-stop+relaunch** → WiFi on → connectivity-drain synced **exactly once** (`BtSyncWorker: Drained(completed=1)`, GET→PUT 200) → confirmed via the app's own server re-fetch (server truth) + logcat. Asset edit/delete + point delete are online-only (disabled + greyed offline, verified). Screens `m6/05–27`.
> **Enhancement (in scope §6.4):** added an optional **no-future date picker** to the "Update value" sheet so users can backdate observations (the sheet was today-only). Extracted a shared `BtDatePickerDialog` + `BtDateField` into `ui/components/BtDatePicker.kt` and refactored the Step-8 tx form to reuse it (removed its private `TxDatePickerDialog` duplicate).
> **Bug found & fixed:** a synced value point was not pulled back into Room after a drain (the engine's `afterDrain` is portfolio-keyed and value-point ops carry `portfolioId=null`), so the chart/history stayed stale until the screen was reopened. `CustomAssetDetailViewModel` now refreshes value points after an online record completes and whenever a queued point leaves the pending set (offline→reconnect). Files: `ui/customassets/CustomAssetDetailScreen.kt`, `ui/charts/BtStepLineChart.kt` (inherited), `ui/components/BtDatePicker.kt` (new).
> **Tests:** 66/66 green. `assembleDebug` clean. **Test data to clean at run end:** "ZZ Test House" custom asset (delete via detail screen). Also spotted a leftover **archived** "ZZ Switcher Test R2" portfolio (Step-6 residue — archived, so acceptable).

### [x] Step 11 — Search & asset pages (spec §6.5) — online-only

Global search from the Assets tab (and a search affordance app-wide): 1-character start, debounce, fuzzy server results, "searching providers…" enrichment refetch; result-row direct actions (open asset, quick buy/sell into the selected portfolio, state-aware in-place watchlist icon). **Asset page**: header info, price, §3.6 area chart with ranges 1D·1W·1M·6M·1Y·5Y·Max, key stats, quick buy/sell, watchlist icon-button. Buy/sell from search/asset pages replaces the held-assets-only limitation from Step 8. Clean "requires connection" states offline.
**Done when:** typo'd search finds the right asset; chart ranges all render; buying a brand-new asset from its page works.
> ✅ Built + **device-verified** (Note20 Ultra, prod API, ZZ App Test). **New data layer:** `MarketRepository` (+`MarketModels`) over `GET /search?q=`, `GET /assets/{id}` (asset+quote+eurPrice), `GET /assets/{id}/history?range=` (FULL set 1D·1W·1M·3M·6M·1Y·5Y·MAX — richer than portfolio history), and the single workboard watchlist (`GET/POST /workboard`, `DELETE /workboard/{itemId}`); new `MarketDtos`. **New chart** `ui/charts/BtPriceChart` — same §3.6 gold-line/gradient family as BtAreaChart but **epoch-ms x-axis** so intraday (1D/1W) renders; time-aware labels (HH:mm intraday / d MMM / MMM yyyy), scrub, morph. **Search** (`ui/market/SearchScreen`): debounced (280ms, cancel-previous), 1-char start, fuzzy, **"Searching providers…" enrichment refetch-and-merge** driven by the server `enriching` flag; result rows = open-asset (tap) + quick **Buy** pill (→ tx form into selected portfolio) + **state-aware in-place watchlist star** (POST/DELETE workboard, stays in search). **Asset page** (`ui/market/AssetPageScreen`): header, price (native + "≈ €" server EUR), day-change badge, price chart + all 8 range chips, key stats (prev close/day change/currency/as-of), thumb-anchored Buy/Sell, watchlist star. **Buy of a brand-new asset works** — search-buy passes the identity through `TransactionFormRoute` (new `assetSymbol`/`assetName`/`sell`) so a NOT-held asset binds instantly (lifts Step 8's held-only limit); the tx-form Create branch resolves route identity → holdings refine. App-wide search icon in the top bar; Assets-tab search bar; holding-detail "Asset page" link now live (was "Coming soon").
> **Device done-when MET:** (1) typo **"gogle" → GOOGL/Alphabet** (`m6/34`); also "microsft" returned none (server fuzzy has limits — app behavior correct, "No matches"); (2) all chart ranges render — **1D intraday** "10:00→01:59" (`m6/37`), 1M dates, **MAX** "Aug. 2004→Juli 2026" (`m6/38`); (3) **bought a brand-new GOOGL into ZZ App Test** from its page → `POST /transactions` 201 → holding appears (**€321,48, +4,87%, 1 GOOGL**, cash €995,63→€688,79) (`m6/40-44`). Watchlist star toggle `POST /workboard` 201, persists across restart (`m6/35,39`). Offline search shows "You're offline" (`m6/45`). Fixed a chart defect found in QA: MAX y-axis printed a negative price label (padding dipped below 0) → clamp the padded floor at zero for non-negative series.
> **Note (pre-existing, Step 8 form):** the buy form shows the price with a "€" suffix even for a USD asset; the server correctly treats the price in the asset's native currency (USD $350 → €306,84 cash). Cosmetic currency-label gap — flag for the Step 19 polish pass, not introduced here.
> **Tests:** 66/66 green. `assembleDebug` clean.

### [x] Step V — Dev update notifier (development-phase feature)

Owner wants to SEE when CI publishes a new debug build. Repo `chris-dev-at/BetterTrackMobile` is PUBLIC (releases API works unauthenticated); CI publishes a rolling prerelease `latest-debug` with `BetterTrackApp-debug-<run_number>.apk` + a `version.json` (versionCode/versionName/apk). (1) CI-aware **versioning** (`-PbtVersionCode`/`-PbtVersionName`, default 1/"dev"); CI injects versionCode=run_number, versionName=`0.<run_number>`. (2) **In-app check** on foreground (≤ once/6h + once/cold-start): fetch latest-debug via public GitHub API, compare versionCode vs BuildConfig; if newer show ONE dialog per version — [Go to GitHub]/[Remind me later]/[Ignore this version] (persist ignore). Offline = silent skip. (3) **Settings badge**: gold "Update available" row with the new version → opens the release. (4) Unit-test compare + ignore/remind persistence; device-verify all states.
**Done when:** local build (versionCode 1) shows the dialog (CI run_number > 1), all three buttons behave, settings badge shows; screenshots per state.
> ✅ Built + **device-verified**. CI-aware versioning (`-PbtVersionCode`/`-PbtVersionName`, defaults 1/"dev"); CI injects `github.run_number` → versionCode + `0.<run_number>` versionName and attaches a `version.json` to the rolling `latest-debug` release (verified live: versionCode 7, "0.7"). Foreground check (cold-start + ≥6h, silent on offline/error) fetches version.json, compares vs `BuildConfig.VERSION_CODE`. Device (local versionCode 1 vs CI v0.7): **dialog appears** ("New version available · Version 0.7"); **Go to GitHub** opened the release page (run #7, commit 9d21fa6); **Remind me later** re-prompts next cold start; **Ignore this version** persists (no dialog after force-stop+relaunch); **gold settings badge** "Update available · Version 0.7 · New". Files: `data/update/{UpdateModels,UpdateCheckLogic,UpdatePrefs,UpdateChecker}.kt`, `ui/update/UpdateNotifier.kt`, wired in BetterTrackApplication (foreground) + BtRoot (dialog host) + SettingsScreen (badge) + AppGraph + build.gradle.kts + `.github/workflows/android-apk.yml`. Tests: `UpdateCheckLogicTest` (4). Screens `m6/82–86`.

### [x] Step 12 — Watchlists (spec §6.6)

Multiple named watchlists on the Assets tab: create/rename/delete, add/remove assets, quotes + day change per row, default "General", list-picker in every add flow (one-tap default + choose-list). Read-only offline from cache.
**Done when:** two lists behave independently; add-from-search and add-from-asset-page both offer the picker.
> ✅ Built + **device-verified**. Clean `WatchlistRepository` interface (UI-first doctrine): **"General" backed by the REAL workboard API** — live add/remove (`POST`/`DELETE /workboard`), cached in Room, read-only offline; **multiple named lists + create/rename/delete are behind `WatchlistFlags.multiList` (= BuildConfig.DEBUG) via an in-memory STUB** ("Tech Picks"/"Dividends" sample lists, badged "Sample list"). Release paths show only the real single General list. Assets tab redesigned = search bar + watchlists (custom-assets link kept). Rows show **live quote + day-change %** (`/assets/{id}` fetched in parallel, colored emerald/red). **List-picker in add flows** (`BoardPickerSheet`, one-tap General default + choose-list, state-aware check) from BOTH the search-row star AND the asset-page star.
> Device: General = chris's real workboard (ZTS +1,15% / KO +1,31% / RKLB −10,40% / GOOGL +0,16% / BMW.SW 0,00%, live EUR quotes); switched to Tech Picks (empty, rename/delete controls); **added TSLA via search → board-picker → Tech Picks** (check filled; TSLA appears €352,62 −4,02%); General unchanged ⇒ **two lists independent**. Screens `m6/87–92`.
> **Platform gap / stub boundary (§6.4/§6.6):** the API exposes ONE unnamed watchlist (`/workboard`). Multi-list management is UI-complete but debug-stub-backed; when the platform ships named-watchlist endpoints, swap the stub for a real source behind the same `WatchlistRepository` — no UI change. **Run-end:** GOOGL was added to the real workboard during testing — removed at cleanup to restore chris's list.
> Files: `data/repo/WatchlistRepository.kt`, `ui/watchlist/{WatchlistScreen,BoardPicker}.kt`, `ui/screens/TabScreens.kt` (AssetsTabScreen), Search/AssetPage star wiring, `MarketRepository.quote`. `assembleDebug` clean; 75/75 tests green.

### [x] Step 13 — Conglomerates lite (spec §6.7) — online-only

Workboard tab: conglomerate list (composition summary + current value), **builder** (search-add assets, weight editing validated to 100%, name), **past-performance graph** (single backtest curve, ranges, entry markers — nothing more), **budget calculator** (budget → weighted buy list, "at least 1 share" toggle, remainder display) with **"Add to portfolio"** committing the buy list (with pay-from-cash) in a couple of taps.
**Done when:** create a 3-asset conglomerate, see its past performance, calculate €1,000, commit to a portfolio — transactions appear.
> ✅ Built + **device-verified**. Workboard tab = the conglomerate list (chris's real "MyConglomerate 1" + created "ZZ Trio"). Clean `ConglomerateRepository` over `/conglomerates` CRUD + `PUT .../positions` + `POST .../allocate` + `POST /backtest/preview` — all server-computed (§7.1). **Builder** (`ConglomerateBuilderScreen`): name + search-add assets (inline market-search sheet) + per-position weight fields validated to 100% (green Total) + an "Even split" convenience; save = create + replace-positions. **Detail** (`ConglomerateDetailScreen`): composition, **past-performance backtest** (single gold curve via `BtPriceChart`, ranges 1Y/3Y/5Y/Max, stats Total return/CAGR/Max drawdown), **budget calculator** (`/allocate`, "at least 1 share of each" toggle, buy list w/ qty + cost + actual%, Invested + Left over remainder, stale-quote notice), **"Add to portfolio"** committing the buy list through the tx queue (pay-from-cash). Delete (confirm).
> **Device done-when MET**: created **ZZ Trio** (AAPL/MSFT/GOOGL, Even-split 33.33/33.33/33.34, saved via create+PUT positions) → **past performance renders** (+49,80% / CAGR +50,01% / max DD −15,91%, ranges) → **calculate €1.000** → buy list (1 share of each, at-least-1-share ON, invested €932,89, remainder €67,11) → **committed into ZZ App Test → 3× `POST /transactions` 201** (AAPL/MSFT/GOOGL). Screens `m6/93–119`.
> **Bug found & fixed:** the commit reported "Added" unconditionally; now it inspects each enqueued op's status and only reports success when the buys land (else "Added X of N — the rest need attention"). First attempt correctly surfaced the server's **insufficient-cash rejection** (§7.3) — the €1.000 budget's €933 buy list exceeded ZZ App Test's €432 cash; deposited €1.000 (`/cash/deposit` 201) and re-committed clean.
> Files: `data/api/dto/ConglomerateDtos.kt`, `data/repo/ConglomerateRepository.kt`, `ui/conglomerate/{ConglomerateListScreen,ConglomerateBuilderScreen,ConglomerateDetailScreen}.kt`, BtApi + AppShell wiring. `assembleDebug` clean; 75/75 tests green.
> **Run-end:** delete the ZZ Trio conglomerate + the AAPL/MSFT/GOOGL test buys; archive ZZ App Test.

### [x] Step 14 — Friends & sharing (spec §6.8, §6.9) — online-only
> *Evolved 2026-07-09 (`fe9f126`): went fully LIVE on sharing-v3 (#332/#341) + Social-v2 redesign — unified 4-rung audience via `PUT /social/audience/{kind}/{subjectId}`, revocable public links (mint-once reveal), named watchlists ("General" locked), FriendOverviewScreen (chat link, their shares, per-item activity-alert pref via `PUT /social/shared/activity`, Remove-friend moved off the row), shared-with-me grouped by person, My Shares live incl. conglomerates. Friend request→accept loop wired but unexecuted (needs the second test account); activity-alert DELIVERY platform-gated (#368).*

Social tab: friends list, add-by-username/email, incoming vs outgoing requests (accept/decline/cancel, clear visual separation), unfriend. **Sharing**: audience picker on every portfolio/watchlist/conglomerate (private / specific friends / all friends / public link) with the **friction ladder exactly per §6.9** (blocking acknowledgment for public + Android share-sheet for the link); **Shared with me** (read-only friend views); **My shared items** (audit + revoke).
**Done when:** full loop with a second test account: request → accept → share portfolio to specific friend → friend sees it → revoke → friend loses it; public link requires the acknowledgment tick.
> ✅ Built + **device-verified** (Note20 Ultra, prod API, real account). Social tab = a 3-way segmented control (**Friends · Shared · My shares**, with live count badges) over a clean `SocialRepository` seam. Friends: add-by-username/email dialog, **incoming vs outgoing** sections (visually separated: Accept/Decline on incoming, Pending+Cancel on outgoing), friends list with per-person **initials avatars** (deterministic CVD-validated tint) + ⋮ → Remove (confirm). **Shared with me** groups friends' shared portfolios/conglomerates/watchlists → tap opens **read-only** detail views (no edit affordances, "you can view but not change it" footer). **My shares** audits every shareable item with its current audience badge → tap opens the **§6.9 audience-picker sheet** (this is the sharing hub, mirroring the web's "My Shared Items" page). Friction ladder replicated exactly: Private = none · Specific friends = inline friend multi-select (none) · All friends = light non-blocking confirm line · **Public link = strong BLOCKING red acknowledgment** — "Create link & share" stays disabled until the "I understand…" box is ticked, then fires the **Android share-sheet** with the link.
> **LIVE vs STUB boundary (exact endpoints):**
> - **LIVE reads (social:read, held):** `GET /social/friends`, `/social/requests`, `/social/shared` (Shared-with-me), `/social/my-shared`, `/social/shared/{portfolioId}` (+ `/watchlists/{userId}`, `/conglomerates/{id}` read-only views). Verified against chris's REAL data: 3 real friends (@mulhammagkaffe/@stevewonder/@mulham), 3 real shared-with-me portfolios (@mulham €988,40 NVDA+GOOGL, @mulhammagkaffe €3.141,57, @stevewonder €1.161.750,00), my-shared = Main (All friends) + General watchlist (Private).
> - **LIVE sharing-visibility mutations** for the two tiers the platform models (`private`↔`friends`): `PATCH /portfolios/{id}`, `PATCH /workboard/sharing`, `PATCH /conglomerates/{id}` — these ride **portfolio:write / workboard:write (both held)**, so "Private"/"All friends" genuinely persist. Verified the full live round-trip on the General watchlist: Private → **PATCH friends** (badge flipped to "All friends", re-fetched from server) → **PATCH private** (restored). **No lasting change to real state** — Main's real "All friends" was never touched.
> - **STUB behind `SocialFlags` (debug sample data + optimistic flows; "coming soon" in release):** (1) friend-graph WRITES (send/accept/decline/cancel request, unfriend) — need a `social:write` scope the mobile client is **not yet granted**; backed by an in-memory overlay (2 sample friends, 1 incoming, 1 outgoing, badged "Sample"). Verified accept moves the request into Friends optimistically. (2) the two app-ahead audience tiers **"Specific friends"** (per-friend ACL) and **"Public link"** (revocable token) — the platform has only `private`/`friends`, so these apply to the debug overlay and render **"Coming soon"** in release. Public-link URL shape mirrors the web's planned `/{kind}/shared/{token}` route.
> - **Cold-process** relaunch straight to Social loads live data cleanly, session persisted (Social is online-only — no cache-cold-start risk). **Platform asks for Christian:** grant `social:write` to the BetterTrackMobile client, and (later) ship per-friend sharing + public-link tokens to light up the two stub tiers.
> Files: `data/api/dto/SocialDtos.kt`, `data/repo/SocialRepository.kt` (+`SocialFlags`/`ShareAudience`), `ui/components/BtAvatar.kt`, `ui/social/{SocialScreen,AudiencePickerSheet,SharedDetailScreens}.kt`, `BtApi` social+sharing endpoints, routes+shell wiring. `assembleDebug` clean; 75/75 tests green. Discretionary follow-up: contextual per-screen "Share" buttons (portfolio/watchlist/conglomerate detail) — sharing is currently centralized in My-shares.

### [x] Step 15 — Friend chat (spec §6.10) — VERIFY & POLISH (UI-first, stub-backed)
> *Evolved 2026-07-09: wired stub→LIVE against #349 — real `/chat/*` adapters (conversations+unread, cursor-paged thread, send w/ 4000 clamp, mark-read, chips resolving through sharing enforcement, unfriended=read-only), new `ChatRealtime.kt` WS invalidation client + foreground-gated polling fallback (~10s thread / ~30s list, verified on the wire), +17 tests. **Live send/receive still 403-gated: `chat:*` scopes not effective on prod — see PLATFORM_ASKS P1 (likely migration 0027 not rolled); everything else device-verified.** WS direct-upgrade 400 → PLATFORM_ASKS P3 (polling covers correctness).*

Conversation list with unread badges + message threads with a friend; realtime via the platform gateway where available, polling fallback; **share-in-chat** chips (asset or shareable item; recipient resolves only if allowed — "not shared with you" state otherwise). No groups/reactions/read receipts.
**Done when:** two accounts chat phone↔web; a private-item chip shows the not-shared state; unread counts survive restart.
> ✅ **Verified + polished on device** (Note20 Ultra, prod session, real account). Chat is **100% STUB** behind `ChatFlags.enabled = BuildConfig.DEBUG`, mirroring the Step-14 `SocialFlags` pattern: a clean `ChatRepository` interface + `ChatGateway` realtime seam, `StubChatRepository` (in-memory messages + SharedPreferences read-state) in debug, **"Chat is coming soon"** `BtEmptyState` in release.
> **Platform reality (independently probed):** production `openapi.json` has **NO** `/chat|/conversations|/messages` routes and **no `chat:*` scope** (only `social:read` in the social family). The web `PROJECTPLAN.md` schedules chat as **V3-P8** (behavior: 1:1 DMs, unread badges, realtime-over-gateway+poll fallback, share-in-chat that never widens access, no groups/reactions/read-receipts — the app matches this exactly) with **`chat:read`/`chat:write` scopes + bearer routes only in V4-P1** (future). Chat is correctly UI-only for now.
> **DTO cross-check:** `ChatDtos.kt` (Conversation/ChatMessage/ShareChip + cursor pagination `?limit&cursor` + `{error:{code,message}}` envelope) aligns with V3-P8 behavior + platform conventions. Since the server contract is not yet field-frozen, these are a thin forward design — the eventual `ChatApi` is a decode-only adapter. No shape change needed; boundary noted.
> **Done-when evidence (device, `s15/`):** (1) **conversation list + unread badges** — `@anna_m` badge **"2"** (bold) vs read `@lukas.k` (`s15/11,13`); total-unread badge on the Social "Messages" header (`s15/09,19`). (2) **thread + composer** — incoming (dark/left) + from-me (gold/right) bubbles, per-message timestamps, paperclip+field+send composer (`s15/12,14`). (3) **share-in-chat chips, both states in one view** (`s15/14`): resolved "Anna's Main · Portfolio" chip with gold **View**, AND private "Conglomerate — **Not shared with you**" lock chip (no data leak). (4) **unread survives restart** — Test A: kill+relaunch process w/o reading → badge still **"2"** (`s15/20`); Test B: read `@anna_m` → badge clears (`s15/21`) → kill+relaunch → **stays cleared** (`s15/22`). (5) **release "Coming soon"** — code-verified (both chat screens gate on `ChatFlags`; identical `BtEmptyState` pattern already verified live in Step 14); not device-tested (no release APK built).
> **Realtime plumbing (bonus, live):** the stub gateway delivered a **live** incoming message to `@lukas.k` (~7s) which reordered the list to top + badged it, then read-on-open cleared it (`s15/12,13`) — proves the badge/unread pipe end-to-end.
> **Defects found + fixed (polish pass):** (1) **release-path leak** — a friend-row "Message" button routed straight to `ChatThreadScreen`, which in release showed an empty thread + dead composer; now gated to the "Coming soon" state (composer hidden in release). (2) **stub gateway accumulated duplicates** — it reconnected+refired on every chat-list entry, piling identical demo messages onto `c-anna` and repeatedly flipping its unread (also confounding the restart test); now fires **once per process** onto the already-read `c-lukas`, leaving `c-anna` clean for the persistence demo. (3) **blank empty thread** — added a warm "Say hi to @friend" hint for new conversations (`s15/17`).
> Files: `data/repo/ChatRepository.kt` (gateway once-per-process → `c-lukas`; extracted pure `chatMessagePreview`), `ui/chat/ChatThreadScreen.kt` (release guard + empty-thread hint + composer gated on `ChatFlags`), `data/api/dto/ChatDtos.kt` (unchanged — shapes confirmed aligned), new `app/src/test/java/at/bettertrack/app/ui/chat/ChatLogicTest.kt` (+11). `assembleDebug` **clean**; **86/86 unit tests green** (was 75).
> **Platform gaps / owner action items:** to go live, the platform must ship **V3-P8 chat** (conversation/message/share-in-chat endpoints + WS `chat.message` gateway room) and **V4-P1** grant the BetterTrackMobile client `chat:read`/`chat:write` + bearer coverage on the chat routes; then delete the stub behind `ChatFlags` and drop in the `ChatApi` adapter (no UI change). Full live **phone↔web** loop also needs a **second test account**.
> **Exit:** logged IN (stub is local; **no server writes → account untouched, no cleanup needed**), device stayon still ON (restored at run end).

### [x] Step 16 — Notifications (spec §6.11)

In-app **inbox** behind the bell (unread badge, mark-read, tap-through deep links) + **FCM push**: Firebase wiring, device-token register/refresh/delete against the platform endpoints, notification channels, deep-link taps, Android 13+ permission asked in context (never on cold first launch). **Notification settings**: the per-type × per-channel matrix (in-app / email / push / muted).
**Done when:** a friend request from the web pops a push with the app closed and deep-links to Social; muting a type in the matrix stops it.
> ✅ Built + **device-verified** (Note20 Ultra, prod API). **FCM CLIENT wired** — `com.google.gms.google-services` 4.5.0 plugin + Firebase BoM 34.15.0 via the version catalog (google-services.json already placed, project bettertrackapp-c6996); **assembleDebug stayed clean through the plugin add on AGP 9** (no fight — plugin resolves via pluginManagement's `google()` which matches `com.google.*`). 4 notification **channels** created at app start (Social · Portfolio & alerts · Account & security · General — all confirmed in the system per-app notification settings). **FirebaseMessagingService** (`data/push/BtMessagingService`): `onMessageReceived` → the shared `postIncoming` pipeline (mute/push gate → inbox insert → system notification with a deep-link PendingIntent); `onNewToken` → (stubbed) register. **FCM token obtained for real** (logcat `present=true, length=142` — value never logged); a debug **Simulate** action (flask icon, debug-only) drives the exact push path so delivery is device-verifiable without the platform sender.
> **LIVE vs STUB (exact endpoints):**
> - **Inbox reads/writes wired LIVE** against `GET /notifications` (`{items[{id,type,title,body,payload?,readAt?,createdAt}], nextCursor, unreadCount}`) + `POST /notifications/mark-read` (`{ids[]}` | `{all:true}`). At test time the whole prod API was returning a transient **Cloudflare HTTP 521** (origin degraded) on every route — so the read fell back to a **debug sample inbox** (5 seeded notifications) and **local mark-read**, logging the precise probe outcome; it lights up live automatically once the origin is healthy AND the mobile client holds a **notifications read scope** (the granted set — `portfolio/workboard/market/social` — has none, so a scope 403 is also expected until Christian grants it). `NotificationRepository` interface + `DefaultNotificationRepository` fall-back seam mirrors the Step 14/15 stub pattern.
> - **Settings matrix reads/writes wired LIVE** against `GET/PATCH /settings/notifications` — but the **server matrix models only `inapp` + `email`** per type (6 types: friend.request/accepted, portfolio.shared, account.invite, account.temp_password, alert.triggered). The app's **Push + Mute columns are app-local** (SharedPreferences) — there is **no server push channel** yet — and in-app/email sync to the PATCH when the scope lands. Muting is purely local suppression.
> - **Device-token register/refresh/delete = STUBBED** (`PushFlags`/`NotificationFlags.stubDeviceRegistration`): the platform exposes **NO device-token endpoint** (confirmed against prod `openapi.json` — no `/device*`, `/push*`, `/token` routes). Logged, not faked.
> **Done-when evidence (device, `s16/`):** inbox opens from the bell with a gold **"3"** unread badge (`01,02,03`); **mark-all-read** clears all + the action disappears (`15→16`); **in-context permission** — cold launches showed NO prompt; tapping the inbox "Turn on push" prompt fired the system `POST_NOTIFICATIONS` dialog (`04`) → granted (`05`, dumpsys `granted=true`); all **4 channels** listed in system settings (`10`); **deep-link** — a friend.request notification intent navigated to the **Social tab** (`12`, matches the spec example); a real **system notification posted** (status-bar bell) via the Simulate path; **muting suppresses locally** — with Price alerts muted, a simulated `alert.triggered` was dropped (logcat `Incoming 'alert.triggered' suppressed by local matrix (muted)`, `21`) while its unmuted cycle-mate friend.request appeared; **matrix persists** — the mute survived a force-stop+relaunch (`22`). The literal **"push with the app closed from the web" is platform-gated** (server needs the FCM HTTP v1 sender + device-token endpoint; service-account key is server-side only) — noted, not device-testable here.
> **Defects found + fixed:** (1) **cold-crash** — `NotificationSettingsStore._matrix = MutableStateFlow(load())` ran before `configurableKinds` was initialized (declared after it) → NPE iterating a null list on every launch; fixed by ordering `configurableKinds` before `_matrix`. Caught + fixed on device, reinstalled, verified stable.
> **Small UX improvements (listed):** unified the whole app on ONE gold count-badge system — extracted `BtCountBadge`/`BtBadgeOverlay`/`BtUnreadDot` and refactored the Step-15 Social "Messages" header to it (holistic badge language across bell + inbox + Social + chat, per the design-continuity note); notification rows use the icon-in-badge language shared with empty/error states; unread rows carry a gold tint + dot; the matrix renders as per-type cards with channel toggle-chips + a mute switch (every target a 48dp tap vs a cramped checkbox grid); added a "Notifications" row under a new Settings → **Preferences** section (light Step-18 preview).
> **Platform gaps / owner action items (Christian):** (a) grant the BetterTrackMobile client a **notifications read scope** so `GET /notifications` + `GET/PATCH /settings/notifications` go live; (b) ship **device-token endpoints** + the **FCM HTTP v1 push sender** (service-account key `bettertrackapp-c6996-…json`, server env only) for real end-to-end delivery; (c) add server matrix columns for **push** (and a mute/all-off) if you want those to sync (today they're app-local); (d) prod API was returning **HTTP 521** during testing — transient origin/Cloudflare issue to check.
> Files: `data/notifications/{NotificationModels,NotificationDtos(→dto),NotificationSettingsStore,NotificationRepository}.kt`, `data/push/{PushChannels,PushTokenManager,BtMessagingService}.kt`, `ui/notifications/{NotificationBell,NotificationsInboxScreen,NotificationSettingsScreen}.kt`, `ui/components/BtCountBadge.kt`, `res/drawable/ic_stat_bt.xml`, wired into `BtApi`, `AppGraph`, `AppShell`, `MainActivity`, `BetterTrackApplication`, `SettingsScreen`, `AndroidManifest`, `strings.xml`, version catalog + both build.gradle.kts. Tests: **102/102** green (+16 `NotificationLogicTest`: type→kind, deep-link resolution incl. payload/blank/fallback, mute/channel delivery rule, prefs accessors). `assembleDebug` **clean**. **Exit:** logged IN (matrix is device-local, no server writes → account untouched, nothing to clean); device stayon still ON (restored at run end).

### [x] Step 17 — App lock (spec §5)

Local 4–6-digit PIN (Keystore-hashed) + BiometricPrompt unlock; triggers on cold start and configurable AFK return (immediately/1/5/15 min, default 1); progressive backoff on wrong PINs + shake feedback (reduced-motion aware); recents-screen content masking; "forgot PIN ⇒ log out" path; setup/change/disable under Settings → Security.
**Done when:** enabled lock gates every entry into the app, fingerprint unlocks, 5 wrong PINs back off, recents shows no data.
> ✅ Built + **device-verified** (Note20 Ultra, API 33 / One UI). **Entirely client-side + LIVE** — no server calls, no stubs. **PIN is Keystore-HMAC'd, never stored**: a per-PIN random salt + `HMAC-SHA256(AndroidKeyStore key, salt‖pin)` (`AppLockCrypto`, alias `bt_applock_hmac_v1`, PURPOSE_SIGN, non-exportable) → the hardware key never leaves the TEE, so a prefs dump can't be brute-forced off-device. Persisted in a **separate** EncryptedSharedPreferences vault (`bt_app_lock`, `AppLockStore`) — independent of the session store so it survives token refresh/expiry (WhatsApp model: gates the screen, not the token). `AppLockController` owns the lock state + triggers; the lock gates ONLY the logged-in branch of `BtRoot`.
> **Done-when evidence (device, `s17/`):** (1) **gates every entry** — cold start locks (`16`,`42`: force-stop→relaunch ⇒ lock screen), AFK return locks (`10`,`20`: background→foreground past threshold); unlocking with the PIN reveals the portfolio (`14`,`29`). (2) **5 wrong PINs back off** — 4 free misses show red dots + "Wrong PIN, try again" + shake + haptic (`11`); the 5th triggers a **30 s lockout** with a live "Try again in 0:29" countdown and a fully **disabled keypad** (`12`,`13`), auto-clearing when it expires. Backoff schedule (unit-tested): 5th→30 s, 6th→1 m, 7th→2 m, 8th+→5 m cap; failure count + lockout deadline persist in the vault so a force-kill can't reset backoff. (3) **recents shows no data** — `MainActivity.setRecentsScreenshotEnabled(false)` (API 33+, driven off the enabled StateFlow so it's armed on cold start AND the instant the toggle flips; FLAG_SECURE is the pre-33 fallback) ⇒ the app's task-switcher card is a **blank dark card**, no net worth/holdings/chart (`19`; contrast the un-masked leak that the first FLAG_SECURE-on-pause attempt showed — fixed). Chosen over persistent FLAG_SECURE so in-app screenshots (and the live UI) still render. (4) **setup / change / disable** — create (`04`–`07`), change-PIN with verify-current→new→confirm and a length change 4→5 that makes the OLD pin un-submittable + a wrong old pin rejected (`21`–`29`), disable with type-safe confirm ⇒ cold start no longer locks (`30`–`32`). (5) **Forgot PIN ⇒ log out** — confirm dialog ("we'll sign you out and clear this device's local data") → wipes local data + revokes/clears session + clears the lock → lands at the **login screen** (`34`,`35`); re-login via the OAuth Custom Tab succeeded (prod API/web confirmed back to HTTP 200 after the platform restart). PIN-pad screenshots of the web re-auth were deleted per the credentials rule.
> **BIOMETRIC unlock — BUILT, owner must device-verify.** Full `BiometricPrompt` path is implemented (androidx.biometric on a `FragmentActivity` host — `MainActivity` switched from ComponentActivity; `BIOMETRIC_WEAK`, negative button = "Use PIN"; on success → `onBiometricSuccess()`, auto-prompted on the lock screen when enrolled+enabled, PIN always the fallback). **Could NOT be device-verified because the owner removed the phone's screen lock + fingerprint enrolments for unattended build nights** — `BiometricManager.canAuthenticate()` returns NONE_ENROLLED (confirmed on device: the Security row read "no biometrics enrolled"). Availability handling is correct (AVAILABLE / NONE_ENROLLED / no-hardware→hidden). **Owner test:** re-enrol a fingerprint in Android settings → open the app / trigger the lock → the fingerprint prompt appears automatically; PIN fallback works throughout.
> **Small UX improvements (listed):** (a) the "Unlock with biometrics" toggle is **settable even before a fingerprint is enrolled** — the preference persists and activates the moment one is enrolled (row explains "Add a fingerprint in your phone settings — this turns on automatically"), instead of a greyed-out switch that forces enrol-then-come-back; graceful PIN fallback until then. (b) Reusable `PinKeypad`/`PinDots` (borderless gold-ripple keys, press-scale, reduced-motion-aware shake, tabular). (c) Unlock auto-submits at the stored PIN length; setup uses an explicit Continue for the variable 4–6 length. (d) Live lockout countdown mm:ss.
> **Platform gaps / owner action items (Christian):** none server-side (fully client-side). **Owner action:** device-verify biometric unlock (see above). Also note: the earlier prod HTTP 521 window was the announced platform restart (now back to 200).
> Files: `data/applock/{AppLockModels,AppLockCrypto,AppLockStore,AppLockController}.kt`, `ui/applock/{BiometricAuth,PinKeypad,AppLockScreen,AppLockSetupScreen}.kt`, `ui/settings/SecurityScreen.kt`; wired into `AppGraph`, `BetterTrackApplication` (controller.start), `MainActivity` (FragmentActivity + recents mask), `BtRoot` (lock gate), `AppShell` (Security + setup routes), `SettingsScreen` (Security row), `BtRoutes` (`AppLockSetupRoute`), `AndroidManifest` (USE_BIOMETRIC), `strings.xml`, version catalog + app `build.gradle.kts` (androidx.biometric 1.1.0 + fragment 1.8.5). Tests: **113/113** green (+11 `AppLockLogicTest`: backoff schedule incl. cap + monotonicity, 4–6-digit PIN format incl. non-ASCII, AFK-threshold mapping/millis). `assembleDebug` **clean**. **PAUSE after this step** (owner biometric test). **Exit:** app **LOGGED IN + LOCKED**, app-lock **ENABLED**, **biometric toggle ON**, throwaway app-lock **PIN = 2468**, re-lock "After 1 minute"; device stayon **ON** (not restored — deferred to end of Step 18). Lock intentionally left ENABLED (not disabled) for the owner's biometric test.
> **Step 17 refinements (round 2, device-verified `s17b/`):** biometric-availability gating (toggle DISABLED + greyed with "Set up a fingerprint in Android settings to enable" until a biometric is actually enrolled; re-reads on resume; reverses the earlier settable-before-enrolled behavior), BetterTrack-PIN setup choice (first-run chooser: "Create a device PIN" 4–6 digits vs "Use my BetterTrack PIN" 4 digits — the latter stored LOCALLY Keystore-hashed via a new `PinSource` seam, NOT yet server-verified; `// TODO(platform verify-pin)` at the seam, PLATFORM_ASKS P2), keypad+PIN lowered into the one-handed thumb zone on both unlock + setup (bottom-weighted, bigger 78dp keys), per-keypress haptics (`View.performHapticFeedback(KEYBOARD_TAP)`, honors system setting) + smoother spring dot-fill. Tests **118/118** (+5 `PinSource`/length rules); `assembleDebug` clean. Device evidence: choice UI (`06`), honest BetterTrack-PIN enter screen (`07`), `source BETTERTRACK`→`DEVICE` in logcat, lowered lock screen (`11`), wrong-PIN red/shake held + correct PIN unlocked (`12`,`13`), biometric toggle disabled + non-interactive (`09`,`10`). **Exit:** LOGGED IN + LOCKED, lock ENABLED, throwaway **PIN = 2468** (device source), biometric toggle correctly DISABLED (no fingerprint enrolled), re-lock "After 1 minute"; stayon ON.
> **Step 17c — real "Use my BetterTrack PIN" verify (device-probed `s17c/`): BLOCKED server-side; option hidden.** On-device probe with the app's REAL OAuth bearer (prod `api.bettertrack.at`, 2026-07-08) → **`GET /auth/me` = 403 `API_KEY_FORBIDDEN`** and **`POST /auth/pin/verify` = 403 `API_KEY_FORBIDDEN`** (probed with `"0"` format-invalid ⇒ 403 at the auth gate BEFORE body validation, and `"0000"` valid-wrong ⇒ 403, NOT 401 — so **no PIN attempt was ever consumed** on the account; network confirmed live via real error envelopes). The whole `/auth/` group is session-cookie-only for the mobile bearer. **⇒ reusing the web PIN is impossible until the platform changes the API.** What shipped: (1) the full real verify path is BUILT — `AccountPinService` (`GET /auth/me` → `pinEnabled`; `POST /auth/pin/verify` with a wrong-PIN never double-submitted, via a new `X-Bt-No-Reauth` header that opts the call out of the 401→refresh authenticator) + `PinVerifyRequest` DTO + `BtApi.pinVerify` + pure status→outcome mappers, and the setup screen wires enter→verify→activate/reject-inline/no-PIN-fallback/offline — all behind `AppLockFeatures.betterTrackPinLock`; (2) that switch is **OFF**, so the chooser is **hidden entirely** and setup goes straight to a **device PIN** (device-PIN-only); (3) the old **unverified local-store interim was removed** (BETTERTRACK now only ever activates via a live 200, never a silent local save); (4) honesty strings fixed ("we don't send it anywhere" → "we check it once, then keep only a locked copy"). Also fixed a pre-existing **secret leak**: the real web PIN was hard-coded as a fixture in `AppLockLogicTest` — replaced with a neutral value. Tests **127/127** (+9 verify/me mapping + a flag tripwire); `assembleDebug` clean. Device evidence (`s17c/`, dots-only): device-only "Create a PIN" with NO chooser/BetterTrack option (`02`), lock enabled + biometric correctly disabled (`03`), wrong-PIN rejected inline (`06`), final logged-in+locked (`07`). **EXACT API change needed (owner → platform):** let the **BetterTrackMobile** OAuth bearer reach **`POST /auth/pin/verify`** (and ideally **`GET /auth/me`**, to gate the option on `pinEnabled` + unblock real username/email display) instead of returning 403 `API_KEY_FORBIDDEN` — i.e. add the mobile client to the bearer coverage/scope for those routes. Once shipped: flip `AppLockFeatures.betterTrackPinLock` to `true`, update the tripwire test, and RE-VERIFY on-device. **Exit:** LOGGED IN + LOCKED, lock ENABLED, throwaway device **PIN = 2580**, biometric toggle correctly DISABLED (no fingerprint), re-lock "After 1 minute"; stayon ON. (Aside: the dev "New version available" update dialog pops over the lock screen on foreground — pre-existing Step V UpdateNotifier, dismissable, not touched here.)

> **Resume fix batch (owner-flagged bugs, 2026-07-08, device-verified `s18fix/`):** four fixes ahead of Step 18; all builder-side code, coordinator commits.
> 1. **PRIORITY — fresh-login stuck on skeletons → FIXED.** Root cause was twofold: (a) the overview VM's `refresh()` read the `WhileSubscribed` `selected` StateFlow right after the list write, racing to `null` so the dependent cascade (detail/holdings/history/cash) never fired on a fresh login — the hero/rollup/chart sat on skeletons until a manual pull-to-refresh; and (b) nothing kicked an initial load off login-success at all. Fix: new `data/session/SessionInitializer` (started in `BetterTrackApplication`, observes `AuthRepository.authState`) fires on login-success AND logged-in cold start when the cache is empty → `refreshPortfolios` → resolve + **persist** the default selection → parallel cascade (detail, history, ledger, cash) + watchlists warm; PLUS the overview VM's `refresh()` now resolves the selection from a one-shot Room read (`PortfolioRepository.defaultSelection()`) so pull-to-refresh/focus always cascade too. Canonical selection rule moved to `PortfolioRepository.resolveSelection` (VM delegates; existing tests unchanged). Empty/failed loads still fall through to the empty/error states (synced-at set only on success). **Device: `pm clear` → login → Portfolio (net worth/chart/holdings/cash), Assets (watchlist quotes) and Workboard (conglomerate) all populate on their own in ~1–2 s, zero manual refresh** (`03`/`04`/`05`).
> 2. **Max / pay-from-cash used TOTAL cash, not the Main wallet → FIXED.** `TransactionFormViewModel` now sizes the coupling (validation, cash-after preview, Max, the card balance) against the **MAIN/default cash source** (`mainCashSourceBalanceEur` over `repo.cashSources`), not `totals.cashEur`; `ensureFreshSnapshot` also refreshes cash so Main is known on open. **Device: Main €5.301,36 vs Total €7.901,36 — Max filled 10,27176414 × €516,11 = €5.301,36 (Cash after €0,00), i.e. the Main wallet only** (`08`/`13`).
> 3. **Online tx rejection dumped to needs-attention instead of inline fix → FIXED.** `submitViaQueue` now only navigates away on genuine success (DONE)/vanished/offline-enqueue; any server reason (NEEDS_ATTENTION, or an online still-open op carrying a reason) keeps the form open with the reason inline and rebinds the SAME op/client-UUID for edit-and-retry. Offline still enqueues a pending row; background/later failures still land in needs-attention; exactly-once untouched. **Device: online oversell → "Rejected by BetterTrack · You only hold 5 shares" stays on the form → lower qty → resubmit succeeds & navigates away → exactly ONE tx lands (AMD 5→4→ verified, then test sell deleted, 5 restored)** (`15`/`16`/`17`/`20`/`23`).
> 4. **FAB overlapped the last holding → FIXED.** Bottom content padding 24dp→96dp on `PortfolioOverviewScreen` + `HoldingDetailScreen` (the other FAB screens — conglomerate/chat/custom-assets — already had 96dp). **Device: last row ("PIMCO … 0 PEBIX") fully clears the FAB** (`06`).
>
> Tests **130/130** (+3 `TransactionFormLogicTest`: main-source balance pick, null-when-no-main, Max-sizes-to-Main). `./gradlew :app:assembleDebug` clean. Test hygiene: only a throwaway AMD sell touched the prod account and it was deleted (holding 5 + cash €7.901,36 restored, queue empty, verified). **Exit:** app LOGGED IN, app-lock OFF (the 2580 lock was wiped by `pm clear`, none re-set — clean launch), cache populated, `svc power stayon` ON. Polish batch (notification icon, conglomerate fractional/whole) to follow.

### [x] Step 18 — Settings & account management (spec §6.12) ✅ 2026-07-10
> Built + device-verified on prod: Account (live identity, change-password w/ proven 401 error path, delete-account wired to the now-LIVE `DELETE /account` behind a type-to-confirm + `DeleteAccountFeature.armed=false` tripwire), Security (2FA TOTP enroll w/ offline-ZXing QR — proven then abandoned unarmed; active sessions w/ revoke — proven via self-created session whose cookie 401'd after revoke; revoke-all-others dialog-only), full EN⇄DE per-app locale switch + complete 589-string `values-de`, About + hidden dev menu (7× version tap). 24 new tests (212 green). Unexecuted by design: real password change, 2FA confirm, email-2FA trigger, revoke-all-others, delete submit. Residue on account: harmless `totpPending:true` (no cancel endpoint — PLATFORM_ASKS P3) + a few idle self-created web sessions (rate-limit blocked cleanup).

Full settings: **Account** (username/email display, change password, **delete account** — type-to-confirm, destructive styling), **Security** (2FA: TOTP enroll with QR/secret, email codes, recovery codes, disable; active-sessions list with revoke / revoke-all-others; app-lock config from Step 17), **Notifications** (matrix from Step 16), **Language** (English/German in-app switch), **About** (wordmark + edition, version, tagline, links). Stub-and-note any endpoint not yet live per the master prompt.
**Done when:** password change works end-to-end; sessions list shows the web session and revokes it; language switch flips the UI.

### [x] Owner-queued follow-up + platform catch-up — code ✅ 2026-07-10 (chat/WS live-verify platform-blocked)
- [x] **Custom-asset "Value smoothing" checkbox** ✅ — create+edit UIs (EN/DE), flag persists + round-trips (PATCH 200); ALSO fixed the silent prod breaker: creation still sent the pre-V3-P2 category taxonomy the server now rejects (→ real Stocks/ETFs/… taxonomy, POST 201 proven). *Design decision (approved) ✅ 2026-07-11: the asset-detail "Value over time" chart now HONORS the flag — **smoothing ON → smooth interpolated (straight point-to-point) line; smoothing OFF → step/staircase chart** (unchanged). Implemented by parameterizing `BtStepLineChart` with a `BtLineInterpolation` (Step | Linear) mode that reuses the gold hero chart's simple-line form (no new chart component, no portfolio-repo baggage); selection via a pure `chartInterpolation(smoothing)` helper (unit-tested). Points/dots/gridlines/fill/axis behaviour identical in both modes — only the interpolation between points changes. Device-verified on prod: "Anthropic" (smoothing ON) → smooth diagonal line, "ZZ Test House" (smoothing OFF) → step chart.*
- [x] **Custom-asset LIST adapter (#387)** ✅ — real `GET /custom-assets`; no-holding asset proven visible.
- [x] ~~Chat LIVE end-to-end verify — PLATFORM-BLOCKED~~ **RESOLVED** — the 403 was the bearer-map gap, fixed platform-side as PR #404; live-verified via polling (see the entry two below).
- [ ] **WS realtime re-test (#396) — PLATFORM-BLOCKED (same P1)**: direct websocket + bearer still → HTTP 400 on current prod; client conforms to the #396 contract; retest with chat-live verify once the platform confirms the deploy.
- [x] **In-app "What's new" rework** ✅ 2026-07-10 — localized resource-backed model (EN+DE, 641 strings each), 7 update-buckets in plain consumer language, collapsed shows exactly the last 5 + "Show more"/"Show less" pill (split logic unit-tested), device-verified both languages; appending a release = one entry + a few `bt_wn_*` strings.
- [x] **Chat LIVE — verified via polling** ✅ 2026-07-10: conversations 200 (PR #404), real thread history incl. share-chip + nullable-senderId on live data, mark-read clears badge server-side; send-path proven by the owner's own live use (builder's single test-send was classifier-blocked — moot). WS instant delivery still awaits the platform's nginx upgrade fix (polling ~10s/~30s carries chat meanwhile); FCM push = #368.
- [x] **Chat composer double-inset fixed** ✅ 2026-07-10 (owner bug: "huge gap between input bar and keyboard"): root cause = no `windowSoftInputMode` declared → Samsung panned the window while the composer also applied IME padding; fix = `adjustResize` on MainActivity (correct for edge-to-edge; single inset source for all 6 IME screens), before/after device-verified flush-on-keyboard.
- [x] **Settings: orientation-lock toggle** ✅ (owner ask 2026-07-10, German): "Lock screen orientation" — ON by default (portrait as before); OFF follows the device sensor (tablet landscape). Persisted pref (`devicePrefs.orientationLocked`), applied in `MainActivity` on start + live on toggle; shipped with the 0.15 What's-new entry. *(Checkbox was stale — the feature was already live in-app.)*
- [x] **Price alerts — Workboard tab** (owner ask 2026-07-10, commissioned directly to a builder) ✅ app-side 2026-07-10 — the Workboard bottom tab is now a two-segment host (**Conglomerates | Alerts**): alerts manager with status-badged rows + per-kind condition lines, create/edit bottom sheets (debounced asset search w/ live-quote header, 6 condition kinds as chips, price/percent threshold field w/ per-kind validation incl. the ≤100% down-cap, repeat toggle w/ 24h-cooldown hint), delete-confirm, re-arm action for fired one-shots; `GET/POST /alerts`, `PATCH/DELETE /alerts/{id}`, `POST /alerts/{id}/rearm` per `packages/contracts/src/alerts.ts` + live openapi. Hardened the inherited draft: `refPrice` made nullable (contract sends null for 4 of 6 kinds — the non-nullable DTO would have crashed every list decode; regression-tested) + a no-change edit dismisses instead of sending the empty PATCH the server 400s. 39 new strings EN+DE; standalone conglomerate route untouched; `alert.triggered` deep-link already mapped (Step 16). 14 new tests → **242 green**. Device QA (prod): segmented host live, create-form E2E incl. live AAPL quote + validation + honest inline errors. **Live CRUD is PLATFORM-BLOCKED: every `/alerts` call 403s for the mobile bearer — missing `MODULE_POLICIES` entry, the exact class chat had before PR #404; filed as new P1 in PLATFORM_ASKS.md.** One device run re-verifies list/edit/delete/re-arm the moment the gate opens. **Scope activation + live-verify (2026-07-11) — STILL PLATFORM-PENDING, worse than a 403:** added `alerts:read`/`alerts:write` to `OAuthConfig` for the platform's incoming seed, but on prod the seed has NOT landed and requesting the un-seeded scopes makes the OAuth **authorize endpoint hard-reject the entire login** ("This app's authorization request is invalid") — not just a `/alerts` 403 (device-confirmed twice, fresh consent, ~4 min apart). To keep the app loginable (non-negotiable), the alerts scopes are **held behind `OAuthConfig.ALERTS_SCOPES_ENABLED = false`** (scopes wired + 4 tripwire tests; flip to `true` + rebuild + re-verify the moment the platform confirms the seed to the BetterTrackMobile client). With the flag off the bearer carries no alerts scope, so `GET /alerts` → **403** with the honest error state surfaced in-app ("API key is missing the required scope \"alerts:read\"", screenshotted). Full create/edit/delete/re-arm checklist remains blocked until the seed lands. Login + smoothing-chart checks passed this run; logged out, stayon restored. **STILL HELD OFF (2026-07-11, platform re-tick pending):** the platform **retracted** its earlier "seed is live" tick (PLATFORM_ASKS commit `61fad1b`) — prod's updater runs migrations but NOT the scope seed, so `alerts:read`/`alerts:write` are (per the platform) still not granted to the BetterTrackMobile client; a **migration-0030 fix** is in flight. Per that retraction the flag **stays OFF**: `OAuthConfig.ALERTS_SCOPES_ENABLED = false` and the tripwire test still asserts OFF (tree reverted to a **zero net diff vs HEAD 133dc80** on both files; **259 unit tests green**, flag-off `assembleDebug` reinstalled on the Note20 Ultra, app logged out at the login screen, `svc power stayon false` restored). **⚠️ CONTRADICTING DEVICE EVIDENCE — needs platform reconciliation before the next flip:** a live flag-ON run earlier this session (prod, ~00:29–00:49, fresh consent) did NOT hard-reject — the authorize page accepted the full scope set incl. `alerts:*`, returned the normal web-PIN unlock, and minted a scoped token; `GET /alerts` returned **200** and the FULL CRUD checklist PASSED live (created a `price_above` + a `pct_day_up` AAPL alert with correct condition lines/badges, edited a threshold with the PATCH persisting after a refetch, toggled repeat, deleted via the confirm dialog, list updated each time). So on the prod instance hit during that window the scopes WERE granted — reconcile with the platform's "not seeded" claim (transient? partial deploy? instance skew?) before re-flipping. All test alerts were deleted afterward (zero AAPL residue; the 3 pre-existing alerts — 2×BTC-EUR, 1×BAYN.DE — left untouched). **✅ FULLY LIVE END-TO-END (2026-07-11 ~03:00–03:24, prod, Note20 Ultra) — the seed landed durably and the flag is ON.** Platform durably ticked the alerts-scope seed (migration 0030, carried on deploy per PLATFORM_ASKS; bearer `/alerts` gate = PR #423). App-side: `OAuthConfig.ALERTS_SCOPES_ENABLED` flipped **`true`** and the 4th tripwire test now guards the flag staying ON (other 3 scope tests unchanged); **259 unit tests green** (OAuthScope suite 4/4), `assembleDebug` clean, reinstalled. **Fresh login (one attempt, no hard-reject):** logged out to drop the stale pre-flip token → "Login with BetterTrack" → the OAuth authorize page **ACCEPTED the full scope set incl. `alerts:read`/`alerts:write`** (warm web session → 4-digit web-PIN re-auth only) → new scoped token minted, returned into the app. **Read scope LIVE:** Workboard → Alerts loaded `GET /alerts` **200** with exactly the 3 owner alerts (BTC-EUR Above €55,000 Triggered · BTC-EUR Below €55,380 Triggered · BAYN.DE Below €49 Active) — no 403 error state. **Write scope LIVE:** AAPL "Price above $500" create (`POST /alerts` → new Active row, live quote $315.32 in the form) → delete via confirm (`DELETE /alerts/{id}`) → segment re-fetch confirmed the delete **persisted server-side** (back to the 3 owner alerts). This **RESOLVES the prior "not seeded / contradicting evidence" uncertainty** — the seed is confirmed live on prod. Cleanup: zero of my alerts remain, owner's 3 untouched, **logged OUT at the login screen, `svc power stayon false` restored**. Screenshots outside the repo in `/Users/cwiesi/bt_scratch/alerts-activation/` (`01`=live list, `02`=AAPL created, `03`=after-cleanup, `04`=server re-fetch, `05`=logged out).

### [x] Platform → Mobile integration batch (PLATFORM_ASKS 2026-07-10) ✅ 2026-07-10
Four app-side tasks from the "Platform → Mobile — new integration tasks" section. Built, unit-tested (**242 → 254 green**, +12: 7 switcher-logic + 5 version-DTO), and device-verified on the Note20 Ultra against prod (test account; logged out + stayon restored after).
- [x] **Portfolio switcher — archived grouping (P2, owner directive)** ✅ — archived portfolios moved out of the flat list into their own collapsible **"Archived"** sub-section (collapsed by default, neutral count badge, rotating chevron, 48dp tap target). The wobble-fix cap (`heightIn(max = 0.7×screen)` + inner `verticalScroll`) is **preserved** — expansion only makes the same single scroll taller, no nested scroll. Device: expand shows all rows; scrolled long list settles with **zero oscillation** (two frames 1s apart byte-identical); drag-to-dismiss still works. `switcherSections()` split is pure + unit-tested.
- [x] **Portfolio hard-DELETE (P3, platform PR #412)** ✅ — `DELETE /api/v1/portfolios/{id}` (bearer `portfolio:write`) wired in `PortfolioRepository.deletePortfolio` (raw-call → purge Room holdings/tx/cash/history/meta for that id → `refreshPortfolios()` re-pulls server truth; selection re-resolves to the promoted default if the deleted one was current). Delete action lives in **both** the archived-row ⋮ menu (Restore + Delete) **and** the active-row ⋮ menu (Rename + Archive + Delete), destructive/red styling. **Type-the-name-to-confirm** dialog + explicit EN+DE warning that transaction history is deleted and historical/tax figures change; `LAST_ACTIVE_PORTFOLIO` mapped to a friendly inline message (dialog stays open). Device E2E: created throwaway **"ZZ Del Test"** → archived (count 7→8, selection auto-fell back to Main) → deleted via type-to-confirm (count 8→7, row vanished, selection sane, **no crash, zero residue**). LAST_ACTIVE live-path NOT triggered on device (would require archiving the real Main/second — forbidden); covered by the error-code mapping unit test.
- [x] **Chat composer focus parity (P3)** ✅ — `ChatThreadScreen` composer now **autofocuses on open** (keyboard shows) and **retains focus after send** (no re-tap for the next message), via a `FocusRequester` + `LocalSoftwareKeyboardController` in `MessageInputBar`. `adjustResize` inset behavior **not regressed** — device-verified composer stays flush with the keyboard before and after send. One allowed test message ("test - ignore") sent to an existing @friedrich thread.
- [x] **Public API build-info in About (P3)** ✅ — new `GET /api/v1/version` (no auth) → `{ commit, shortCommit, builtAt }` wired via `BuildInfoRepository` (fail-soft) into an **"API build"** row on the About screen (row hidden on fetch failure, never an error) + the hidden dev/Sync-queue screen. `VersionResponse` decode + `formatApiBuiltAtDate` are pure + unit-tested. **Note for platform:** the live server currently returns `{"commit":"unknown","shortCommit":"unknown","builtAt":"unknown"}` (HTTP 200, wire-confirmed) — the app faithfully shows "unknown · unknown"; the build-info env vars aren't populated on the running deployment. When the platform wires them, the app shows the real shortCommit + date with **no app change**.

### [x] Notifications v2 go-live (PLATFORM_ASKS 2026-07-11 #4, platform PR #427) ✅ 2026-07-11
Step 16's stubbed seams unstubbed and wired LIVE against prod (**259 → 275 tests green**, `assembleDebug` clean, device-verified Note20 Ultra, logged out + stayon restored after; screenshots `/Users/cwiesi/bt_scratch/notifications-v2-2026-07-11/`).
- [x] **Device-token registration LIVE** — `POST /notifications/devices {token, platform:"android"}` (upsert) + `DELETE /notifications/devices {token}` (DELETE-with-body); `PushTokenManager` registration is bearer-gated (defers while logged out, fires on login + logged-in cold start + `onNewToken` rotation), best-effort bounded DELETE on logout **before** credential wipe (200 confirmed live). `stubDeviceRegistration=false` (kept as kill-switch) + tripwire test. Token value never logged/persisted (presence+length only). **Real FCM delivery stays platform-gated on the Firebase service-account key (#421)** — zero app work remains once it lands.
- [x] **Inbox LIVE** — debug sample inbox removed; `GET /notifications` authoritative (network/5xx surface for retry, scope-403 soft-degrades to empty). Device: 40 real server items incl. the 2 owner BTC-EUR `alert.triggered` rows; single + mark-all mark-read 200s; read state + badge **persisted server-side across force-stop+relaunch**.
- [x] **Prefs matrix synced incl. push** — server matrix grew to **4-channel cells `inapp/email/push/webpush`** (PATCH requires all 4 → app echoes `webpush` verbatim, proven 200), 10 types (3 beyond the app's taxonomy: `watchlist.shared`/`conglomerate.shared`/`friend.activity` — not surfaced, future enhancement), global `muted`/`channels` (ignored; per-type Mute stays app-local), `chat.message` now server-modeled. App's Push column migrated off SharedPreferences to server sync; PATCH round-trip device-verified incl. restore.
- [x] **PII log fix (coordinator)** — `AuthRepository` logged `Logged in as @username (email)` at INFO on every login (real leak found in the builder's captured logcat); now logs role only. PII sweep over all other `Log.*` calls clean.

### [x] WS realtime + E2E push retest (PLATFORM_ASKS 2026-07-11 #5 ask) ✅ app-side 2026-07-11 — WS LIVE, push platform-blocked (new P1)
Verification-only run (zero code changes; HEAD `1d5e280` build; screenshots+logs `/Users/cwiesi/bt_scratch/ws-push-verify-2026-07-11/`).
- [x] **Direct WebSocket LIVE from the app** — `wss://…/ws/?EIO=4&transport=websocket` with the account bearer: 101 → Engine.IO OPEN → Socket.IO CONNECT ack in **427 ms** (`sio.connect ok — realtime live` on-wire, tag `BtChatWs`); poll cadence auto-widened (list 30→60s, thread 10→25s); clean `1000` close on leaving chat; no reconnect churn. **Chat instant delivery is live end-to-end** — polling remains the safety net. Note: no `chat.message` self-echo on the sender's own socket (recipient-room-only emit; sender UI updates via its own REST round-trip — semantics confirm asked of platform).
- [x] **E2E closed-app push: SERVER-SIDE FAN-OUT GAP (new P1 filed in PLATFORM_ASKS §OPEN)** — device fully armed (token registered 200 on login, POST_NOTIFICATIONS granted, channel HIGH, server prefs `alert.triggered push=true`); born-breached test alert (BTC-EUR above €1, repeat off) flipped to `triggered` server-side after ~9–11 min, but **no `GET /notifications` row + no FCM send ever arrived** (14 min watch). Either born-breached alerts don't emit by design or trigger fan-out is broken. **Zero app work remains** — retest on the platform's word. Deep-link tap leg not exercisable without a push (resolver device-proven earlier via Step-16 simulate).
- Residue: one "test - ignore" chat message to @friedrich (not deletable); test alert deleted (owner's 3 alerts intact); nothing else changed; logged out, stayon false.

### [x] Step 19 — Polish, i18n & release prep (spec §6.13) ✅ 2026-07-11 (two passes)

Full **German localization** audit (every string externalized, EN + DE complete, locale-correct money/date formats), empty/error/loading sweep over every screen, accessibility pass (TalkBack labels, contrast, touch targets), reduced-motion audit, performance sanity (cold start, list scrolling), ProGuard/R8 release build, signing config placeholder, versioning, and a Play-listing asset checklist (icon, screenshots list, description draft) for the user.
**Done when:** release build installs and runs clean in both languages; TODO.md fully checked; a final summary report lists anything still stubbed for the user to resolve.

> 🟡 **Builder pass 2026-07-11 (device-verified, R8 release ran clean; ONE documented i18n gap left for the coordinator — see below). Tests 275 → 293 (+18).**
> **1. Transaction-form features (device-verified EN + DE, debug AND R8 release):**
>   - **Uncovered (over-)sell** (PR #429) — `allowUncovered` + optional `uncoveredEntryPrice` added to `TxOpPayload` (persists in the offline queue) + `CreateTransactionRequest` **and** `UpdateTransactionRequest` (synced-edit replays) + `ApiOpExecutor`. New pure logic `uncoveredSellState`/`uncoveredSellSubmitOk`/`parseUncoveredEntryPrice` (10 tests). UI: an amber `UncoveredSellCard` (warning + held/uncovered counts + 0%-basis explanation) with a **required "Sell anyway" ack** (48dp toggleable, `Role.Checkbox`) gating submit, plus an **optional native-currency buy-in price** field. Device: AMD (5 held) → sell 10 → card shows, "Record sell" disabled → tick → enabled; EN + DE copy correct ("Trotzdem verkaufen", "0% Rendite"). **Did NOT submit (real Main account) — server round-trip left un-exercised by design; gating/payload proven by unit tests + UI.**
>   - **Currency-aware price labels** — new shared `currencySymbol()`/`formatMoney()` in `MoneyText.kt` (mirrors `AlertFormLogic`); `AssetPick` gains `currency`, plumbed from holdings/tx/market/route (+ `assetCurrency` on `TxOpPayload` + `TransactionFormRoute` + `onTrade`). Price/fee suffix **and** the order Total now render the asset's native symbol; cash-after stays EUR. Device: AMD shows "557,890014 **$**" and Total "**5.578,90 $**" (DE grouping) — was "€". 8 tests.
> **2. i18n:** EN↔DE name parity **perfect (701=701, 0 missing/orphan)**; the brief's "esp." deltas (alerts, notifications v2, switcher, delete dialogs) already translated; **uncovered-sell copy** added EN+DE; DE money/date formatting spot-checked live ("5.578,90 $", "9.878,90 €", "0,0312"). **Chat externalized** (`ChatListScreen`+`ChatThreadScreen`, ~24 strings EN+DE — was 100% hardcoded; device-verified "Nachrichten"). ⚠️ **REMAINING GAP (coordinator to slot a focused follow-up):** the **Social screens are still hardcoded English** in release — `ui/social/SocialScreen.kt` (~40), `FriendOverviewScreen.kt` (~14), `SharedDetailScreens.kt` (~13), `AudiencePickerSheet.kt` (~19). Social reads are LIVE, so DE users see English there (confirmed on-device). Not in the brief's recent-delta scope; pre-existing from Steps 14/15. ~85 strings across 4 files.
> **3. a11y:** icon-button audit → one real defect fixed — the **password reveal/hide toggle** (`BtTextField`, used on login/change-password) had `contentDescription = null`; now labelled (`bt_action_show/hide_password`, EN+DE). New uncovered card is TalkBack-sane (toggleable Checkbox role, 48dp). Reduced-motion already handled by the design system (`btPressScale`/`BtMotion`).
> **4. perf:** LazyColumn `items()` are **keyed across the board** (good scroll perf); no `derivedStateOf` misuse; cold-start load already fixed in the resume batch (`SessionInitializer`). No speculative rewrites.
> **5. R8 release + signing + versioning:** `release { isMinifyEnabled + isShrinkResources }` + minimal keep rules (`app/proguard-rules.pro` — serialization DTOs/routes safety net, `SyncWorker`, OkHttp dontwarns; libraries ship their own). **APK 82.8 MB → 7.19 MB.** Signing falls back debug-key ↔ `~/.bettertrack`/`BT_KEYSTORE*` (nothing committed). CI `-PbtVersionCode/Name` intact. R8 seeds confirm 2148 `$$serializer` + routes + worker kept. **Device: R8 release installs + runs CLEAN** — login/session, portfolio, tx form (currency), notifications inbox (40 live items), German — all decode fine; logcat clean of ClassNotFound/MissingField/SerializationException.
> **6. Play listing:** `docs/PLAY_LISTING.md` written — owner action items, EN+DE short+full description drafts, 8-screenshot plan, content-rating + Data-safety drafts, permission justification.
> > 🟢 **Pass 2 (Social-DE completion + push retest, 2026-07-11): Step 19 gap CLOSED.** Social tab i18n DONE: `ui/social/` fully externalized (**103 new `bt_social_*` keys EN+DE**, incl. ViewModel toasts via a new locale-safe `SocialToast` type + `SocialRepository` detail→count refactor so "My shares" subtitles localize) — device-verified DE walk (friends / shared / friend-overview / shared-portfolio / my-shares / audience-picker with gender-correct per-kind strings) + EN round-trip; one DE stat label shortened to fit ("Buchg. G/V" — owner may rename); EN↔DE parity **831 = 831**, zero missing/orphan; **293 tests green**, assembleDebug clean. Screenshots `/Users/cwiesi/bt_scratch/step19-social-de-2026-07-11/`.
> 🎉 **Push retest coda — PASS (closes the fan-out P1 + Step 16's literal done-when):** after the platform's worker fix (#435/#436), a born-breached BTC-EUR alert (16:51:03) produced a **real FCM status-bar push at 16:51:52 (~49 s) with the app backgrounded**, deep-linking Workboard→Alerts, row Triggered, inbox row added. **The closed-app push loop is live end-to-end.** Test alert deleted (owner's 3 intact); the `alert.triggered` inbox row is non-deletable server residue.
**assembleDebug + assembleRelease + 293 unit tests all green.** Exit: app **LOGGED OUT** at login, **DEBUG** build installed (versionCode 1), language **EN**, `svc power stayon false`, WiFi/airplane untouched, **zero test data** (never submitted). Screenshots: `/Users/cwiesi/bt_scratch/step19-2026-07-11/`.

### [x] Owner directive — notification ARCHIVE + DELETE (+ friend-alerts ungate) ✅ 2026-07-11, LIVE-verified
Owner asks (chat + PLATFORM_ASKS #11/#12): implement the notification delete; remove the obsolete "Coming soon" on friend activity-alerts (owner confirmed the alerts already arrive).
- **Archive/delete UX (Notifications-v3, platform #437 / PR #440):** inbox **Active | Archived | All** filter; per-row overflow (Mark read / Archive / Unarchive / Delete); archive→**Undo snackbar** (undo re-inserts instantly — coordinator polish after live QA); per-item delete + bulk **archive-all-read / delete-all-archived / delete-all** behind destructive confirm dialogs with counts; **bell badge = server unread-ACTIVE only**. Behind `NotificationFlags.archiveDeleteEnabled` (kill-switch, ON) — flag-off = exact pre-#437 behavior, no `view=` param (prod 400s on unknown `view` pre-deploy — probed). Repo/DTOs/routes per as-shipped #13 semantics (one path reconciled: `POST notifications/archive-all-read`).
- **Friend-alerts ungate:** "Coming soon" badge removed from `FriendOverviewScreen` (toggle live-verified round-tripping: `PUT social/shared/activity/...` + toasts); `bt_social_coming_soon` string dropped EN+DE.
- **First-class inbox rows** for `friend.activity` / `watchlist.shared` / `conglomerate.shared` (icons, EN+DE copy, deep-links: friend→overview, conglomerate→shared view, watchlist→Social; payload-tolerant fallbacks) — verified on a real `friend.activity` row ("friedrich bought PEN.TA." → @friedrich overview).
- **LIVE E2E verification on prod (Note20 Ultra):** default view + 0=0 badge ✓; archive→Archived→unarchive round-trip server-confirmed ✓; undo server-correct ✓; per-item delete of OUR `alert.triggered` BTC-EUR test row → gone from all views, survives refresh + cold restart ✓; bulk archive-all-read **executed for real** (account left tidy: Active=unread-only, 47 archived — matches the owner's desired UX) ✓; both bulk-delete dialogs opened with correct counts and **cancelled** (never run against prod) ✓. No deviations from #13.
- **322 unit tests green** (+29 for archive/delete logic incl. undo-restore); EN↔DE parity **859 = 859**; screenshots `/Users/cwiesi/bt_scratch/notif-archive-delete-2026-07-11/`. Exit: logged out, EN, stay-awake off.

### [x] Idempotency-key adoption (#432) — code ✅ 2026-07-11, GATED OFF (prod bug found) + search-bar inset fix
- **Full client adoption shipped**: the queue's existing persisted `clientId` UUID (unique-indexed, minted at enqueue — no schema change, real migrations confirmed, pre-existing queued ops already carry a key) is sent as `Idempotency-Key` on exactly the #9 endpoints the queue uses (tx-create, cash deposit/withdraw/transfer, custom-asset value points; nullable header — non-queue callers unchanged). Handling: 409 `IN_PROGRESS` → transient retry (same key); 409 `MISMATCH` → permanent needs-attention; 400 `KEY_INVALID` → regenerate once, persist, then permanent; >48 h caveat documented in code. Note-marker reconcile retained as defense-in-depth. 15 new tests (11 MockWebServer wire-level in `ApiOpExecutorTest` + 4 engine), **337 total green**.
- 🔴 **Shipped OFF behind `SyncFeatureFlags.IDEMPOTENCY_KEYS_ENABLED=false`: device A/B proved the header itself 500s `POST …/transactions` on prod** (same body: header → 500 ×5 incl. fresh-key repro; no header → 201; GETs 200 throughout). Filed as P1 in PLATFORM_ASKS #15 with exact timestamps. **Flip the flag + re-verify when the platform ticks the fix**; offline sync meanwhile stays exactly-once via the existing client-side reconcile. Replay-memoization unverifiable live until then (client side proven by wire tests; controlled double-send impossible by design — reconcile-first; curl fallback skipped: bearer materialization permission-denied, stopped per rules).
- **E2E otherwise green**: offline enqueue (WiFi off 21:21:48→21:25:22 CEST only) → pending badge, totals honest → online auto-drain (flag-off) 201 → cleanup deleted both test txs (204 ×2), BTC-USD back to baseline 0.0312, cash untouched, zero pending. One **pre-existing queue-history row flagged, untouched**: "Buy NVDA · 3 × €122.83 · Added 21:17 · Synced" — predates the builder's device work, not created this run; surfaced to the owner (may be stray earlier test data server-side).
- **Owner ask — asset-search bar clipping FIXED**: root cause = `SearchScreen`'s hand-rolled Scaffold `topBar` Row had no status-bar inset (M3 only insets the body; `TopAppBar` elsewhere self-insets) → drew under the status bar edge-to-edge. Fix: `.statusBarsPadding()` on that Row (matches the app's inset pattern). Device-verified before/after incl. keyboard-open + live results. Screenshots `/Users/cwiesi/bt_scratch/idempotency-2026-07-11/`.

### [x] Idempotency LIVE + owner batch (prefill truncation, shared-screen clipping) ✅ 2026-07-12
- **Idempotency keys ON and re-verified live** after the platform's #441 fix (missing `idempotency_keys` table — migration skipped by a corrupt journal stamp; their root-cause answered our shared-vs-tx question: shared): header on the wire, tx-create **201** (500 pre-fix), drain completes, **exactly-once held under a reconnect flap AND an app-kill mid-POST** (same key resent, zero duplicates both times). `SyncFeatureFlags.IDEMPOTENCY_KEYS_ENABLED=true` (kill-switch retained). Results posted as PLATFORM_ASKS #19.
- **Owner directive — money-prefill truncation** (narrowed from the #18 display rules to INPUT autofills): new `truncateMoneyForPrefill` — market-data money prefills (date→price link) cut to cents, string-safe (231.499320001 → 231.49, never rounded; float-artifact-proof via shortest-decimal form — also hardened `formatDecimalForInput` the same way); sub-cent prices keep 6 significant decimals; stored-value edit prefills exact; quantities untouched; user input untouched. 6 new tests; **owner live-verified on device**. Full display sweep deferred until the platform posts the web formatter's as-shipped edge cases (their #18 tail).
- **Owner bug — shared portfolio/watchlist/conglomerate top clipping FIXED**: the three shared view screens dropped their Scaffold's inner padding (content lambdas ignored the passed Modifier — bare `fillMaxSize()` LazyColumns drew under the app bar, hiding the owner header). Fix: `SharedScaffold` applies the padding centrally (`Box(fillMaxSize().padding(pad))`), callers can no longer drop it. Reproduced + verified on device via both entry paths (Social and chat-card View); before/after in `/Users/cwiesi/bt_scratch/bugbash-2026-07-12/`.
- Owner-reported "error opening own portfolio" = the evening's flaky-WiFi fetches (his old work-location network; phone now on hotspot) — not reproducible on a stable link, closed without code change. **343 unit tests green.** NVDA queue-history row resolved: real server-side data (multiple buys, executed 2025-05-12) — not test residue, untouched.

### [x] CI stable APK signing LIVE (in-place "Update" works) ✅ 2026-07-12
- **Owner ask closed** ("still can't press Update on GitHub APKs"): root cause = the `BT_SIGNING_KEYSTORE_B64`/`BT_SIGNING_PASS` repo secrets were never set, so the Step-V workflow's silent fallback signed every CI APK with the runner's throwaway debug key (new signature each run → Android refuses in-place update). No code change needed — workflow + gradle scaffold (2026-07-10) were already correct; secrets uploaded via `gh secret set` (keystore + pass live only in `~/.bettertrack/` and GitHub secrets, never the repo).
- **Verified**: runs #57 and #58 (workflow_dispatch) both log "Stable dev signing configured."; both APKs' V2 cert SHA-256 = `b5388b40…9a568802` = the local `~/.bettertrack/bt-dev-signing.keystore` cert — CI↔CI and CI↔local now share one identity, versionCode strictly increases per run (57→58). First stable-signed build = **0.57**; rolling `latest-debug` now serves 0.58.
- **One-time migration**: any install predating 0.57 (throwaway-signed) needs a final uninstall → install of a ≥0.57 APK; every later APK then updates in place via the system dialog.

### [x] In-app update flow: Download & Install + auto-check toggle ✅ 2026-07-12
- **Owner directive** (built by an Opus builder, coordinator-verified): update dialog actions restructured — **Download & Install** (filled primary) ▸ **Go to GitHub** (outlined) ▸ Remind later / Skip this version (side-by-side tertiary, owner-steered mid-build). In-app download streams the `latest-debug` release APK (~80 MB debug; GitHub→CDN redirects followed, no call-timeout cap) to `cacheDir/updates/` with live **%** progress, then commits a **PackageInstaller session** (system confirm sheet — never silent). `REQUEST_INSTALL_PACKAGES` in manifest; non-exported explicit status receiver launches the confirm sheet on PENDING_USER_ACTION.
- **Install-source hint on the progress screen**: `canRequestPackageInstalls()` false → prominent card + deep-link button to `ACTION_MANAGE_UNKNOWN_APP_SOURCES` (on Samsung this opens the unknown-sources LIST, OEM routing — grant verified working); granted → subtle one-liner. Cancel available in every state (incl. Installing, so a denied source can never dead-end).
- **Cleanup belt-and-braces**: APK deleted on any failure/cancel; `sweepOnStart()` wipes `cacheDir/updates/` on every launch (covers post-success — the install kills the process — and stale partials; proven twice on device incl. a planted APK+`.part`).
- **About toggle "Automatic update checks"** (default ON, DataStore-persisted, survives logout): OFF hard-gates the launch/foreground check + clears any shown dialog; ON re-checks immediately. New files `data/update/UpdateInstaller.kt`, `UpdateInstallReceiver.kt`, `UpdateInstallLogic.kt` (pure, tested); EN+DE strings, parity kept. **Tests 343 → 352 green** (coordinator re-run).
- **Device E2E** (screenshots `/Users/cwiesi/bt_scratch/update-flow-2026-07-12/`, DE locale so DE strings validated): real download of 0.58 with % → source-permission hop → confirm sheet → installed + relaunched **still logged in** (session survives updates); failure path (WiFi killed mid-download) → error + Retry, no partial left; toggle OFF/ON behavior verified. Exit: feature build installed, logged out, stayon false, WiFi on, `cache/updates` gone.

### [ ] Step 20 — Play Store release readiness (the publishing gate)

*Added by the platform coordinator (Chief of Development on the main-repo side), 2026-07-08 — everything Google Play requires before the app can be accepted for production. Split into **app-side** (this repo) and **platform-side** (main api/web — tracked in `PLATFORM_ASKS.md`, the coordinator drives these through the factory). Requirements verified against Play Console policy as of July 2026; re-check before submitting, Play tightens these yearly.*

> 🟢 **App-side batch + web-parity number sweep DONE 2026-07-14** (builder-built, coordinator-verified: **386 unit tests green**, both flavors assemble, EN↔DE parity **862 = 862**, .aab cert + device exit state re-checked).
> **Formatting sweep (PLATFORM_ASKS #18/#19, display-only):** new canonical `ui/format/BtNumberFormat.kt`; the shared helpers (`formatEur`/`formatMoney`/`formatPercent`/`formatPrice`/`formatQuantity`/`formatWeight`) delegate to it — half-away-from-zero money, symbol-last, em-dash for null/NaN/±Inf, DE %-space (EN none) + unsigned zero on signed percents, quantities ≤ 8 decimals trimmed, sub-cent unit prices 6 significant decimals. Leaks fixed: `SharedDetailScreens` private dot-separator weight formatter → shared helper; asset-page "≈ EUR" line now sub-cent-aware (a €0.000012 price never renders 0,00 €). Device-verified EN + DE incl. SHIB-USD "0,00000424 $" and DE "+0,83 %". 30 new tests.
> **Play release engineering:** `github`/`play` product flavors (dimension `distribution`, SAME applicationId — Firebase/OAuth depend on it): github = today's self-update dev channel (`REQUEST_INSTALL_PACKAGES` moved into `app/src/github/AndroidManifest.xml`, `SELF_UPDATE_ENABLED=true`), play = the whole updater compiled out (flag false, permission absent — Play forbids self-update); `com.google.android.gms.permission.AD_ID` stripped from every flavor (`tools:node="remove"` — no ads/analytics). CI workflow → `assembleGithubDebug` (rolling `latest-debug` channel unchanged). **Real upload keystore** `~/.bettertrack/bt-play-upload.keystore` (RSA-4096, alias `btupload`; passwords only in `play-signing.env`, chmod 600, never in the repo) signs the **first .aab: vc 10001 / vn 1.0** → `/Users/cwiesi/bt_scratch/step20-2026-07-14/BetterTrack-1.0-vc10001.aab` (upload-cert SHA-256 `27:0C:3F:4D:…:BD:3F:19`; Play channel = 10001+/1.x, GitHub dev = 0.<run#> — deliberately disjoint).
> **Account deletion ARMED (`DeleteAccountFeature.armed=true`) + live-verified BOTH paths:** wrong-password probe on the owner's account (server 401 surfaced inline, nothing deleted) AND a real success-path delete of a **builder-created throwaway** ("garbageuser", registered via the web signup during the run) — deletion 204 → token dead → full local wipe → login screen; throwaway gone, zero residue. 4 new MockWebServer tests (204/401/429 mapping). One deviation for the record: to switch accounts the builder signed out the owner's WEB session inside the login browser (owner just re-logs in on the web; nothing account-side changed).
> **Permissions audit (playRelease merged manifest):** INTERNET, ACCESS_NETWORK_STATE, POST_NOTIFICATIONS, USE_BIOMETRIC (+lib USE_FINGERPRINT), Firebase WAKE_LOCK/RECEIVE_BOOT_COMPLETED/c2dm.RECEIVE, WorkManager FOREGROUND_SERVICE; **AD_ID and REQUEST_INSTALL_PACKAGES absent**. Full table in the runbook.
> **Store assets:** `docs/play/icon-512.png` + `docs/play/feature-graphic-1024x500.png` (no account data → in repo); EN+DE screenshot candidates (real data → NOT in repo) in `/Users/cwiesi/bt_scratch/step20-2026-07-14/screenshots/`. **Runbook: `docs/PLAY_RELEASE_CHECKLIST.md`** — full Play-Console sequence, [APP]/[OWNER] split, versioning scheme, audit table.
> **REMAINING for Step 20:** owner console actions (create app, Play App Signing enrollment on first upload, closed-testing track + **12-tester/14-day gate**, identity verification, IARC, Data Safety + Financial-features declarations, listing copy/screenshot approval) + the platform's privacy-page completeness pass (PLATFORM_ASKS § OPEN P2). Coordinator polish note: server error strings (e.g. "Current password is incorrect.") pass through untranslated on DE screens — candidate for a later error-code→string map.
> 🟢 **Launch-support batch DONE 2026-07-15 (overnight):** (1) **Store screenshots from a demo account** (owner decision — no real data on the store): owner provisioned a dedicated reviewer/test user; builder populated a persistent demo world ("Growth Portfolio", 8 backdated buys 2024→2026 across VWCE.DE/AAPL/MSFT/NVDA/BTC-EUR/ETH-EUR, Max **+42.60%**, ≈€46.3k net worth, cash+dividends; watchlist "Tech Radar"; conglomerate "AI Leaders" w/ backtest; 2 demo alerts) and shot **8 screens × EN+DE (1080×2316) + 16 framed 9:16 variants** → `/Users/cwiesi/bt_scratch/play-shots-2026-07-14/` (kept out of the repo; supersedes the earlier real-data candidates). Demo data PERSISTS — the account doubles as the Play-reviewer login for the App-access declaration. (2) **About → "Privacy policy" row** (opens the live privacy page; EN+DE, tap-through device-verified; parity **863 = 863**, tests **386** green). (3) **.aab regenerated** (same vc 10001 / vn 1.0, upload-key signature re-verified) so it carries the About row + display sweep. (4) Board work: sub-cent autofill owner-ruling relayed (web adopts the visible 6-sig-dec way, #31); **primitive ToS + GDPR privacy upgrade + Impressum ordered from the platform (#31)** — the last non-owner launch blocker. Ops: the ~00:00 5h-limit death auto-recovered via the in-session reset cron (01:50 reset → 02:15 revival); standing owner budget rule: at 30% weekly usage, finish in-flight tasks, then pause.
> 🟢 **Owner bug-report batch DONE 2026-07-16** (two builders, coordinator-verified, **403 unit tests green**, all device-verified on R5CN80ABXBK): (1) **Notification deep-link back-stack fix** (`b8d55ed`) — a Social deep link now uses the bottom bar's tab-switch semantics (popUpTo start + saveState/singleTop/restoreState) and drops the inbox off the stack first; Portfolio taps no longer bounce back to Social, inbox-tap and cold-push paths both verified. (2) **Provider-search polling parity with web** (`251b0fe`) — poll every 1.5 s while `enriching`, 10 s cap (web's ENRICH_POLL_MS/ENRICH_TIMEOUT_MS), stale "Searching providers…" row fixed; live-verified with uncached HARVIA → G1U.F Frankfurt appearing on its own. (3) **10 s hard cap on queued sync attempts** (`52ec9fd`) — a stalled server can't hold an op in-flight at the FIFO head; timed-out send → one bounded reconcile → done if landed, else parked needs-attention with a "timed out" message + existing Retry/Remove (same idempotency key on retry = exactly-once); virtual-time engine tests. Note: the timed-out park message follows the existing English-constant pattern (`MSG_KEY_INVALID`) — folds into the known error-code→string-map candidate for DE.

> 🟢 **Platform v4 drop absorbed — notifications package DONE 2026-07-17** (one builder, coordinator-verified, **422 unit tests green** (+19), device-verified on R5CN80ABXBK against **live v4 prod**; commit `9d6b6b0`): (1) **Six-channel PATCH compatibility** — v4's strict `notificationTypeRoutingSchema` requires six booleans per cell (telegram/discord added); the app now echoes exactly the channel keys the last GET carried (four pre-v4, six on v4). This was a silent breakage-in-waiting: the old four-key PATCH gets 400 on v4 prod — verified fixed (PATCH 200 live). (2) **Telegram/Discord matrix columns (#495)** — rendered only when the server `channels` availability reports them configured. (3) **read=archive inbox (#474)** — unread Active list + Archive history, one Archive affordance per row, toolbar "Archive all"; live-observed (read row dropped from Active cleanly). (4) **Final deep-link matrix (V4-P0c)** — follow.published/follow.alert.created/follow.alert.fired/account.notice kinds added; PublicProfile(username) resolves via friends list at tap time → Social fallback (app has no by-username profile screen — non-friend followees land on Social, never a dead tap); alert types with no assetId land on the inbox (no standalone Alerts screen). (5) **Announcements (#493)** — account.notice rows render + deep-link to notification settings; live announcement verified. (6) **Unknown chat-chip fallback (#502/#503)** — `ShareChipKind.fromWire` fell back to Asset (an `idea` chip would have navigated to a bogus asset page); now falls back to a neutral non-navigating Unknown chip. **Still open from the v4 drop:** ~~retiring the `[bt:<uuid>]` note-marker~~ (done below); four now-unused notification string keys left in place; DE error-string map still pending.

> 🟢 **Note-marker retirement DONE 2026-07-17** (#417 tail; builder + coordinator inline fix, coordinator-verified, **428 unit tests green** (+6), device-verified incl. Room v4→v5 migration on-device; commit `3a8ca5f`): notes go verbatim (no more `[bt:<uuid>]` pollution visible on web); `OpExecutor.lookup` removed — ambiguous ops reconcile by replaying the same Idempotency-Key (landed → server replays stored 2xx; never-landed → runs exactly once); new `firstAttemptAtMs` streak stamp + 40 h `REPLAY_SAFE_WINDOW` park (server TTL ≥48 h); **owner's 10 s-timeout semantics preserved** (coordinator caught + fixed a builder regression where a hung op would have auto-replayed at the FIFO head for up to 40 h): fresh timeout → ONE automatic replay → second timeout parks "timed out" with Retry/Remove, successors flow (~10 s + backoff + 10 s worst case). Direct tx edit/delete now send per-submission keys. Legacy display-strip kept for old rows. Known leftovers: `SyncDebugController.cleanupTestData` still greps `[bt:` (debug-only, finds nothing new — harmless); `mergeNotePreservingMarker` re-attaches a legacy row's existing marker on edit (by design, no new markers minted); timeout/park messages remain English constants (DE map backlog).

**App-side (this repo):**
- **Target API level** — new submissions must target **API 35 (Android 15)** minimum (Play policy since Aug 2025). We currently target SDK 36, which satisfies it; keep current with Play's rolling target-API policy and re-verify at build time.
- **Release artifact** — signed **Android App Bundle (.aab)**, not APK; enroll in **Play App Signing**; real upload keystore + signing config (replace the Step 19 placeholder); R8/minify + resource shrink on; version code/name bumped.
- **Permissions audit** — ship only what's used: `INTERNET`, `POST_NOTIFICATIONS` (Android 13+ runtime, asked in context), biometric (app-lock), FCM. **No location/contacts/SMS/restricted permissions** — this keeps the Data Safety form small and avoids the sensitive-permissions declaration + video review. Remove anything unused; declare foreground-service types only if actually used (the sync path is WorkManager background work, so likely none).
- **Account deletion — in-app path** — the Step 18 type-to-confirm delete flow must call the **live** platform deletion endpoint (see platform-side). Play requires in-app deletion when an app has accounts.
- **Store listing assets (EN + DE)** — 512×512 icon, 1024×500 feature graphic, ≥2 phone screenshots, short + full description, category **Finance**, contact email, and the **privacy-policy URL** (below).
- **In-app declarations wiring** — nothing that contradicts the Data Safety form (e.g. don't collect data the form doesn't declare); crash/analytics SDKs (if Sentry etc. is ever added) must be declared.

**Platform-side (main api/web — coordinator's factory work, mirrored in `PLATFORM_ASKS.md`):**
- **Self-service account deletion — BOTH in-app *and* a public web URL** — Play mandates account deletion be reachable in-app **and** via a web resource, and that it deletes the associated user data. Needs: a bearer-callable delete-account endpoint (for the app) **and** a web-facing deletion page/flow on the site. *(This is the single hardest publishing blocker — schedule it deliberately.)*
- **Privacy policy page** — a stable public URL (e.g. `https://bettertrack.at/privacy`) describing data collected, use, sharing, retention, and the deletion path. Required to complete Data Safety and shown in the listing.
- Bearer identity + the rest of the built-but-stubbed Settings/Security/Notifications endpoints and scopes (see `PLATFORM_ASKS.md`) so Steps 16 & 18 go fully live before submission.

**Owner / account actions (not code — flag to Christian):**
- **Closed-testing gate** — if the Play Console account is a **personal** account created **after 2023-11-13**, production access requires a **closed test with ≥12 testers opted-in continuously for 14 days** (Google reduced this from 20 to 12 in 2026). This is a hard time-gate — stand up the closed track and recruit testers **early**, in parallel with Step 19, not after. Org accounts and pre-2023-11-13 personal accounts are exempt.
- **Developer identity verification** complete on the Play Console (identity/address; D-U-N-S if an organization account).
- **Content rating** — complete the IARC questionnaire (finance/utility, no objectionable content → expected "Everyone").
- **App content declarations** — Data Safety form (declare account data, financial info = portfolio holdings/values, device/FCM push token, encryption-in-transit, deletion available — must match the privacy policy), **Financial features** declaration (portfolio tracker that does **not** execute trades or move money → declare accordingly, no money-transmission license implied), Ads = none, target audience = adults (not child-directed).

**Done when:** a signed `.aab` is uploaded to a closed-testing track; Data Safety + content rating + all app-content declarations are complete and consistent with the published privacy policy; in-app **and** web account deletion both work against live endpoints; the 12-tester / 14-day closed test is running or complete; and a Play-Console release checklist (each item → green) is captured in this repo for Christian's final submit.

## V5 holiday-sprint plan (2026-08-04) — owner mandate via platform chief (board: v5 drop parts 1+2 + addendum)

Context: owner on ~30 h holiday, both sides autonomous. **Prod is OFFLINE deliberately** — the ONLY live target is the local dev backend on this Mac: API `http://localhost:3000` on the phone via `adb reverse tcp:3000 tcp:3000` (+ `tcp:6771` for the web/consent origin `http://localhost:6771`; re-arm after every replug), Mac LAN fallback `http://192.168.0.114:3000`. Dev login: the seeded `demo` account (credentials on the board, main-dev-provided). OAuth client seeded with all 14 scopes + new `cash:read/write` `mirrorchain:read/write` (migrations 0079/0080); activation = request scopes + re-login. Platform chief sweeps the board hourly, fixes platform issues same-day. App facts pre-verified: global `ignoreUnknownKeys=true`, OAuth callback already handles `error=access_denied`, no account-export/tax code (those v5 breaking changes don't apply).

- [x] **S1 (P1) — dev-backend hookup:** debug-only runtime override for API_ORIGIN/WEB_ORIGIN (persisted, editable in the secret dev menu, restart-applied; gradle-property fallback stays), adb reverse re-arm + verify, add the +4 scopes behind a guarded flag (alerts-scope pattern — un-seeded scope request hard-rejects login; dev has the seeds, prod gate noted on board #39.4), demo login, smoke pass of existing features against dev. Unblocks every live test for the sprint.
- [ ] **S2 (P1) — v5 wire absorption:**
  - [x] **S2a defensive:** global `PARANOID_MODE` 403 interceptor → designed "paranoid account" state (never generic error/€0); intraday history — optional `time` on history/performance points, chart keyed on `time ?? date` (1D/1W/1M are dense curves now); mirror-seam write errors in sync engine + repos (409 `MIRROR_CONFLICT`/`MIRROR_ROW_DELETED`/`CASH_MOVEMENT_NOT_EDITABLE` → park with clear message; 503 `MIRROR_SYNC_STALLED` → retry+backoff, NOT park); register 11 new notif types (`dividend.event`, `budget.exceeded`, 8× `mirror.*` grouped) + synthetic `notifications.digest` (deep links: dividend→asset, budget→inbox, mirror→inbox/social, digest→inbox; unknown-type fallback stays); ETag conditional GETs (`If-None-Match`, weak ETags) on `GET /portfolios/:id`, `.../history`, `/search` → 304 serves cache.
  - [x] **S2b quick wins (scopes held):** cash movement edit/delete (`PATCH/DELETE .../cash/movements/:id`, editable kinds deposit/withdrawal/fee only, Idempotency-Key, solvency-replay 409s) + `POST .../cash/fee`; digest cadence + quiet-hours settings UI (compact; echo-verbatim PATCH discipline); discreet mode (flag on `/settings/account` + app-wide amount masking); `source` badges (`manual|standing-order|import:*|sync:*`) + `?source=` filter; mirror chain badge + `mirror.addedBy` attribution on group-portfolio rows; standing-orders CRUD.
  - [~] **S2c features (newly unblocked by addendum):** (S2c-1 below; S2c-2 = market intel + social + mirrorchain read + ideas remains) cash tags/budgets/rules/summary/trends (`cash:*`); mirrorchain read + invite accept/decline + leave (`mirrorchain:*`; admin stays web); market intel on asset detail + portfolio dividend calendar/projection/news digest (gate on `available`); comments + reactions on shared items; friend groups as audiences; `POST /backtest/compare`, `POST /backtest/shared/:id/preview`, `GET /conglomerates/:id/resolved`.
- [~] **S3 (P1) — storage-mode architecture + first-run wizard:** (W1 seam DONE below; wizard = W5) storage-backend abstraction (server adapter = today’s API client; Drive adapter = new; design so Drive-only can attach a server account later); first-launch wizard: data home = BetterTrack server / Google Drive (autonomous) / both.
- [~] **S4 (P1) — Drive-autonomous mode:** (W4 core merged below; W5 wizard + W6 market-data remain; device gates blocked on Google OAuth client) Google Sign-In + Drive `appDataFolder` persistence; local domain engine ported from platform `packages/domain` as LITERAL translation with its test vectors as conformance suite (never reimplement-from-understanding); `packages/contracts/src/vault.ts` entity kinds as schema baseline; `BTVAULT1` envelope (AES-256-GCM, Argon2id-wrapped key) for future byte-compat; direct-provider market data with graceful "no live prices" degradation; zero-connectivity operation.
- [ ] **S5 (P2) — paranoid-with-server:** `vault:sync` adapter when the platform ships it (board-watched); OAuth-for-paranoid corner case (authorize needs unlocked vault web-side).
- [ ] **S6 (P2) — UX overhaul sweep:** audit every screen/flow for ambiguity/friction (owner's standing complaint), post the debt list on the board, burn it down — affordances, dead-ends, navigation consistency, loading/empty/error everywhere, discoverability (chat, alerts, sharing); DE i18n + empty/error/loading bar to platform standard (incl. the long-standing error-code→DE-string map).
- [ ] **S7 (P3) — polish pass:** animations, haptics, edge-to-edge, tablet sanity, dark-mode consistency — the "Android 2026" bar.

> 🟢 **S1 + S2a DONE 2026-08-04** (Builder A, coordinator-verified, **482 unit tests green** (+54), device-verified on R5CN80ABXBK against the LOCAL DEV backend): runtime origin override (debug-only: Settings → Developer → Dev backend + login-wordmark long-press; cleartext allowlisted to localhost/LAN in a debug-only manifest overlay — release merged manifests verified clean); +4 v5 scopes behind `V5_SCOPES_ENABLED` (18 scopes confirmed on the wire); demo login + full smoke pass; paranoid-mode interceptor + designed screen (Portfolio surfaces only); intraday `time` points + millis-keyed chart with new 1D/1W ranges (dense curve device-verified); mirror-seam 409s park with clear messages, 503 MIRROR_SYNC_STALLED retryable; new notif kinds (dividend.event/budget.exceeded/mirror.* prefix-matched/notifications.digest) + EN/DE strings + deep links (cold-start dividend.event → asset detail verified); ETag conditional GETs with 304-replay (real 304 observed; coordinator fixed a totalBytes accounting drift on LRU count-eviction). Screenshots: `/Users/cwiesi/bt_scratch/v5-hookup-2026-08-04/`. Polish note for S6: 1D curve's last segment renders as a long straight diagonal (gap between last dense point and the live point). App-side gap for S2c: no ideas surface (2 seeded ideas unreachable). Raw-wire cash labels (`fee`/`dividend`/`tax_withholding`/`tax_refund`) render untranslated — S2b fixes.

> 🟢 **S2b DONE 2026-08-04** (Builder B, coordinator-verified, **551 unit tests green** (+69), device-verified against dev with wire evidence — every PATCH carried only user-changed keys, echo-verbatim proven on the wire): cash movement edit/delete (fresh-UUID edit keys, stable delete keys, DELETE-response repaint via `applyMovementDeletion`); `fee` entry kind (POST 201) with drag-performance hint; all cash movement kinds properly translated EN+DE with icons + safe unknown fallback; digest cadence + quiet-hours compact "Delivery" section (hidden entirely on pre-v5 GETs); discreet mode with TOTAL masking by construction (masking inside `btFormatMoneyCore`/`btFormatUnitPriceCore` on Compose snapshot state — new screens cannot forget; press-and-hold hero peek); source badges (`manual|standing-order|import:*|sync:*`); mirror overlays live-verified on the seed's real chain portfolio (gold "Group · 2 members" switcher badge, "From group" row attribution). Room v5→v6 (provenance/mirror columns, defaults match server semantics). Builder corrections vs brief: solvency refusal is 400 `INSUFFICIENT_CASH` (not 409); `mirror.sync` has 4 keys. NEW: real EN↔DE `StringParityTest` (key sets, plurals, placeholder-mismatch crash guard) — parity was previously hand-checked only. Standing orders deferred to S2c. Screenshots: `/Users/cwiesi/bt_scratch/v5-s2b-2026-08-04/` (25).

> 🟢 **W1 DONE 2026-08-04** (Builder C, coordinator-verified, **581 unit tests green** (+30), both release assembles pass, device smoke = zero behavioural difference): `StorageMode`/`StorageModeStore` (plain prefs, survives wipe; grandfathering fired once on-device → `server`); `PortfolioBackend`+`ServerPortfolioBackend` and `MarketDataSource`+`ApiMarketDataSource` extracted VERBATIM (multiset of api./db. call sites byte-identical; PortfolioRepository 793→~420 lines; zero UI/VM signature changes); `ModeRoutingOpExecutor` dispatching on the op's own `backendTag` (Room v6→v7 on-device against the live DB — queued op preserved with tag backfilled); mode-aware `hasSession` + logout wipe scope (`LogoutWipeRuleTest`); debug storage-mode row; **per-origin v5 scope gating** (`v5ScopesAllowedFor(effectiveApiOrigin)`: prod 14 / non-prod 18 — supersedes the flat flag, board #42.1). Deviations from plan documented in-tree (backend returns, repo composes projection tails; MarketDataSource keeps today's types, no fx() yet). ⚠️ Pre-existing bug found for S6: debug "Clean up test data" archived the live selected portfolio instead of matching test data (restored on-device; helper is only safe on a "ZZ App Test" portfolio). Screenshots: `/Users/cwiesi/bt_scratch/v5-w1-2026-08-04/` (40).

> 🟢 **W2 DONE 2026-08-04** (Builder D, coordinator-verified, **752 unit tests green** (+171)): `packages/domain` holdings/seriesStats/settingsScope ported LITERALLY to pure-Kotlin `at.bettertrack.app.domain` (1,761 lines, zero Android deps — only java.time/util.Locale/kotlin.math); vendored pinned snapshot `tools/domain-vectors/` @ `cb530f7e` (byte-identical to the plan's `439d0d5d` pin for all vendored paths — verified); vector generator (`node --experimental-strip-types`, ZERO npm installs, byte-identical re-runs) emitted 149 vectors + 6-entry skip manifest, all hand-ported; **serverTwrParity golden byte-exact on all 34 points** (asserted at 0.0 — strictly tighter than the web reference's 1e-12); exactly ONE tolerance in the whole suite: 1 ULP on one `deflateSeries` point from a V8-vs-JVM `Math.pow` last-bit disagreement (StrictMath checked too — no JVM function reproduces V8 there; relative 1e-15, necessity verified). Notables: `jsNumberToString` (real ECMAScript Number::toString) keeps error messages character-identical; NaN-propagation semantics preserved (load-bearing for cagrPct). Nothing consumes the package yet — W4 does.

> 🟢 **W3 DONE 2026-08-04** (Builder E — survived a mid-run API-stall wave that killed its three sub-agents; coordinator resumed it for a sequential salvage; coordinator-verified, **1007 unit tests green** (+255)): `CashLedger.kt` ported (188 generated + 11 hand-ported vectors, ZERO tolerances); full BTVAULT1 vault stack in pure-JVM `at.bettertrack.app.vault/` — **BC 1.80 Argon2id (v1.3 pinned) reproduces the published `kekBase64` exactly**; `vectors.fixture.json` replays byte-identical across all 18 conformance cases incl. an envelope rebuilt from nothing but passphrase + deterministic CSPRNG, passphrase-change, rotation, recovery-kit bytes, rollback; `clientMoney.fixture.json` decrypts and the W2+W3 engine reproduces its published money at 0.0; merge port covers all four rules + provenance. Findings: (1) `java.util.zip.Deflater` CANNOT reproduce fflate bytes at any level×strategy — `RawDeflate.kt` is a literal fflate 0.8.3 port (sha-pinned vendor), with a test asserting the zlib non-equivalence; (2) zod `^3.24.1` resolves to 3.25.x whose `datetime()` makes seconds optional — port matches deployed behaviour, recorded in PINNED_AT; (3) the fixture's `updateRequired` label is historical — current platform behaviour is `envelope-invalid` via strict shape (their own suite asserts this); genuine update-required proven with synthesized newer-version envelopes. Watch item: BC adds ~8 MB to UNSHRUNK debug APK — confirm R8 shrink at next release build. W4 consumes all of this.

> 🟡 **S2c-1 CODE-COMPLETE 2026-08-04 ~22:45 — DEVICE VERIFICATION PENDING (phone off USB)** (Builder F through a suspension + collision; coordinator-verified suite **1168 green** (+161), all features wire-verified against live dev, test data cleaned): cash tags (Room v7→v8 `cash_movements.tagIds` + `cash_tags` catalog for offline chips; colour-as-dot for legibility; system tags = delete ABSENT not disabled), budgets (month stepper, gold→red bars, `exceeded` read from the wire so the app can never contradict the `budget.exceeded` push), auto-tag rules (enable/priority/apply-now + debounced live-preview chips in the entry form; numeric priority over drag by design), standing orders full CRUD + pause/resume (bare-object envelope, 200-with-body pause/resume, no Idempotency-Key — corrected against platform source), privacyMode proactive paranoid routing (tri-state: absent = no opinion), vault:sync scope (dev 19 / prod 14), exact `mirror.*` type strings (real names use underscores — the prefix guess would have missed all seven; fallback kept). Summary/trends skipped (small follow-up; strings + repo methods exist). **Pending when the phone returns: install, device-verify all five features, screenshots, `svc power stayon false`.** ⚠️ Phone `R5CN80ABXBK` absent from `adb devices` since the ~8h suspension — needs a physical replug/re-auth (owner).

> 🟡 **S6 WP-A CODE-COMPLETE 2026-08-04 ~23:05 — device pass queued (phone off USB)** (Builder J, coordinator-verified, **1198 unit tests green** (+30)): `ParanoidGate` unifies all 7 guard sites (reactive collect, back arrow on pushed routes, "Open the web app" actually wired to the effective origin — closes audit P0-1+P1-14); debug cleanup hardened (explicit chip selection, exact-`ZZ App Test` arming, type-to-confirm, controller re-reads Room and REFUSES non-throwaway names — closes P0-2); chart gap segmentation `Δt > max(3×median, 90min)` with line+fill both breaking, isolated points as dots, mixed-density fallback (closes P0-3; the 1M "cliff" needs eyes-on-device to confirm class); refresh failures surfaced on Transactions+Watchlist via dismissible RefreshFailedBanner + retry (closes P0-5); `[bt:` retirement finished — `mergeNotePreservingMarker` deleted AND the delta comparison made marker-aware (builder-caught follow-on bug: naive deletion would have rewritten legacy notes on every unrelated edit; closes P1-17). Also: R8-shrunk release APK = 8.7 MB (vs 86.7 MB unshrunk debug) — the W3 BC watch item is closed, vault stack costs nothing shippable. Device-pass queue (with S2c-1): paranoid explainer on all routes + web-open, gaps across 6 ranges, cleanup end-to-end, refresh banner + DE strings, legacy-marker edit no-op.

> 🟢 **TAX ENGINE PORT DONE 2026-08-04 ~23:25** (worktree builder, coordinator-merged `259bbf3`, main suite **1500 green** (+302)): `tax.ts` → `Tax.kt` (1530 lines, pure JVM) — AT/DE/FI/custom engines, realizedSellsEur (moving-avg + FIFO + uncovered + storage-drift waiver), all settle functions, DE carry pots. **269 vectors at exact equality, ZERO tolerances** (structurally sound: the module has no transcendentals); deTaxFixtures replayed verbatim; 33 hand-ported tests. Findings: the year-key iteration hazard doesn't exist in tax.ts (ordered arrays, pinned by swap tests); the real rounding trap was the platform harness's `centsOf = Math.round(v*100)` (JS half-toward-+∞ vs Kotlin away-from-zero — opposite on negative refund deltas; ported as floor(x+0.5)); `taxFloorCents` named apart from cashLedger's (genuinely distinct functions, pinned equal on 12 boundary values). Mutation sanity: quantizer/sort mutations kill 133/302 tests. Nothing consumes Tax.kt yet — Drive-mode tax + tax UI are later packages.

> 🟡 **S6 WP-C CODE-COMPLETE 2026-08-04 ~23:35 — device pass queued** (Builder K, coordinator-verified, **1522 unit tests green** (+22)): switcher shimmer + capped-concurrency prefetch (offline → immediate `—`, failed ids not re-shimmered); scroll-aware FAB (re-shown at top, never stranded); ALL 12 deep-link types through one owning-tab helper (`DeepLinkTabs.kt`, pure-function tested); top-bar chat affordance with gold unread badge (bell's twin) + Alerts segment triggered-count badge + inbox "Manage alerts" overflow entry (design call: alerts stay in Workboard — labelled segment + badge beats a third home); search keeps the field shape but now shows the keyboard on land + Role.Button for TalkBack; empty-state FAB suppression (Workboard/ConglomerateList; Chat+Portfolio verified non-duplicating); cash "Primary/Primär" badge + Deposit-primary hierarchy + documented icon-family rule (user rows = direction only, institution glyph = tax); friend-row chevron dropped + CDs + inbox row baseline alignment; five orphaned routes deleted; inbox archive de-golded + debug flask into debug-only overflow. Device-pass additions: top-bar density on ~360dp, FAB feel, inbox ⋮ alignment, Alerts badge with a real trigger. Note: builder restored `.claude/scheduled_tasks.lock` to HEAD while cleaning IDE noise (harness file, watch for scheduler oddities).

> 🟢 **W4 MERGED 2026-08-04 ~23:50** (`16cd591`, worktree builder + coordinator conflict-resolution, main suite **1635 green** (+113), debug AND release assemble): the full Drive-vault working stack — `DataHome`/`LocalDataHome`/`DriveDataHome` (Drive REST v3 over plain OkHttp, multipart upload, approximated CAS with re-GET-before-PATCH, duplicate detection, quota/absent-remote designed outcomes), Room v8→v9 `vault_entities`/`vault_meta`/`price_cache` (renumbered at merge — S2c-1 had taken v8), `VaultOpExecutor` (local-apply journal semantics, domain refusals → needs-attention), `VaultPortfolioBackend` + `VaultProjection` (clientMoney fixture derives into the REAL Room columns at exact equality — the W2+W3+W4 composition gate), coalescing `VaultSyncCoordinator`/`VaultSyncWorker`, `VaultKeyCustody` (Keystore-cached VK, off-main Argon2id), `NoLivePricesMarketDataSource`, file-name derivation pinned against the executed web reference, all behind a debug-only Drive-mode gate (release byte-equivalent). Builder-caught release bug: R8 would have renamed `VaultSyncWorker` and broken previously-enqueued work — keep-rule added with explainer. DataHome contract suite runs identically against local AND fake-Drive-over-HTTP. **Device-time queue (needs Google OAuth client + phone):** real sign-in/token round trip, Argon2 timing on the Note20, airplane→reconnect E2E, uninstall/reinstall restore, two-device convergence.

> 🟢 **W5 DONE 2026-08-05 ~00:20** (Builder L, coordinator-verified, **1736 unit tests green** (+101), debug + release assemble): storage gate above AuthState (`rootGate` state machine; builder caught + fixed a REAL grandfathering ordering bug — an upgrade-in-place would have flashed the wizard during the async owner-key probe; 15-combination ordering test); first-run wizard (CHOOSE → server login | Google connect → passphrase → recovery kit → blocking acknowledgment → first portfolio → verified round trip: encrypt→write→read→decrypt→compare writeId+vaultVersion — mode persisted ONLY after; passphrase never in a saveable Bundle); VaultUnlockGate (app-lock twin, off-main Argon2, SAF recovery-kit unlock, vault follows the existing PIN idle timer); Settings → "Where your data lives" (nine sync-status sentences, change passphrase, new kit, lock, §1.4 transitions with inline blocked reasons, Drive-mode disconnect/delete-everything); per-mode surface gating (tabsFor; absent-not-greyed); mode-aware logout with real vault tables in LogoutWipeRuleTest. Honesty calls: **BOTH refuses to complete without Google** (offers the two true exits); **per-call backend routing** (`ModeRoutingPortfolioBackend`/`ModeRoutingMarketDataSource`) replaces W1's restart-applied assumption — a wizard-finished DRIVE install no longer touches the server until restart. Designed-pending on the Google client throughout; device queue: real-Google paths, Argon2 timing, SAF on-device, EN+DE wizard walkthrough, upgrade-in-place proof.

> 🟢 **NIGHT-SHIFT TRAIN MERGED 2026-08-05 ~01:50 — S5 + S2c-2 + W6, main suite **2015 green**, both build variants assemble:**
> **S5** (`7bb5c8e`): ServerVaultDataHome over the live vault:sync surface — wire-verified on dev incl. scope gate (403 INSUFFICIENT_SCOPE all five routes), demo-account absent state (404 VAULT_NOT_FOUND, media privacyMode:normal), and a FULL CAS round trip + paranoid kill-rail contrast on a self-provisioned throwaway paranoid account (`s5probe@bettertrack.local`, left on dev for platform cleanup — nothing real in it): create 204 ETag"1", byte-identical read-back, 304 on If-None-Match, 412-carrying-winner-ETag on stale If-Match, 413 over 16MiB cap, and the payoff `GET /vault 200` while `GET /portfolios 403 PARANOID_MODE` on ONE token. Server-side findings coded around: server parses the envelope header (refuses non-advancing versions) but does NOT validate schemaVersion and CANNOT verify AEAD — tamper detection is client-side; adoption distinguishes wrong-passphrase from damaged-bytes and rolls back half-adoption. Media-set sync: one encrypt per pass to N media, per-medium CAS cursors + status, failure isolation, honest floor chip. The app JOINS media sets, never enlarges one (bearer PUT could create a vault for a normal account — deliberately refused).
> **S2c-2** (`1027e89`): market intel (asset section + portfolio screen, per-section retry), comment threads + reactions (chat idiom; U+FE0F heart verified), friend groups + audience rung (radio — wire carries one groupId), mirrorchain invites/chain-sheet/leave, Workboard Ideas segment, cash summary/trends. Integrator catches: shared-ideas DTO gap (ideas silently dropped from /social/my-shared), BtCustomTab dedup, and a REVERSED earlier call — paranoid 403s on intel roll-ups stay VISIBLE errors (silence worse than futile retry; reasoning in-code). Open gap logged: friend's shared idea needs a row + clone flow in Shared-with-me.
> **W6** (`fc66c4e`): manual price entry for any Drive-mode asset (validated, currency-mismatch refused, edit/delete with cache-key invalidation — two real traps fixed), honest no-live-price states across 9 surfaces **fixing a pre-existing €0-totals lie in VaultProjection** ("Excludes N holdings with no price"), session-gated "BetterTrack for prices only" toggle (structurally can't touch the portfolio backend — asserted by test), provider scaffold compiled OFF pending owner. Manual prices are DEVICE-LOCAL (portability needs a platform-blessed vault kind — board ask pending).
> Upstream watch: platform #1094 (holdings storage-drift envelope) + #1095 (spendableAsOf tie vector) → re-pin vectors when their fixes land, harness drives the change in. DEVICE-PASS DEBT (phone still off USB): S2c-1, WP-A, WP-C, W5 wizard EN+DE walkthrough + upgrade-in-place proof, S2c-2 surfaces, W6 states, S5 chip; plus Google-OAuth-gated: real Drive sign-in E2E.

> 🟢 **VECTOR RE-PIN + #1094/#1095 ABSORBED 2026-08-05** (coordinator-verified, **2039 tests green** (+24)): pin `cb530f7e` → `fc970e8a`; the harness caught **5/622 divergences unaided before any Kotlin was touched** — including the audit's exact OversellError-in-deriveHoldings repro (the class that 500s web's portfolio overview). Both fixes ported literally: holdings storage-drift waiver envelope (`HOLDINGS_QTY_STORAGE_QUANTUM`, driftRows counter, reset-on-close) and `spendableAsOf` shared tie-ordering (`orderCashMovements<T>`, credits-before-debits retired; net −30 lines and a downcast eliminated). Vectors 606 → 622, still exactly ONE tolerance in the whole suite (the W2 pow-ULP case). Builder extra worth keeping as doctrine: `storageDriftVectors.json` asserts the fixture's DECLARED expectations, not just the recorded TS replay — platform-engine-vs-declaration drift can no longer propagate silently into our port.

> 🟢 **S5 LIVE E2E DONE 2026-08-05** (coordinator-verified with a security correction, **2049 tests green**, 7 env-gated skips): the paranoid account's REAL browser-produced vault ran the full chain — wire (ETag "2", 304 round trip, media/history shapes, metadata headers correctly only on history fetches; integration note #1 was ALREADY implemented right and is now pinned by a mutation-verified regression guard), decrypt (BouncyCastle Argon2id on the browser's own salt; AAD = exact wire header bytes), hydrate, project — **all 16 hand-derived board numbers reproduced bit-for-bit** (cash 21,521.27; AAPL 6@180.65 avg, realized 255.90 USD; MSFT 6@390.25; KESt row linked to its sell; totals/day-change exact). Fixture = inert ciphertext ONLY: the builder had vendored the derived KEK (harness-flagged); coordinator ruling — no derived credential material in repo files even for board-public dev dummies — the decrypt chain is env-gated (`BT_PARANOID_VAULT_PASSPHRASE`), proven 54/54 locally with the passphrase read from the board at runtime. Known non-scope: adapter doesn't yet send If-None-Match on /vault (server 304 verified working; DataHome-semantics change deferred). Envelope is schemaVersion 2 with clientSecurity present — the reply-#41.2 read-and-preserve gate holds today.

> 🟢 **R1-A MERGED 2026-08-05 ~11:50** (`0b6a1c9`, worktree builder, coordinator-verified, main suite **2072 green** (+23)): the new IA skeleton — 5 tabs in mandate order (Home NEW · Portfolio · Workbench · Markets · People; DE Start/Portfolio/Werkbank/Märkte/Leute), start destination = Home (account-level deep links follow; Holding stays Portfolio), one spec-driven `BtTopBar` (Home: wordmark+Search+overflow with inbox-count/discreet/Settings; People: title+Messages; Workbench/Markets: title-only — deviation: no empty ⋮), chat-unread + triggered-alerts as tab dots (`BtTabBadgeDot`, `AlertsRepository.triggered`), dead-code sweep (−200 lines: SocialTabScreen/RefreshableTabScreen/PlaceholderScreen/NotificationBell/BtTopBarChats/PortfolioSelectorChip), Home stub with final signature + `homeNetWorth` honesty rules + `moneyHero` 44sp. Grep proofs zero; strings verified append-only (0 removed lines). MERGE NOTES honored: `bt_top_messages` REMOVED from the R1-tail deletion list (People's Messages action reuses it); Portfolio renders `ownsItsHeader=false` shell title until R1-B's collapsing header flips the flag. Device debt: badge-dot density, 44sp hero on 360dp, Home peek gesture, ⋮ dot alignment.

> 🟢 **R1 COMPLETE + WP-B MERGED 2026-08-05 ~12:35 — the S6 audit is 23/23 DONE and the app wears its new face** (`1286ab2` R1-B + `cb75103` WP-B merge; main suite **2120 green**, lint **0 errors**, both APKs build): Home built out (44sp moneyHero hero with honesty shapes, movers by |Δ%|, "Needs you" rows, Drive-mode unpriced-holdings row, quiet tail with inbox/discreet/settings second paths); Portfolio re-laid out behind `BtCollapsingHeader` (112→64dp, tap-to-switch, tonal scroll elevation) with the mandate's hierarchy (hero → 150dp chart → slim allocation bar, donut behind See-all → holdings → cash/transactions as secondary rows); Fable's overflow second-path rule verified app-wide. WP-B semantics everywhere: 224-code EN/DE catalog with compiler-enforced `BtMessage`, Room v10 park-codes (legacy English renders verbatim, self-heals on retry), one app-level snackbar (0 Toasts), terminology glossary, `rememberBtLocale`. Integrator caught: 3 R1 locale sites (lint), R1's silent discreet-revert rerouted through the snackbar with Retry. DEVICE DEBT now spans: everything since S2c-1 + the Portfolio 3-way nested-scroll feel + R1 screenshot rounds for Fable. R1 tail cleanup queued (retired string keys minus `bt_top_messages`, `bt_overview_holdings_value`).

> 🟢 **R2 DONE 2026-08-05 ~13:15** (coordinator-verified, **2127 tests green** (+7), lint 0 errors, both flavours assemble): Workbench (Needs-you block leads: triggered alerts + unfinished ideas), Markets (search → watchlist → intel doorway riding the scroll), People (own header + Messages action; Needs-you with inline accept/decline; Add-friend → scroll-aware FAB), Settings + 4 subscreens (tonal `BtGroup`s replace border walls; danger zone keeps the only border); 10 pushed-screen bars converted to the 3-element collapsing idiom (chat exempt — IME/composer justification in code); global radii moved to `BtShapes` (12/16dp — R1 screens inherit §4 for free); 32 retired string keys deleted at proven-zero references, EN 1708 = DE 1708. Fable-worthy note: "ideas needing a decision" reads `thesis == null` honestly — a REAL decision workflow needs a platform field (board ask). Device debt grows: subtitle-bar fade on 360dp, pinned-block budget, gold wash on OLED, radii eyeball everywhere + all prior debt + Fable screenshot rounds.

> 🟢 **R3 DONE 2026-08-05 ~14:05 — THE R-ARC IS CODE-COMPLETE** (coordinator-verified, **2137 tests green** (+10), lint 0, both flavours): NavHost motion spec (two idioms, one 300ms rhythm, 90/210 split, 1/12-vs-1/36 parallax; reduced-motion honored; shared-element DECLINED for two documented structural blockers — LargeTopAppBar double-composes its title, and detail screens resolve async so the match wouldn't exist at animation start; needs a route-shape change, recorded); animated header expand replacing two single-frame snaps; 13 empty/error/loading surfaces fixed incl. 4 that actively lied (CashScreen eternal shimmer, FriendOverview computed-and-dropped error, two Empty/Failed conflations); `BtOfflineState`/`BtInlineError` added to the DS + gallery de-boxed; EN/DE copy pass (54 EN + 111 DE rewrites — headline: error-catalog terminology drifted from screen terminology, "basket"/"circle" vs "Conglomerates"/"Friend groups"; DE unified on Mitteilung/Wert/das Tag; hard-coded-English `categoryLabel()` duplicate deleted for the localized labeller); 4-meaning haptic vocabulary on every primary confirmation; IME insets fixed on 7 forms + nav-bar insets on 8 sheets (`consumeWindowInsets` — Scaffold doesn't consume what it passes); 32 more dead keys (grep -a needed — a non-text byte was silently hiding a file from grep); 1699 = 1699. HONEST RESIDUE (next batch): state ledger not fully zero (TransactionsScreen failed-first-fetch conflation + 7 no-error-state + 5 no-loading-state screens + ~12 spinner sites + bare-Text empties/errors), 9 hard-coded plurals need code changes, Fable call needed on Portfolio-vs-Workbench empty-CTA asymmetry. Device debt: motion feel numbers, haptics density, 3-button-nav sheets, API-28 scrim + everything prior.

> 🟢 **LEFTOVERS BATCH DONE 2026-08-05 ~16:00 — every code-side residue closed** (coordinator-verified, **2204 tests green** (+67), lint 0, both APKs): state ledger to zero (TransactionsScreen got its missing VM error channel + five-way split; same conflation fixed in Cash/CustomAssets/HoldingDetail/Watchlist/ChatList; all 7 no-error + 5 no-loading screens closed; three bordered banners tonal; `CashAnalyticsError` deleted); all 9 hard-coded plurals → real `<plurals>` (23→33 both languages, StringParityTest gained a sound %d-beside-countable-noun guard); shared-idea row + clone (201 verified; revoked→404 mapped, deliberate no-retry); vault restore-picker wired (verify-then-commit, successor versioning, WrongKeyEra vs damaged-bytes split, rollback, type-to-confirm); If-None-Match on the server-vault GET (validator+body as one unit) — plus a real find: `GET /vault` 404 carries a decoy weak ETag on the ERROR body which could have poisoned a later 304; pinned by test with the live value (board-noted). New DS primitives: `BtInlineEmpty` (17 files), `BtFormError` (11), `resolveListSurface` (8 files, 11 tests). `kotlin.daemon.jvmargs=-Xmx3g` added to gradle.properties (multi-agent OOM root cause). Deliberate keeps: AppLockSetup spinner (gates path, not content), MirrorInvitesCard's own message type, BlockingAcknowledgment's border (the one outline = "stop"). German judgement calls queued for owner: `bt_workbench_needs_more` neuter, `bt_chain_members_count` cap-1 phrasing. **ALL CODE-SIDE WORK IS NOW DONE** — remaining queue is entirely phone-gated (device pass + Fable screenshot rounds), owner-gated (Google OAuth client, provider decision, light-theme call, German calls), or platform-gated (vault price-point kind, ideas decision field, s5probe cleanup, #1094-class pings).

> 🟢 **OWNER CRASH REPORT RESOLVED + OVERVIEW IA CHANGE 2026-08-05 ~21:45** (coordinator-verified, **2238 tests green** (+34), lint 0, both builds; device-verified on the returned phone): **the "constant crashes" were NOT crashes** — logcat crash buffer empty, dropbox zero crash records; his installed build was versionCode=1 (pre-R1, DB v7, self-update never landed due to signature mismatch → an un-completable "Version 0.88" updater dialog on every launch) with the origin override pointing at localhost:3000 with no adb reverse → every request dead + nagging dialog = reads as "constantly crashing". **The sweep still found real landmines, all fixed:** (1) migration 4→5 unguarded ALTER crash-loops any fresh install from 07-17..08-04 (duplicate column); (2) the W4-branch v8 collision leaves such devices without cash-tag columns → validateMigration throw; both now guarded/healing, proven by `BtDatabaseMigrationTest` (real migration objects through JVM SQLite from every version + both poisoned twins + HIS actual pulled v7 DB: 7→10 with all rows surviving incl. 2 queued sync_ops; backup kept). (3) REAL cold-start crash path: SessionInitializer's coroutineScope/launch exception escape killed the process on refresh throw — fixed + CoroutineExceptionHandlers on both process scopes + BtCrashGuard (logs, then dies honestly); TokenManager doomed-refresh ANR backoff; 24 repos boundary-routed (maintenance-page HTML 200 → Err, not SerializationException); **network failure can never log the user out** (pinned). **IA per owner:** 4 tabs (Portfolio start), Home → "Overview/Übersicht" pinned gold atop the portfolio switcher with own header; selection persists as a separate DevicePrefs flag (not a sentinel id — six screens read the real id); account-level deep links push over the current tab; Drive bar = Portfolio+Markets. Device: fixed build installed, LAN origins verified live, zero crashes everywhere incl. WiFi-off cold start (honest states, clean recovery). ⚠️ **demo login REJECTED twice on the dev stack** (board password; no login.fail audit row → password stale or the login POST dies pre-auth) — Overview/4-tab on-device screenshots pending login; app left at the login screen 3 taps from working.

> 🟢 **OWNER NAV RESTORATION DONE 2026-08-06** (coordinator-verified, **2263 tests green**, lint 0, device-verified, 42 screenshots): Settings gear on all 4 tab bars (1 tap from anywhere, enforced by new structural `TopBarNavigationTest` — no top bar may carry an overflow, gear renders last); ALL 5 top-bar ⋮ menus dissolved with every item's in-content path grep+device-verified (cash's tags/rules/standing-orders had NO second path — a "Manage" doorways group was BUILT at the cash foot; inbox bulk-deletes → red danger rows in matching views; gallery back on wordmark long-press); unread dot moved to the Portfolio tab badge (visible from every tab). Earlier same-day batch (`3f59a66`): wordmark restored non-fading in the header, web-anatomy selector pill (gold icon chip 44→34dp), continuous charts (owner override, always-connect pinned by 9 tests), perf pass (main-thread JSON decodes off Main, nav-graph rebuild on badge-tick fixed, ICU formatter caching; chart Path caching measured 2.6x WORSE and reverted). Honest perf note: the follow-up harness (24 runs) puts same-binary noise at ~4.6pp — the earlier 1.17pp scroll-cost figure is NOT established; two draw simplifications kept on correctness grounds (dead clip layers; RenderNode only while press animates). Fable absorbed all owner overrides as spec of record; screenshot rounds retired as a gate.

> 🟢 **PORTFOLIO + GROUP MANAGEMENT PARITY 2026-08-06** (coordinator-verified, **2283 tests green** (+20), lint 0, four assembles, 28 screenshots, live-wire probed): `PortfolioSettingsScreen` (rename, audience via AudiencePickerSheet, tax cascade, chain section, danger zone with type-to-confirm); COMPLETE tax capability — Settings→Taxes account default, per-portfolio override with effective/override/source cascade (PUT/DELETE round-tripped live), tax-year list + detail (conditional DE pots block) + CSV export via share sheet (FileProvider added, scoped) — all verified against the seed's real AT data; group admin: all nine ops modeled behind a session-cached capability probe, rendering "Manage on the web" today and lighting up on a platform config change (no app release needed) — all nine confirmed 403 per the platform's `MIRRORCHAIN_BEARER_ROUTE_ALLOWLIST`; account sweep (display currency, default visibility, 16-icon profile picker) + REAL BUG fixed: `explicitNulls=false` silently dropped `profileIcon: null` so "No icon" could never clear (explicit-null JsonObject body now). OWNER ADDENDUM shipped: Portfolio header PINNED at 64dp (no expand at scroll-top; `rememberBtPinnedHeaderBehavior` keeps tonal lift; TopBarNavigationTest extended to both bar variants). Deliberate non-write: `PATCH /settings/taxes` left unexercised to keep the seed's AT default (same serialization path covered by the portfolio PUT). 

> 🟢 **B2 MODERNIZATION COMPLETE 2026-08-07** (B2-A `a8d5357` + B2-B `37909c4` + B2-C merge `4eaaba2` + integration; main suite **2373 tests green**, lint 0, both assembles): dual light/dark theme public (Settings→Darstellung: System/Hell/Dunkel + Reines-Schwarz; live-preview picker; system bars follow the app), 5-step one-hue neutral ramp, `ShortNavigationBar` 64dp with the travelling indicator synced to the swipe gesture + Origin glyphs, tone-vs-hairline sweep (borders die in dark, survive in light), 16 pixel-diffed web avatar drawables + JS-hash-parity test + 28 Origin glyphs + `BtPortfolioChip` kind hues (header chip wears the portfolio's own colour — Overview stays gold by rule), rangeAccent rails, light categorical palette FIXED (3 real defects: grey teal, deutan-inseparable lime/red-brown pair) and validated both grounds, 26-key copy trim EN+DE (one flagged lossy call: the idea-share hint detail — owner veto open), hero chart light = goldEmphasis (coordinator call on the 1.7:1 open item). Integration: BtIcons both-added conflict → full set; handover patch hand-applied onto B2-B's header; kind glyph+tint wired at the Overview call site. Escalations for next board post: MirrorInviteDto lacks profileIcon; portfolio `kind` field still client-local both sides; the CORRECTED light palette values supersede #69's.

> 🟢 **WHITE-FLIP + BRAND-YELLOW FIX 2026-08-07** (coordinator-verified, **2392 tests green**, lint 0, both assembles, device-verified both modes): light mode reworked to a CLEAN-WHITE page (owner order) with a 6-step tinted-container ramp (cards #F2F4F7, bar/sheets #ECEFF4 — the card step deliberately SMALLER than a naive value-swap so the app doesn't read greyer); the "rusty gold" diagnosis: #8F5F00's ZEROED BLUE channel was the rust, not the darkness — text-gold now #866419 (lightest point on the brand ray clearing 4.5) and a NEW `goldGraphic` token (#A77D1F light / #F6B82E dark) drives charts/edges at the correct 3:1 graphical floor; palette re-validated on all three new grounds (all pass, zero changes); skeleton pairing fixed; dark byte-identical (pixel-sampled). **SUPERSEDES B2 spec §1.4**: all six light neutrals + navBar + goldInk changed; "raised = lighter in both modes" is FALSE in light now; goldGraphic is post-spec. Follow-ups noted: GalleryScreen demo frames invisible on white (dev-only), light sheet scrim soft (needs mode-conditional mapping).

> 🟢 **BRIGHT-YELLOW + POLISH/PARITY + UPDATE-FIX 2026-08-08** (`0041fa3`, `277b7a4`, `cc5e1ad`; suite 2425→2532, lint 0): owner-final gold system (goldGraphic retired, goldInk #D49E28 brand-ray, geometry compensation); combined batch: optimistic tab-tap latch (47.7ms median removed), BtPickerSheet family (incl. searchable lazy variant opening on ticked value), equal Cash/Transactions chips, About snooze row, parity ruling executed (default-visibility/true-black-UI/per-type-mute deleted + trueBlack stranding healed; global mute, memberSince, quiet-hours None-UTC + explicit use-my-timezone, tax disclaimer + /portfolio/tax link, recovery-codes download byte-identical; /control/* defer link rows), audience public-rung acknowledgment tightened to web rule + friend-gate dropped, third PRODUCT origin (legal rows + all self-links follow effective origins, proven live), chart modes [€%][€][%] combined-first with one-mark €% label + sign color gated to pure-% (readout bleed fixed in combined AND €); update popup: 6h gate → 15-min debounce, per-foreground-session reset, cached-release offers, scrim/back/Dismiss no longer book the 24h snooze (only the explicit button does). Parity corrections vs audit: Etc/* unhide premise false (web never shows them; force-add detected/stored zones adopted instead), audienceTransitionRequiresConfirmation never existed (real gate AudiencePicker.tsx:258), dev stack serves NO legal pages (SPA catch-all false positive). Board: #73 Vaults v2 review (approve-with-needs, 2 blockers), #74 r2 verification (6 residuals, 2 data-loss) — ALL SIX ACCEPTED by platform; r3 + P5 hardening pass (vectors) in flight; P4 sign-off condition met when they land.

> 🟢 **ARCHITECTURE: LIVE 4-PAGE PAGER + 45 SHEET SUBPAGES 2026-08-08** (suite 2497 green, lint 0, three variants assemble, 24 screenshots, device-measured): tabs freed from the NavHost into a HorizontalPager with all four pages permanently composed (BtTabPager.kt); all 45 subpages became full-screen bottom sheets over the shell (BtSheet.kt — grabber, pull-down-from-scroll-top dismiss, back/close; stacking one level; deep links open sheet over owning tab; ParanoidGate renders INSIDE the sheet, structurally pinned); BtTabSwipe/BtTabPeek (725 lines) + both selection latches DELETED — gesture and bar read the same PagerState in-frame. Numbers: aggressive 3-swipe bursts 30/60→60/60 pages (20/20 perfect bursts; pager fling tuned: snap stiffness up, threshold 0.1), steady-state jank 21.25%→14.91%, swipe-run p99 89→30ms, memory −65.8MB PSS. Coordinator rulings: shipped with +231ms debug cold-start regression (bisected: NOT the pager; ~190ms unlocated in shell restructure — profiling follow-up queued); pull-to-refresh KEPT on the 5 data sheets (grabber/back/system-back still dismiss; content-pull dismisses on the other 40); sheet chrome grabber-only (titles stay in each screen's own top bar — no double naming). Guards: BtTabPagerTest + BtSheetTest new, TopBarNavigationTest +4 structural ("tab pages live in the pager, not the nav graph"; "every subpage route is a sheet"), BtTabSwipeTest retired with its subject.

> 🟢 **SETTINGS CONTROL-CENTER + GESTURE COMPLETION 2026-08-08** (suite **2555 green**, lint 0, both flavors, device-verified incl. my own logout→login-screen→login cycle): Settings regrouped to the web control center taxonomy (KONTO merges Account+Profile — identity block with live memberSince; Appearance split out deliberately for Drive-only installs; NEW-PORTFOLIO-DEFAULTS group; SettingsTaxonomyTest pins it); Connections + Authorized apps NATIVE (owner override of the defer rule) behind the capability probe — `GET /settings/oauth-grants` 403s (board ask #75; platform held for owner ruling: recommend GET-bearer/DELETE-session split — OPEN owner decision), google link-status 404 self-heals; login screen: gear top-corner → pre-login sheet (Server/Design/Sprache), server footer now pure text. GESTURE ARC: the one root cause behind "offline doesn't work" + "can't scroll during skeleton" = sheet states outside any scroll container (~60 branches/25 files) → BtStateFill/BtScrollFill everywhere + SheetStateScrollGuardTest (source-scanning; found 3 more files; exemptions now empty after I fixed LanguageScreen myself); bounded refresh (320ms floor/25s ceiling/finally — stuck-spinner=page-close bug dead); OWNER'S NEW STACKED-SHEET MODEL implemented+device-tuned: horizontal=depth (sub-sub pages enter from the RIGHT, right-swipe pops one; chrome static), vertical=two-stage notch dismiss (0.50–0.72 slight-resistance zone: release=back-one/close; past it=close ALL — resistance retuned 0.5→0.7 after device pass proved close-all humanly unreachable; reachability pinned by test; grabber pill widens/golds as the live cue). Known cosmetic: blank reveal behind depth-2 slide-out (NavHost doesn't compose covered destination). Slow-path 25s ceiling JVM-only (device repro would need repointing the owner's server config — declined). Update popup device-verified firing. OPEN owner ruling: oauth-grants split (list-native-now vs full-native-revoke-later).

> 🟢 **SHEET GESTURES V2 + CHART CONTROLS 2026-08-08/09** (suite **2597 green**, lint 0, both flavors, device-proven with recordings): owner rejected v1 stacked-sheet feel → REBUILT: subpage sheets left ComposeNavigator for `BtSheetNavigator` (FloatingWindow destinations) + `BtSheetStack` — ONE persistent container composing the top TWO entries live (BtSheet.kt/BtNavMotion.kt deleted, re-homed as BtSheetNavigator/BtSheetStack/BtSheetGestures) → depth transitions are real two-plane slides: frame-measured **0px vertical drift, 0 blank frames** across 290 frames; notch corrected (12% dead-zone; release ≤0.72 resistance = back ONE with the connected slide; past 0.78 = close ENTIRE stack — device-proven Security→Settings-gone); depth-1 linear (no phantom detent — coordinator-approved); refresh min-visible floor DELETED → 600ms gesture disarm + 500ms "pull again to close" chip EN/DE (25s ceiling + finally kept). NOTE: this task hit a deterministic API content-filter kill 4× — always at first BtSheet.kt emission; workaround that succeeded: plan-to-disk + new-files-first + <40-line incremental edits (recorded for future runs). CHART CONTROLS (owner): range pickers app-wide adopt the segmented design via shared `BtRangeSegmented` (three chip-row implementations deleted; width-policy by TextMeasurer: divide vs scroll — divides on 411dp phones, scrolls on 360dp class; both device-proven incl. fontScale 1.3); side-by-side mode+range REJECTED by measurement (452dp needed vs 328dp) → pickers bracket the chart; Cash+Transactions moved BELOW the graph (owner order); AssetRange/BacktestRange labels de-hardcoded → shared EN/DE window resources (1J/3J/5J live; StringParityTest guards Y-vs-J per window). CLOSE-OUT FIXES: gallery sheet scroll-freeze = M3 TwoRowsTopAppBar self-drag modifier on 4 unpinned demo headers inside the LazyColumn (pinned; CollapsingHeaderScrollGuardTest — fails pre-fix); conglomerate list stale after delete/create = repo had no observable list → repo-owned StateFlow with delete-prune/create-upsert (8 tests). Screenshot/recording evidence: bt_scratch/{gesture-v2,chart-controls,gallery-fix}-2026-08-08/.

> 🟢 **GESTURE V3: NOTCH UP + HAPTIC + REFRESH LATCH 2026-08-09** (suite **2610 green**, lint 0, both flavors, device-verified at 120Hz): notch moved 0.72→**0.36–0.44**, resistance stiffened to 0.40 (close-all now a 1255px stroke, was 1790); ONE-SHOT LongPress detent haptic on crossing into close-all (first+only haptic in the app — explicit owner order; fires once per crossing, verified single vibration + zero on sub-notch pulls); refresh disarm 600→**1100ms** with routing LATCHED at pointer-down (mid-gesture expiry completes as started — device-proven with a pull spanning the boundary). JANK DIAGNOSIS (honest): first hypothesis (coroutine dispatch per pointer event) DISPROVEN by A/B; real cause = GPU/RenderThread-bound overdraw — pager+scrim+sheet surface+two planes rasterized per frame = 10.68ms vs 8.33ms budget at 120Hz; ZERO dropped frames (120fps presents) — symptom is ~7ms pipeline latency. Rungs (b)/(c) correctly declined (crossfade wouldn't cut overdraw). Shipped: scrim offscreen-buffer removal, grabber derivedStateOf, synchronous drag path (KEPT — 1 frame less lag, depth p99 85→65ms; jank% metric rise = honest per-frame traversal, not regression). NEXT LEVER (coordinator-approved, next batch): occlusion culling — skip pager rasterization while the sheet is at full height (top 8dp strip + corner slivers show scrim-tone backdrop instead of the dimmed page; live page returns the first frame of vertical travel). New standing device rule: every pass ends screen-off (OLED care).

> 🟢 **SHEET OCCLUSION CULLING 2026-08-09** (suite **2617 green**, lint 0, all four variants assemble, device-measured at 120Hz): while a sheet rests at full height, the shell Scaffold + scrim CLIP their draw to the uncovered 176px strip → HWUI quick-rejects pager/bar/header render nodes. PIXEL-IDENTICAL (0 differing pixels vs pre-change — the approved flat-backdrop compromise was NOT needed; the slivers show the real dimmed page); raster-only skip (state/semantics/predictive-back untouched); draw-phase derivedStateOf so 120Hz travel writes re-record nothing. Numbers: RT+GPU medians two-plane drag 10.98→8.43ms (−23%), depth back-swipe 10.23→7.52 (−26%), notch pull 7.07→5.73; pipeline latency −1.8–2.6ms; gap-to-pager 32% closed; residual 4.84ms MEASURED to be the sheet's own two live planes + Surface (shell-draws-nothing ceiling = 7.87ms; chart-offscreen no better; corner-clip only 0.37ms) — occlusion is at its floor, further wins would need plane-level changes. Handoff: 286 frames extracted, 222 past the cull line, per-frame correlation vs the real page median r=1.0000, ZERO wrong-content frames. Tabs-with-no-sheet unaffected (BtSheetStack composes nothing at depth 0). Files: BtOcclusion.kt (+7 tests incl. structural "pager subtree inside the clipped draw"), BtSheetStack.kt, AppShell.kt.

> 🟢 **GESTURE TUNE: QUARTER-NOTCH + HEAVY DETENT + FLICKER KILL 2026-08-09** (suite **2623 green**, lint 0, both flavors, device-measured): notch 0.36→**0.25** (band 0.25–0.34, dead-zone 0.12 so back-one = 0.12–0.25); resistance eased 0.40→0.50 → close-all stroke **1248px→~958px (41.4% of screen**, was 54.8%) — bracketed with real strokes 900/940/970→back-one, 990/1000→close-all; new bracket guard pins detent between 1.5–2.5× band width. HAPTIC: root-caused imperceptibility — (a) phone's touch-vibration channel is at 40% (VIB_FEEDBACK_MAGNITUDE=2/5; owner can raise "Touch interaction" intensity — left untouched), (b) LongPress maps to Samsung's lightest pattern → detent now VibrationEffect.EFFECT_HEAVY_CLICK direct (103ms, USAGE_TOUCH, VIBRATE permission added — normal level); REAL BUG found+fixed: direct Vibrator calls escape Samsung's haptic_feedback_enabled switch (enforced only in performHapticFeedback) → BtDetent now checks the setting itself, verified both ways. FLICKER (sub-sub→sub): root cause = plane reset in a LaunchedEffect dispatched one composition too late → pop showed the surviving page off-screen (tx=1080) for ~34ms/4 frames; fixed by pure `sheetPendingSlide(depth,lastDepth)` telling the draw phase the pending direction (~35 lines, no architecture); push-path mirror artifact fixed by same; 240-frame after-scan: zero near-uniform frames.

> 🟢 **NOTIFICATIONS BACK: BELL + GRANULAR WEB ROWS 2026-08-09** (suite **2626 green**, lint 0, both flavors, device-verified): inbox regression archaeology — never deleted, ran out of callers across 2b7b0e9 (bell file deleted → overflow) → ec722a7 (overflow dissolved; replacement paths named on Overview) → 0e6fd58 (Overview demoted into switcher → 4 steps deep, one affordance unread-gated); "inbox is reachable" was written nowhere → NOW: bell as a CONSTANT of the shared tab bar (between per-tab action and gear; not a BtTabHeaderAction so it doesn't cross-fade under swipes), unread COUNT badge (hoisted out of IconButton — M3 clips badges to the 48dp circle), Portfolio tab dot RETIRED (was aiming a global signal at one tab; Chat/Alerts dots stay — their tabs contain their destinations); inbox joined the shared refresh family (1100ms disarm); guard added so reachability is now a tested fact. Notification settings rebuilt per the new granularity rule: native section (permission/mute/quiet hours) + four NAMED web rows exhaustively matching the web's four PanelGroups — Zustellhäufigkeit (digest), Zuordnung (routing), Telegram und Discord (server-`channels`-gated like web; absent on dev stack correctly), Browser-Push; NO blanket row; all four open /control/notifications because that's the only URL the web has (matchControlPanel = one segment, no anchors — verified, not invented). REAL BUG fixed: six resolveDeepLink branches returned null with "lands on inbox" comments and MainActivity ?.let-dropped them → target-less pushes (budget/chain/id-less alert) cold-opened Portfolio showing nothing; NotifDeepLink.Inbox added at the push call site only. Dev-stack env notes: web app Vite serves no index (URL proven via address bar); alerts jobs empty (no evaluator) — test alerts cleaned. Overview's two old inbox paths deliberately KEPT.

> 🟢 **AUDIT-FIX BATCH 2026-08-09** (suite **2637 green** +11 guards, lint 0, both flavors, device-verified): storage add-second-medium dead-end rows HIDDEN via `offerableTransitions` filter (model table untouched; restoration = one filter removal, KDoc names Vaults v2 P4 as owner; honest consequence: SERVER-no-vault storage screen = just the mode card); my-shares badge wired to real `sharedCount` (device: badge 1 vs "Du teilst 1 von 9") + BONUS clipping bug fixed (unbounded German label pushed badge off the pill → maxLines+ellipsis+weight); manage-groups row now on the group rung whenever offered — NOTE: web's AudiencePicker has NO management affordance at all (only zero-groups prose); ours is a documented ADDITIVE control (gates still clause-for-clause `canSubmit`, pinned) — coordinator-approved, owner may overrule; dead params deleted (onOpenChangelog — doorway lives in About since a1e7882; BtSheetHost discreet pair; SharedWatchlistViewRoute.ownerName end-to-end) and `stillFriend` RENDERED (ex-friend no longer told "@name hasn't shared anything" — relationship fact, not person fact; Remove-friend row absent when not friends); Settings→Taxes native doorway "Steuerberichte · <portfolio>" → TaxYearsRoute (default portfolio NAMED on the row; absent if unresolvable) + web row retitled "Druckfertiger Steuerbericht" (print route = web's only surplus: auto-print + per-position dividends; per-portfolio both sides, no aggregation lost). OPEN owner ruling: idea creation restore vs view-only (audit finding 1).

> 🟢 **CHARTS FEEL-PASS 2026-08-10** (suite **2671 green** +34, lint 0, both flavors, device-verified): scrub HAPTICS — BtHaptics.scrubTick on BtDetent plumbing (inherits the Samsung settings-gate fix), BtDetent parametrised by weight (HEAVY_CLICK notch / EFFECT_TICK scrub), pure cadence machine (per rendered point, floors 55ms/5px) — log-proof 39 ticks in a 2.5s drag, median gap 62ms, bezel feel; FUTURE-DIMMING right of scrub x (container-colour scrim 0.68/0.74, O(1), all palettes); ASSET CHART rebuilt on the hero architecture (was time-proportional x + DrawScope-onScrub + 138dp-in-card ratio bug — "too thick" was ratio not stroke) → 240dp full-bleed, house strokes, page re-hierarchised (price → chart → ranges → 2×2 stat grid + range hi/lo → intel → trade); EARNINGS: ground truth = /intel/earnings EXISTS (next + recent, EPS est/actual) → grouped est-vs-reported EPS bars, verdict-coloured, 6 periods; EPS quantity-formatter bug fixed (4,71331 → 2dp); fundamentals DO NOT EXIST server-side (Yahoo module whitelist doesn't fetch them) → ask #76; HOLDING page gained "Kauf- und Verkaufszeitpunkte" chart — trades at PRICE PAID (not curve close), green/red ringed triangles, shingled clusters, out-of-window counted never clamped, scrub-onto-marker surfaces detail; 1D: server hard-codes 15-min grid, query schema .strict() (any client param would 400) → changed nothing, ask #76; footnote deleted; FAB shrinks 56→40dp (never vanishes, real size anim = honest touch target); ALLOCATION redesign (filters → one BtSegmented governing the visible bar, 58dp weight-first legend column killing the FAB-overlap class, slim bar collapsed ↔ 168dp donut expanded, disclosure row names the count).

> 🟢 **VAULTS V2 P4 GATE 1 + FUNDAMENTALS/FINER-1D 2026-08-10** (two commits `7389f82`+`90c635a`; suite **2727 green** +56, lint 0, both flavors; coordinator-verified split + secret-scanned + pushed): **P4 gate 1 GREEN** — all six v2 conformance families (header derive/wrap+r3 §21 MAC, multi-slot passphrases, 26-kind per-portfolio split, migration transcript, recovery kit v2, canonical QR/code KDF) replay BYTE-EXACT on Android via 11 new Kotlin units under vault/v2; vectors re-pinned to platform origin/main @8884c5cb (NOT the platform checkout's stale main @b7cebe48 — vendored via `git show origin/main:` per their README, trap recorded in PINNED_AT); v1 confirmed BYTE-IDENTICAL (git R100 rename — relocation, no drift → nothing to escalate); deterministic-IV safety precondition holds (no split-serialization drift); no hand-rolled crypto (HKDF=BC RFC5869 pinned to vector A.1, AES-GCM+deflate reuse v1, BIP-39 wordlist digest-pinned). Two r3 production fixes carried: parse-time mergeLog>20 cap REMOVED (r3 mobile finding A1.2 — an oversized diag log must not brick an otherwise-readable v2 doc; write-time trim instead) + aesGcm private→internal for single-impl reuse. Family 3 needed NO engine restructure (pure function → full replay, nothing deferred to "P4 phase 2"). **FUNDAMENTALS** (#76): new asset-page card (annual/quarterly toggle, revenue-vs-net-income bars, scrubbable readout, 6-ratio grid) fetched OUTSIDE the capability probe (contract omits it → own `available` flag is the gate); large-money rule 7 in BtNumberFormat (DE Tsd./Mio./Mrd./Bio., EN K/M/B/T, symbol-last, tier-promotion), NOT discreet-masked (public filing data); debtToEquity rendered %-as-provided no rescale (Yahoo convention; flag if a provider flips to fractions); trailing EPS from ratios.trailingEps. **FINER 1D** (#76): `interval` now a real DTO field (ignoreUnknownKeys was swallowing the required field); app sends nothing → gets 5-min default (~156 pts). THREE on-device fixes tests couldn't catch: padded scale-ceiling label removed, invisible selection band → mode-aware gold wash, annual/quarterly toggle made conditional on periods existing. Device pass clean: demo/stay-off/WiFi-on/screen-dozing, test custom-asset created-then-deleted (account clean). Board: #76-consumed + family-3 commonKinds heads-up posted. OPEN: platform-chief ruling on 4 flagged kinds (expenseTransaction/expenseBudgetFire/cashBudgetFire/cashRuleTag) placed in `common` by inference — if it flips, family 3 changes and both clients move together. KMP/iOS (branch kmp-ios, separate worktree): P0+P1 MET — iOS app boots on the simulator running real shared-domain code (resolvePortfolioSetting), Android suite unchanged 2637/0, 11 commits pushed to origin/kmp-ios; next is P2 shared-core migration after a main-merge (absorbs the required `interval` field + the v2 vectors).

> 🟢 **COMMONKINDS RULING — CLOSED 2026-08-11** (platform PR #1183, no code change): the family-3 `commonKinds` question escalated on the board is settled by the platform chief — all four kinds (`expenseTransaction`/`expenseBudgetFire`/`cashBudgetFire`/`cashRuleTag`) STAY in `common` (r2 §8 was illustrative-not-exhaustive; none carries a `portfolioId`, so `common` is their only valid home). Our P4 split already pinned them there via the vectors (the oracle) → nothing moves app-side, no re-pin — family 3 is LOCKED both sides. #76 convention notes ack'd (a future non-Yahoo debtToEquity fraction normalizes at the provider adapter not the client; trailing EPS from ratios.trailingEps confirmed). Only remaining OPEN platform-side item: #75 oauth-grants (still the owner's call).

> 🟢 **HOME-SCREEN WIDGETS (Jetpack Glance) 2026-08-15** (suite **2766 green** +40, lint 0, both flavors assemble, device-verified on R5CN80ABXBK): two resizable Glance 1.1.1 widgets — **Net Worth** ("Vermögen") and **Watchlist** — new `at.bettertrack.app.widget` package (NOT `ui/widget/`: Glance palettes need raw Color literals which BtThemeDisciplineTest bans in ui/**; placed outside + guarded by BtWidgetPaletteMirrorTest which parses BtColors.kt and asserts all 7 mirrored tokens still match both themes). Data read HEADLESSLY from Room via `homeNetWorth(homeActivePortfolios(...), priceCoverage(...))` — literally the call HomeScreen makes, zero new money math; mode-agnostic (VaultPortfolioBackend.writeProjection fills the same tables the server backend does) so net-worth works in BOTH server + Drive modes. Watchlist quotes cached as JSON in the existing `meta` table (key widget_quote_cache, NO schema migration, KEY_PORTFOLIO_KINDS precedent), refreshed by BtWidgetRefreshWorker (≤12 rows, 4 concurrent, single-asset MarketRepository.quote() — no batch endpoint exists, ask filed). Refresh triggers: onEnabled→periodic (45min, CONNECTED, KEEP), onUpdate→fetch+repaint, post-sync repaint-only from SyncWorker(Drained)/VaultSyncWorker/SessionInitializer. Discreet mode masks the amount (persisted setting, NOT live BtDiscreetMode.masking — an in-app reveal must not unmask the launcher), % stays live. States: signed-out CTA / syncing / offline "as of …" / empty. Deep-links: net-worth→Overview (NEW NotifDeepLink.Overview — Overview stopped being a tab in the 2026-08-05 IA change, so AppShell pairs the tab switch with setOverviewSelected(true), same shape as Alerts; DeepLinkTabsTest 13→14), watchlist row→Asset. Day/night themed to brand, system corner radius on API31+ (square pre-31 by platform convention). DEVICE-VERIFIED: both widgets render with real data (38.579,23 € matches Overview exactly; live MSFT/ETH quotes; RKLB em-dash), tap→Overview confirmed. DEGRADATIONS (honest): watchlist is server-mode-only in practice (watchlist entities aren't in VaultContracts.ENTITY_KINDS, WatchlistRepository is server-only → Drive user sees empty state); watchlist prices ~45min cache not live; square corners API28-30. OPEN polish (owner-optional): vertical-balance content when the cell is taller than 2×2/4×2; relative "yesterday"/date on the as-of once older than a few hours. Platform ask filed: batch quotes endpoint.

> 🟡 **QUEUED — OWNER UI BATCH "PORTFOLIO+CASH REV" 2026-08-16** (dispatch AFTER the widget mockup-conformance loop lands; owner: "you can wait till the widgets guy is done but then do this"): **Portfolio page** — remove the 6M range option; move the €/% toggle + the range selector side-by-side BELOW the chart; HIDE net-0 holdings everywhere (sold-out positions must vanish from Holdings AND from the "excludes N holdings with no price" banner — owner holds 0 PEBIX and still sees both); consolidate the chart delta: drop the top-right "+x % 1M" badge and make the line right above the chart read "+/-xx € (xx %) · <timespan>" with € and % green/red by sign, timespan in the current neutral color and in WORDS ("heute", "letzte Woche", "letzter Monat", … — not 1D/1W; whether the chips themselves also switch to words = builder judgment on-device); REMOVE the Allocation section from the page → new bottom button next to/like "Portfolio Settings" ("More insights" / "Mehr Einblicke") opening a subpage that houses allocation + future insight modules; **holdings rows**: left = name over holding QUANTITY with no ticker suffix ("4", not "4 AMD") using a smart significant-digits truncation (owner examples that MUST hold: 0.0424512→0.042, 11.66666667→11.6), remove the %-allocation element, right = total P&L € colored green/red over the P&L % (as today), tighten the intra-row spacing (currently reads disconnected), keep allocation-size default ordering and add a top sort toggle (Allocation | Profit) styled like the chart's timespan selector. **Cash section** — decrowd the overview: deposit = GREEN button, withdraw = RED and RENAMED (owner: "withdraw" sounds like fetching physical cash — choose clearer EN+DE wording, propose e.g. "Add money"/"Spend" · "Einzahlen"/"Ausgeben"); add a real cash-SOURCE switcher (other sources currently unreachable); below it, link-rows to subpages (Budgets etc.; Transactions move to their own subpage) mirroring how Portfolio links to Cash; below the links a cash-flow chart + a BRIEF all-budgets overview (per-budget used-up bars only — not the current crowded block) with manage options at the very bottom; the budget month selector must NOT allow navigating into future months. **Transaction editing** — eliminate the "This transaction is linked to your cash balance. Delete it and add it again to change the amount" dead-end: editing linked transactions must just work (prefer a server-side edit if the API/web supports it — check the web repo first; else orchestrate an atomic delete+recreate through the existing exactly-once queue machinery; only if genuinely impossible → PLATFORM_ASKS + tell owner). **Popups** — replace EVERY remaining anchored 3-dot context menu (the "cheap square next to the dots", e.g. tag editing) with bottom sheets, app-wide. Owner will review further pages later.

> 🟢 **WIDGET REDESIGN v3 — CODEX-STUDY SET, DEVICE-CONFORMANT 2026-08-16** (suite **2865/0 both flavors**, lint 0 errors, device-verified on the owner's LIVE account; LOCAL COMMIT ONLY — push awaits the owner's verdict): 13 widgets → **9** rebuilt to the owner-approved Codex round-1 mockup study (headerless content-first cards, delta chips, subject lines, pace footers, per-size OWN hierarchies): Vermögen pulse, Portfolio performance (4x4 hero: EINBEZAHLT/HOCH/RENDITE band + localized movement ledger), Asset, Budget (ring/bar/number), Watchlist+Movers (shared row family, GEWINNER|VERLIERER split default at 4x2), Monatsfluss (equation/6-month/tag modes), Allocation (reinstated), Quick Actions (Neue Transaktion / Cash-Buchung / Märkte via new NotifDeepLink targets). In-app builder Settings→Appearance→Widgets with live sample-data mocks + requestPinAppWidget stash/claim (pre-configured pins). Size classes measured from THIS launcher (120dp row / 250dp 2-row / 366dp 4-col — the old 92dp assumption was the "squished" root cause) + charts painted at exact occupied px (SizeMode.Exact, cap 1400, smoothed path, gradient fade). Owner rulings honored: NO in-widget range chips (span = per-instance config, default 1M); round-2 variant study demoted. Rich EN+DE sample data in picker previews + builder mocks. 7 live-data defects found+fixed on device: RemoteViews 10-child cap silently truncated the 4x4 hero (nested sub-Columns); budget ring clip/collapse at 2x2; EUR-native quotes showed "—" (server omits eurPrice when already EUR — identity fallback at the data source, also fixes the in-app watchlist, +4 tests); QuickActions label truncation; flow 2x2 dead band; movers preset → SPLIT; raw wire kinds in the ledger → Cash-screen localized labels. Deferred polish (backlog): flow 4x4 tag-donut hero, budget 4x2 Tagesverlauf mode, row-family 4x2 sparklines. Env notes: local dev stack (node :3000/:6771/:6772) left running on the Mac for cleanup; app now points at PRODUCTION, owner logged in himself; one Vermögen 2x1 showcase left on the launcher. Design-source artifacts (Codex mockup study) in session scratchpad codex-widget-design/.

> 🟡 **QUEUED — WIDGET ROUND 4 (owner 2026-08-16 late)** (dispatch AFTER the Portfolio+Cash UI batch lands — one driver per tree; Codex round-3 designs in flight → widgets3.html): (1) **Quick Links widget** replaces the text-tile Quick Actions: pure icon-button grid in app-icon look (rounded-square tiles, gold glyphs, NO text labels), per-instance CONFIGURABLE action set from a catalog — portfolio overview, a NAMED portfolio, assets/markets, chat, social, watchlist, cash, add transaction, add cash entry, etc.; sizes 2x1/4x1/2x2/4x2 as icon rows/grids. (2) **NEW Cash-Wallet widget** per configured cash source: source name + balance, quick actions "Bezahlt" (out, red) / "Erhalten" (in, emerald), PLUS the future photo-booking action designed in NOW ("Foto-Buchung": photograph a receipt → AI reads and books it — ships later; in the widget as a visually present affordance with an honest "demnächst" behavior until the AI feature exists); 4x2 may add a small recent-movements list. Both per Codex round-3 design (round-1 system, widgets2 stays demoted). (3) **Relax chart width restriction** (owner: "lass es zu das graphen kleiner gemacht werden können als volle screenbreite") — the conformance round pinned Portfolio performance to min 4-wide; allow sub-full-width chart shapes again (2–3 columns) with properly COMPOSED narrow renditions (follow the study's 2x2 asset-chart language; recompose, never squish — that rule stays). Update provider XMLs, size classes, previews, builder cards, guard tests accordingly.

> 🟡 **QUEUED — WIDGET ROUND 4 ADDENDUM (owner 2026-08-16 late)**: (4) **Asset widget: arbitrary-asset SEARCH** — the config (config activity + in-app builder picker) must allow SEARCHING any asset via the existing online search (`/search`, market:read, debounced — reuse the app's search UX), not only the held ∪ watched dropdown; the chosen asset is stored per-instance and its quote fetched/cached via the existing single-asset quote path (BtWidgetQuoteStore / MarketRepository.quote()) like watchlist rows. Search is online-only — offline the picker degrades honestly to the held ∪ watched list + a "search requires connection" note.

> 🟡 **QUEUED/IN-FLIGHT — PORTFOLIO LIST ROWS (owner 2026-08-16 late, addendum to the Portfolio+Cash batch)**: the "Deine Portfolios" rows on the Portfolio overview get the SAME anatomy as the new holdings rows — LEFT: portfolio name (prominent) with the current VALUE beneath it (less prominent); RIGHT: total gain/loss in € on top, colored emerald/red by sign, with the total % beneath it. Tight vertical pairing (connected units, not floating apart), consistent with the holdings-row treatment.

> 🔴 **DEFECTS FOUND 2026-08-16 (linked-tx investigation, tracked separately from the UI batch)**: (a) `UpdateTransactionRequest` (dto/PortfolioDtos.kt) is missing **`baseSeq`** — the mirror stale-edit guard the web sends (TransactionDialog.tsx:925); without it, edits on MIRRORCHAIN portfolios skip `409 MIRROR_CONFLICT` protection. (b) the same DTO sends `allowUncovered`/`uncoveredEntryPrice`, which the server's `.strict()` PATCH schema (contracts/portfolio.ts:613-631) does NOT accept — masked today only by `explicitNulls=false`, so it 400s on a real uncovered-sell edit. (c) **`VaultPortfolioBackend.updateTransaction` (data/storage/VaultPortfolioBackend.kt:337-356) desyncs the Drive-mode ledger**: it rewrites quantity/price/fee/executedAt but leaves the linked `CASH_MOVEMENT` untouched — precisely the desync the server refuses (its own `deleteTransaction` at :358-373 tombstones the cash leg correctly). The web's E2EE vault store has the SAME defect (apps/web/src/user/vault/vaultPortfolioStore.ts:735-788) → worth raising with the platform as a shared client-side bug. Also noted: server-side, the `TRANSACTION_CASH_LINKED` guard sits OUTSIDE the `!opts?.force` check (portfolioService.ts:2046-2089) — even mirror replica-apply cannot bypass it; and the PATCH contract's lack of cash fields (CREATE has `payFromCash`/`addProceedsToCash`/`cashSourceId`, UPDATE has none) is the ROOT CAUSE of the "delete it and add it again" dead-end → candidate PLATFORM ASK if the client-side delete+recreate orchestration proves insufficient.

> 🟡 **QUEUED — TRANSACTIONS FILTER + STATS (owner idea 2026-08-16)**: the transactions/ledger view gets a real filter bar — not only by cash SOURCE but also by **TAGS** and by **TIMESPAN** (All ↔ a specific period; reuse the app's range/segmented-control language). Below/above the filtered list, show **aggregate stats for the current selection**: total, total in (+), total out (−), and further useful roll-ups (e.g. count, net, biggest single movement, per-tag split). Rules: prefer SERVER-provided aggregates where an endpoint exists (`/cash/summary`, `/cash/trends` family) — only fall back to a plain sum of the displayed movements, and then label the scope honestly ("für die Auswahl"); never mix cash sums with portfolio valuation math (§7.1). Filters must combine (source × tags × timespan), be visibly resettable, and persist per-session; empty result = designed empty state, not a blank card. Applies to the new Cash ledger subpage first; check whether the portfolio transactions list deserves the same treatment.

> 🟡 **QUEUED — LEDGER FILTERS v2 + EXPORT (owner 2026-08-16 late)**: the shipped filter row (source chips / tag chips / 4 preset windows) is too basic. Codex round-4 design in flight → `ledger.html` + `DESIGN_NOTES_LEDGER.md` (scratchpad/codex-widget-design/). Build to that design: (a) proper **custom date range** — presets PLUS start/end date pickers when the timespan isn't "Alle", resolved range shown as a removable token ("1. Juni – 16. Aug"); multi-select sources/tags with counts; always-visible active-filter summary; per-facet + global reset; filter bar that collapses/scrolls with the list; bottom sheets only (anchored menus are banned app-wide + guarded by AnchoredMenuDisciplineTest). (b) improved "Für diese Auswahl" stats (in/out/net/count + largest movement, per-tag split, average — bookkeeping only, never portfolio performance). (c) **EXPORT of the filtered selection** — format per Codex's reasoned choice (CSV/PDF), exported file states the applied filter, progress + success/failure states, Android share-sheet/Files convention, edge cases (empty selection, very large export, failure). EN+DE for every new string; de-AT money; discreet-mode rules apply to the on-screen view (decide + document what an EXPORT does under discreet mode — masking a file the user explicitly requested is likely wrong; state the ruling).

> 🟡 **IN FLIGHT — CASH VOCABULARY ALIGNMENT (owner 2026-08-17)**: owner approved the Codex cash-wallet widget design and ordered the APP to match it so both speak one language — **supersedes the B1 wording** ("Einzahlen"/"Ausgeben"): DE **"Bezahlt"** (out) / **"Erhalten"** (in), EN **"Paid"/"Received"**, with **Bezahlt FIRST, Erhalten SECOND** in every place the pair appears (buttons, sheets, CTAs, form headers, confirmations, empty states) and semantic colors kept (Bezahlt red, Erhalten emerald). Data layer untouched (CashKind/wire values unchanged) — user-facing copy + ordering only; movement row labels aligned only where they name the same user action (transfers/buy/sell_proceeds left alone); widget strings aligned too so launcher and app match. Also in the same round: portfolio rows re-spec'd (name over colored P&L € left; neutral current value over colored P&L % right) and holdings second line → "2.400,32 € (34,51 BAYN.DE)" — ticker back inside brackets, quantity TRUNCATED (not rounded) to 2 decimals, 3 when |q|<1, extended until 2 significant non-zero digits; "current money invested" implemented as the position's CURRENT MARKET VALUE (coordinator ruling — owner can flip it to cost basis).

> 🟡 **QUEUED — IN-APP FEEDBACK REPORTING (owner 2026-08-17, both clients)**: users must be able to report ANYTHING — feature suggestions, change requests, bugs, general feedback — up to **5000 characters**, which Christian reads in the **admin panel** (his priority order: features/changes he can implement > bugs > rest). App side: a "Feedback senden / Send feedback" entry in Settings (+ a discreet path from About/Developer), category chooser (Feature/Verbesserung · Bug · Sonstiges — the category drives his admin sorting), free-text field with a live 5000-char counter, optional auto-attached context (platform, appVersion, osVersion, device, locale, current screen), honest submitted/failed states, offline queueing. Build the UI + repository seam NOW behind a capability flag (established stub→adapter pattern), go live when the endpoint ships. **Platform ask #78 posted to the board 2026-08-17** (`POST /feedback`, session+bearer, category/message/subject/context, 201 {id,createdAt}, rate-limited, scope TBD by platform; admin panel needs category filter+sort; optional `GET /feedback/mine`; attachments explicitly out of scope for v1). Web app gets the same surface so both clients feed one queue.

> 🟢 **BOARD 2026-08-17 — #77 ANSWERED (batch quotes ALREADY LIVE) + domain-drift tick**: platform says the batch endpoint we asked for shipped 2026-08-13 (their #1140, web watchlist perf) and is **live on prod, bearer-reachable — no deploy or flag needed**; our ask went unticked by their omission. Contract: `GET /api/v1/assets/quotes?ids=<uuid>,<uuid>,…` — comma-separated single string (NOT repeated params), **cap 100 ids** (over → `400 VALIDATION_ERROR`, no truncation), duplicates de-duped server-side, query object is `.strict()` so ANY extra param (cache-buster, `_t=`) is a 400. Response differs from our sketch — nested, not flat: `{quotes:[{assetId, quote:{price, currency, prevClose?, dayChangePct?, marketState?("open"|"closed"|"pre"|"post"), asOf(upstream observation)}, stale, asOf(cache write)}], failed:[uuid…]}`. **ACTION (queued): swap `BtWidgetRefreshWorker`'s 12-call / 4-concurrent fan-out for one batch call** (parse the nested shape, honor `failed[]` + `stale`, keep the ≤12 cap and the cache write path); also consider it for the in-app watchlist. Second board item: platform→mobile **domain drift tick #1272, new baseline `3e61872d`** — re-pin conformance vectors to that baseline on the next vault/domain touch (their post states zero wrong-money risk today; the exposure is KDoc wording for a FUTURE re-derivation, not live numbers).

> 🔴 **RESTORATION QUEUE — "was ist noch alles verschwunden?" audit 2026-08-17** (read-only audit, evidence-backed; full inventory in the session transcript): **P0 bug — per-type notification mute is STILL actively deleting stored choices**: `data/notifications/NotificationSettingsStore.kt:315-318` calls `.remove("$k.muted")` on EVERY write (same reasoning as the true-black healer: "a stale true would sit in SharedPreferences forever with no UI able to clear it") — the per-type cards were removed, only account-wide mute remains → restore the per-type mute UI + stop the deletion (medium). **P1 — Saved Ideas is the ONE genuine reachability regression**: `ui/workboard/WorkboardScreen.kt:138` enum dropped `Ideas`; impl `ui/ideas/IdeasSection.kt:228` is orphaned; an OWNED idea is viewable exactly once (only navigator = `AppShell.kt:1649` fired from `FriendOverviewScreen.kt:317` right after cloning a friend's idea) → back out and it's permanently unreachable. Restore = enum + segment + `when` branch + thread `onOpenIdea` через `ui/screens/TabScreens.kt:239` → `AppShell.kt:963-968` (3 files, ~30 min; the code's "reversible in one line" is optimistic). Also `WorkboardScreen.kt:221` hard-codes `unfinished = emptyList()` (trivial, pairs with it). **P1 — secret Developer menu** (`SettingsScreen.kt:762`, gallery+sync debug) compiles out of every release build though the owner asked for it by name → decide debug-only vs. release-with-unlock. **P2 — new-portfolio default visibility** (`SettingsScreen.kt:568-573`): cut as "app-invented", but the server stores `defaultPortfolioVisibility` and the web sets it → a web-set value is invisible+uneditable on the phone; strings still shipped (small). **P2 — digest cadence + notification routing matrix** (`NotificationSettingsScreen.kt:76-100`) handed to the web ("match the web exactly or link to the web") — owner had no in-app name for the digest (medium). **Play-flavor casualties (policy, not accident)**: `SELF_UPDATE_ENABLED=false` also hides the ENTIRE in-app changelog (`ChangelogScreen` + `ui/settings/Changelog.kt` intact, registered at `AppShell.kt:1715`, no navigator on Play) and `SERVER_SETTING_ENABLED=false` hides Settings→Server. **Audit method note (load-bearing for future audits): three source files contain raw NUL bytes and are SILENTLY SKIPPED by grep** (`ui/social/ItemThreadSection.kt`, `ui/components/BtRangeSegmented.kt`, one test) — grep-based audits in this repo under-report; re-run NUL-safe.

> 🟡 **OWNER RULINGS 2026-08-17 on the disappearance audit**: (1) **NOTIFICATIONS — FULL WEB PARITY, owner-ordered, supersedes the "match the web exactly or link to the web" cut**: "ich will die selben einstellungsmöglichkeiten wie in der web Version haben und nicht weniger" — build the COMPLETE settings surface IN the app: the per-type × per-channel routing matrix, per-type mute (P0: `data/notifications/NotificationSettingsStore.kt:315-318` must STOP calling `.remove("$k.muted")` on every write — it silently deletes stored choices), digest cadence ("Zustellhäufigkeit"), quiet hours, and the remaining web PanelGroups (Telegram/Discord where the server exposes channels, browser-push is web-only by nature). Mirror the web's exact option set + labels (web repo READ-ONLY at `/Users/cwiesi/Desktop/Coding/Github/BetterTrack`, its four PanelGroups on /control/notifications are the spec). Verify the settings API is bearer-writable with `notifications:read|write` BEFORE building the writes; anything the bearer can't reach → platform ask + keep a labeled web link for that facet only. (2) **Developer menu stays debug-only** — owner: "entwicklermenu nicht im release ist ok und soll so sein". Ruling recorded; do not restore it in release builds. (3) **Play-flavor gates stay as they are** — owner: "playstore dings da auch okay" (self-update off ⇒ in-app changelog unreachable on Play; Settings→Server hidden on Play). Both accepted, no work. (4) New-portfolio default visibility: explanation sent to the owner, decision pending.

> 🔴 **SETTINGS-PARITY AUDIT 2026-08-17 (server truth vs app coverage; enforces the owner's "API is the shared control layer" doctrine)** — read-only audit, bearer answers taken from `apps/api/src/http/middleware/bearerAuth.ts`, NOT openapi metadata (note: that old warning is now STALE — `pathAcceptsBearer()` generates the spec, so both agree). Two corrections to earlier assumptions: (1) the app's bearer holds **19 scopes**, not 12 (`OAuthConfig.kt:76-113` adds alerts:*, cash:*, mirrorchain:*, vault:sync) — several "blocked" items are reachable TODAY; (2) **`defaultPortfolioVisibility` is NOT user-settable on the web either** (admin-only, `AccountDefaultsPage.tsx:125`; the user Control Center refuses it, pinned by `AccountPanel.test.tsx:186-198`) → the app's cut was CORRECT parity; adding it would put the app ahead of the web (owner call, trivial).
> **CLOSE IN APP (bearer-reachable, impact order):** 1. **Portfolio Icon (`kind`)** — the live doctrine break: phone writes a Room `meta` row, web writes `PATCH /portfolios/{id}.kind` (`private|family|business|savings|property`), the two silently disagree; add `kind` to `PortfolioDto`+`UpdatePortfolioRequest`, keep the meta map as a one-time migration read (`PortfolioRepository.kt:113-132` carries the stale "API has no field" comment) — small. 2. **Notification routing matrix (25 types × 6 channels)** — LINK-ONLY today, so stopping a PHONE push requires a browser; app already fetches+honours it (`NotificationSettingsScreen.kt:377-382`) — large. 3. **Per-type digest cadence** — writer `NotificationRepository.kt:326` exists and is ORPHANED; only per-type UI missing — medium. 4. **Telegram link/confirm/unlink + Discord webhook save/test/remove** — both bearer-reachable — medium/small. 5. **Public profile toggle + bio** (`PUT /social/profile`, DTO already carries `isPublic`+`bio` at `AccountSecurityDtos.kt:199-204`; the "icon is the only writable field" comment is false) — small. 6. **Account PIN set/change/disable + `pinLockIdleMinutes` select** (`PUT|DELETE /auth/pin`, `PUT /auth/pin/idle-timeout`) — the 2026-07-09 "web-managed PIN" directive PREDATES the bearer allowlist; note the device AFK picker is a DIFFERENT setting — small each. 7. **Data export** (`/account/export*`, re-auth+poll+download all bearer-OK) — medium. 8. **Alerts visible to followers** (`PUT /alerts/sharing`) — small. 9. **`defaultPayFromCash`** persist-back — trivial. 10. **Mobile widget-layout sync** — platform reserved `/settings/widget-layout/mobile` for exactly this — medium. 11. **Follow graph + per-follow prefs** (`/social/follows`, autoFollowItems/notifyOnAlertCreate/notifyOnAlertFire) — the app has NO follow feature at all — large.
> **PLATFORM ASKS (paste-ready, post as #79):** bearer+`account:security` on `GET|PATCH|DELETE /auth/passkeys[/{id}]` (list/rename/delete is not a WebAuthn ceremony); bearer on `GET|DELETE /settings/oauth-grants` (#75 still open — app screen ships probe-gated); bearer path for `POST /account/paranoid/enable|disable`; bearer `vault:sync` on `PATCH /vault/media` + retired-purge pair; bearer on `GET /settings/taxes/years` + `unlock|relock`; bearer `account:security` on `POST|DELETE /auth/remembered-device`; a bearer-completable Google **link** flow (`/auth/google/start` is a cookie redirect whose callback bounces to web); bearer on `POST /auth/first-run/complete`.
> **STALE COMMENT TO FIX:** `BtApi.kt:1142-1160` claims MIRRORCHAIN admin refuses bearer — board #67 widened it (rename/invite/revoke/role/transfer/kick/dissolve are in the bearer allowlist and live) → the capability probe should already light that screen.

> 🟡 **QUEUED — ALTERNATIVE CHART FORMS + "Darstellung" CONFIG (owner 2026-08-17)**: owner wants options beyond donut/bar for part-to-whole and comparison data — "verschiedene blasen mit verschiedenen größen oder vierecke oder andere möglichkeiten", in-app AND in widgets. Codex round-5 study in flight → `charts.html` + `DESIGN_NOTES_CHARTS.md` (scratchpad/codex-widget-design/): bubble/circle-packing, treemap, waffle/dot-grid, single 100 % stacked bar, radial bar/sunburst, ranked bars, heat strip, beeswarm for signed movers, donut as control — each shown on four real data sets (allocation by class; allocation by POSITION with a long tail where the donut fails; spending by tag; winners/losers) across a SIZE LADDER (full-width card ~380x240dp, compact card ~380x120dp, widget 2x2 and 4x2), including where each form BREAKS. Widget constraint honored in the brief: widget charts are bitmaps — no hover/tooltip/reflow, whole-widget tap only. Study also covers legend attachment, long-tail "Andere" bucketing, selected-segment treatment, empty states. **Build follow-up**: make the chart form a user-selectable **"Darstellung"** option per surface (insights allocation first, then spending/movers, then the widget builder's viz choice — the widget config already has a style knob pattern from Budget ring/bar/number). Default per data set = Codex's recommendation; only ship forms that survive their size ladder.

> 🟢 **BOARD 2026-08-17 (2nd pair) — #78 FEEDBACK CONTRACT LOCKED + domain prose tick**: platform ACCEPTED our feedback ask essentially verbatim and filed three issues — **#1315** (API+schema+scope), **#1316** (admin inbox), **#1317** (web submission surface). **Contract, match exactly:** `POST /api/v1/feedback`, session cookie AND bearer, body `{category:"feature"|"bug"|"other" (required), message: 1..5000 (required), subject: ≤120 (optional), context:{platform,appVersion,osVersion,device,locale,screen} (optional, stored as JSON, inner keys NOT schema-locked — send what you have)}` → `201 {id, createdAt}`; over-length/bad category → standard validation envelope; rate limit ≈5/user/hour. **Wire enum stays exactly `feature|bug|other`** — translate display copy only (Feature/Verbesserung · Bug · Sonstiges), web uses the same mapping so both clients land in one bucket. **Scope = new `feedback:write`, and they SEED it to the BetterTrackMobile client BEFORE we request it** — ⚠️ **do NOT add it to the authorize request until they tick it live here**, otherwise we hit the un-seeded-scope hard reject that broke alerts; `/feedback` gets its `MODULE_POLICIES` entry in the same PR (avoids the `/chat`+`/alerts` `API_KEY_FORBIDDEN` class bug). Then: one re-login. Answers to our questions: anonymous web feedback **NO** (auth-only both clients, spam + owner wants to know who to follow up with); **`GET /feedback/mine` SKIPPED for v1 — don't build that UI**; attachments confirmed out of scope. Their admin inbox defaults to **feature → bug → other, newest first**, status new/triaged/done, submitting user shown, context rendered as labelled fields. **Their merge tick = our go-live signal** (build the UI now behind the capability flag). Second item: domain tick `050a588f` (#1303) repairs storage-quantum PROSE only — comment-only, 4 insertions/5 deletions in `packages/domain/src/holdings.ts`, zero non-comment lines, no vector moved; new baseline `239d161c`. Nothing to port; the P3 doc-parity divergence is smaller than this morning.

> 🟢 **BOARD 2026-08-17 (3rd) — #79 ANSWERED: ALL EIGHT ACCEPTED, zero new scopes, NO re-login**: platform verified every item against `bearerAuth.ts` and route source. **Headline: all eight ride scopes we ALREADY request** (`account:security`, `vault:sync`) — no seeding migration, no re-authorize, no re-login, no release needed for the scope side. Standing rule unchanged for the future: never add a scope to the authorize request before they tick it seeded (that hard-rejected the whole authorize during the alerts rollout). Five issues filed: **#1324** (passkeys list/rename/delete + tax-year list/unlock/relock + first-run complete), **#1325** (oauth-grants), **#1326** (paranoid enable/disable + vault media), **#1327** (remembered-device redesign), **#1328** (Google link design). **#75 CLOSED as resolved-by-#1325**: `/settings/oauth-grants` opens to FIRST-PARTY clients only (gate on the client, not the scope — allowlisting on `account:security` alone would let any third-party app holding it enumerate/revoke the user's OTHER connected apps, a cross-third-party leak + mutual-eviction primitive). `btc_IbT1mzw_7kBiPHPkGfaE0Q` IS first-party → **our Authorized-Apps screen lights up with no release from us** (it already ships behind the capability probe). **PARANOID (#1326) grows a step-up on BOTH clients**: enable/disable will require a credential in the request body (password | fresh TOTP | recovery code) verified inside the account lock — enable is a one-way destructive purge and a stolen phone token must not be an account-erasure primitive; correction in our favour: the WEB has no password/PIN gate today either, so this is new work on both paths, not us being held to a stricter bar. UI consequence: budget a credential prompt shaped like account-deletion/passkey-revoke (password/code/recoveryCode, at least one); **`POST /auth/reauth` is NOT reusable as a pre-step** (204, mints nothing). **⚠️ BLOCKING QUESTION ON US (#1326 media half)**: `PATCH /vault/media` on `vault:sync` is fine, but ADDING `server` back needs a `server-candidate` verification bound to a staged candidate, and `PUT /vault/media/server-candidate` stays session-only → as filed the phone could retire `server` but never move back. **Choose: (a) ask them to widen the candidate PUT+GET as an amendment, or (b) the app refuses the server-add edge.** Recommendation: (a) — one-way doors are exactly the trap the storage arc kept hitting. **Item 6 premise was WRONG (ours)**: `POST /auth/remembered-device` returns the device id only as the signed httpOnly `bt_rdid` cookie, which on a bearer call lands on OkHttp, not the Custom Tab that is its sole consumer (`/auth/pin/quick-auth`) → we'd mint orphaned Redis state; and `DELETE` is already PUBLIC, reading the id only from that cookie, so a bearer call would forget nothing and answer `{ok:true}` — a silently-lying success. #1327 instead builds bearer list/revoke-one/revoke-all over the existing per-user reverse index, raw device id never leaving the server; minting stays browser-only (our Custom Tab can already hit it during the OAuth leg — app-side work, not a missing endpoint). **Item 7 (#1328)**: confirmed the only real design item — `start` also hard-demotes a bearer caller to anonymous sign-in (`linkUserId = req.authUser && !req.apiKey ? … : null`), so allowlisting could never have expressed "link to MY account"; candidates are a bearer-minted single-use link ticket with a registered mobile deep-link target, or device-code; the ticket must replace the signed state cookie as CSRF defence at equal strength. Design ticks here BEFORE we wire 6 or 7.

> 🟡 **QUEUED — FEEDBACK v2 (owner scope change 2026-08-17, board ask #80 posted)**: feedback is no longer fire-and-forget — users post ideas/feedback **and see the state**; Christian triages in the admin panel, sets a status and can **converse** with the submitter. Consequences: (a) **`GET /feedback/mine` returns to scope** (platform had skipped it for v1 and told us not to build that UI) — the app needs a "Meine Beiträge" list with status, last-change time and unread marker; (b) **a per-submission thread** (`GET|POST /feedback/{id}/messages`, or the platform may model it on the existing `/chat/*` infra — we adapt, but we don't want two message models in one app); (c) proposed status vocabulary, wire values lowercase-stable and copy translated per client: `received` Eingegangen · `reviewing` In Prüfung · `planned` Geplant · `in_progress` In Arbeit · `shipped` Umgesetzt (+ version tag closes the loop) · `parked` Vorgemerkt (his "future idea") · `declined` **Nicht geplant** (deliberately softer than "rejected", carries an optional REASON the submitter sees — a bare rejection with no sentence is what makes feedback systems feel like a void) · `duplicate` Doppelt (pointer to the original); (d) a **notification type** for status-change + new-reply so the loop closes without polling — it slots into the 26×6 routing matrix the app now renders and mobile picks it up once the catalog carries it. **v1 composer stays shipping behind `FeedbackFlags.enabled=false`** per the locked contract; light it up on the platform tick, then add status list + thread as a second pass. Reminder recorded on the board: our production probe of `POST /api/v1/feedback` returned **403 `API_KEY_FORBIDDEN`** (session-only verdict, NOT `INSUFFICIENT_SCOPE`) → `/feedback` still lacks its `MODULE_POLICIES` row; the go-live tick must cover module policy AND scope seeding.

> 🟡 **QUEUED — INSIGHTS STUDIO + REPORT EXPORT (owner 2026-08-18)**: owner wants the Insights page to grow up — *"give me more insights and ways to export a report and check all the insights i want in the report... i can setup each insight how i want and then export the insights i want into a nice looking pdf or share a graphic quick as picture"*. Codex round-6 design in flight → `insights.html` + `DESIGN_NOTES_INSIGHTS.md`. Three parts: **(A) insight catalog** — a ranked set of honestly-producible cards (from holdings, portfolio history 1M/6M/1Y/MAX, allocation by class/position, cash movements + tags + budgets, monthly flow, transactions incl. fees + realized P&L, dividends/earnings intel, tax summaries; NO market-wide universe exists, nothing may depend on one), each with default viz form, config knobs, compact+full renditions and empty state, plus a verdict on which five are default-on. **(B) per-insight configurator** — bottom-sheet config, reorder/hide cards, live preview with real data, coherent with the existing per-data-family `Darstellung` preference so the two don't fight. **(C) report builder + export** — multi-select which insights go in, one period/scope for the whole report, page-count estimate, and an A4 PDF designed as PAGES (cover, summary of headline numbers, one section per insight with chart + factual caption, header/footer pagination, provenance page: data as of / which portfolios / what's excluded); plus the quick path **share a single insight as an image** (1080×1080 poster + 1080×1920 story, branded, chart + headline number + period) behind an "Als Bild teilen / Als PDF exportieren" sheet, with progress/success/failure/empty states. **Privacy ruling to design + document**: a self-exported PDF carries real numbers (existing app ruling for CSV/PDF ledger exports), but a picture shared to social is a different act → an "Beträge ausblenden" toggle keeping percentages/shapes/colors while dropping absolute euros, with a stated default. Build after round 5; reuse the shipped `ui/charts/viz/` engine and the PDF plumbing from the cash export (framework `PdfDocument`, no new dependency).

> 🔴 **QUEUED — ROUND 6: WIDGET UX + CASH FIXES (owner 2026-08-18, dispatch right after round 5)**:
> 1. **Widgets must repaint IMMEDIATELY on config change** — owner: changing the portfolio / the shown budget / the asset does nothing for a long time. Config-apply was built for the pin path; the RECONFIGURE path (long-press → config, and builder edits of existing instances) evidently still waits on a data fetch. Fix: on commit, write state → repaint from CACHE synchronously (`update(glanceId)`) → THEN kick the network refresh and repaint again. Never let a config change wait on I/O; a stale-but-correct card that sharpens a second later beats a frozen one. Verify on device for asset, budget, portfolio and heatmap-scope changes.
> 2. **Some widget charts are CUT OFF** — reproduce per family × size and fix (suspect the exact-size bitmap path or a padding/measure mismatch at particular buckets). Screenshot every family at every size.
> 3. **Cash: honor the selected source in the entry actions** — when a cash source is selected on the Cash screen, "Bezahlt" and "Erhalten" must preselect THAT source; when the scope is "alle Quellen", preselect the MAIN/default wallet. Same rule in the Cash-Wallet widget (it already passes source — make the in-app screen agree).
> 4. **Cash-flow chart: TAP ONLY, no drag-scrub** — owner: horizontal scrubbing fights the page's vertical scroll. Remove the drag gesture, keep tap-to-select a month (sticky selection + readout stay). This REVERSES the scrub reuse from round 3 — keep the haptic on tap, drop the drag handler so vertical scrolling is never intercepted.
> 5. **Built-in tags: a reset + explanation path** — a way to restore the default built-in tags, and at minimum a visible description of what each default tag represents (owner: "so you get the default back or you see at least what the default tag represents").
> 6. **Quick Links: per-button TARGETS, not just action types** — owner wants e.g. three buttons each opening a DIFFERENT cash source overview (main cash of this portfolio · the savings portfolio's cash · a second bank source), and the same parameterization for "open portfolio" (which one) and "add transaction" (into which portfolio/source). So every catalog entry becomes {action + target parameters} with a picker per entry, an icon that reflects the target (monogram/initial where sensible), and deep links that carry the parameters. Keep the grid capacity rules (3/6/4/8) and the no-label default.
> 7. **A graph for ENTIRE ACCOUNT wealth** — net worth across all portfolios as a chart (the Vermögen hero currently has the number; the aggregate SERIES is the gap). NOTE the known constraint: the platform serves per-portfolio history only and client-side summing of series is forbidden (§7.1) — so check whether an account-level history endpoint exists; if not, this is a platform ask, and the app ships whatever honest approximation the server can back (e.g. the single-portfolio chart with a scope note) rather than summing locally.
> 8. **Widget tap default = just open the app, no page** — owner: always landing on Overview when tapping a widget edge is annoying. Change the default whole-widget destination to a plain app launch (last screen / no forced navigation); keep specific deep links only where the tap target is genuinely specific (a ticker row, an action button, an explicitly configured Quick-Links tile).

> 🟡 **ROUND 6 ADDENDUM — ledger stats card restyle (owner 2026-08-18)**: the "Für diese Auswahl" card on the cash transactions screen should carry more weight and less chrome. Owner: *"make the 'for this selection' stand out a bit more. like move the xx movements text below it and remove the for this selection up top and just put on the bottom corner selected or something."* Concretely: **drop the "Für diese Auswahl" header line at the top**; let the figures lead (Netto is the headline, in/out beneath it per the current hierarchy); **move the "xx Bewegungen" count line BELOW the figures** instead of sitting beside/above them; and put a small, quiet scope marker in the **bottom corner** of the card (e.g. "Auswahl" / "Ausgewählt", muted, small) so the scope is still stated but no longer announces itself first. Net effect: the numbers stand out, the labelling recedes. Keep the stats content itself (Netto, Zufluss, Abfluss, count, Ø Betrag, größte Bewegung, Umbuchungen, per-tag details), the honest-scope rule, discreet masking, tabular figures and EN+DE.

> 🔴 **ROUND 6 ADDENDUM 2 — widget refresh flash + config state (owner 2026-08-18)**:
> 9. **Black flash on refresh in light mode** — owner: *"wenn sich die widgets aktualisieren zeigen sie kurz schwarz an obwohl ich white mode angemacht habe"*. Round 2 painted the loading layout (day `#FBFBF9` / night `#161B22`) and round 3 hooked widgets to the app theme, so this is the remaining hole: the transient frame during an update still resolves DARK under a LIGHT app theme. Likely causes to check in order: (a) the intermediate/placeholder frame uses the night palette or a system attr instead of the app's persisted theme preference; (b) the widget reads the SYSTEM dark-mode flag while the app theme is an in-app preference (his system is light but the app runs Dunkel + Reines Schwarz — the two disagree, and the flash frame is likely picking the wrong one); (c) `provideContent` publishes a frame before the theme is resolved. Fix so NO frame in the update cycle is ever painted in the wrong theme, and verify by forcing refreshes in both app themes on device.
> 10. **Widget config: explicit SAVE + load the instance's CURRENT settings** — owner: *"mache überall speicher buttons darunter und lade die einstellungen vom jeweiligen widget. weil wenn ich zb einstellung x als standard habe und jetzt einstellung y einstelle und dann das menu neu öffne und dann wieder x ausgewählt ist stört das"*. Two defects in one: (a) config sheets/editors open showing DEFAULTS instead of the values that instance actually has → every editor must hydrate from that specific `glanceId`'s persisted state before first paint; (b) no explicit commit affordance → add a **Speichern / Save** button at the bottom of every widget configuration surface (config Activity AND the in-app builder's per-widget editor), with the established immediate-apply behavior on commit (write state → repaint from cache → then refresh). Also handle cancel/dismiss sanely: leaving without saving must not silently change the widget. This pairs with round-6 item 1 (instant repaint) — same commit path.

> 🟢 **BOARD 2026-08-18 — #79 item 6 DESIGN CONFIRMED (remembered devices, #1327)**: our literal `POST`/`DELETE /auth/remembered-device` premise was correctly refused; the platform confirmed a PLURAL management API instead — `GET /api/v1/auth/remembered-devices`, `DELETE …/{handle}`, `DELETE …` (all), **session + bearer on the `account:security` we already hold**. Rows: `{handle, createdAt, lastSeenAt, expiresAt}` with historical timestamps possibly `null` for pre-metadata bindings. **`handle` is a domain-separated SHA-256/base64url digest of the raw cookie id** — resolved ONLY while enumerating the authenticated user's reverse index (no global lookup, no client user id, no raw device id in any response). One/all revoke clears binding + reverse-index member + quick-auth marker + metadata; unknown/expired/foreign handles are **idempotent no-op success**. The singular MINT route stays browser-session-only (`bt_rdid` must land in the Custom Tab's cookie jar) and the singular public delete stays cookie-bound. **This is a DESIGN tick, not go-live**: we may build the "Vertraute Geräte" adapter against this contract now but must not enable it until they tick the #1327 merge.

> 🟢 **BOARD 2026-08-18 — #79 item 7 DESIGN CONFIRMED (bearer Google link, #1328)**: shape locked — `POST /api/v1/auth/google/link/start` (cookie + bearer on the `account:security` we already hold) returns a Google authorization URL backed by a **short-lived one-time server ticket**; Google's public `GET /api/v1/auth/google/link/callback` atomically consumes it and redirects to a **newly registered native deep link `bettertrack://oauth/google-link`** with `google=linked` or the web flow's stable error taxonomy. Security shape: opaque state = independent random handle + 256-bit secret stored only as hashes, constant-time compared, atomic Redis consume; the ticket binds the caller's user id AND the server-selected redirect, so no account selector or redirect target can come from the callback; both legs on the login per-IP rail, start additionally per-account throttled, start/success/failure audited; the bearer path **can never mint a session** and existing cookie start/callback behavior is untouched. They **rejected device-code polling** with a reason worth remembering: the registered native callback already provides the rendezvous, so polling would add another credential, endpoint and pending-state lifecycle without improving the trust boundary. **DESIGN tick, not go-live** — build the Connections adapter + register the deep link against this contract now, enable only on their #1328 merge tick. NOTE for us: `bettertrack://oauth/google-link` is a NEW scheme path alongside the existing `bettertrack://oauth/callback` — keep `DeepLinkTabsTest` honest when it lands.

> 🟢 **DONE 2026-08-19 — MOVERS OVER MULTIPLE TIMESPANS (owner 2026-08-18: *"kann man das mit der tagesbewegung auch für mehr zeitspannen machen. also tages wochen monats jahres und all time"*)**. Gate **3620/0 both flavors, lint 0 errors**. The Bewegungen card gets its own range vocabulary (`ui/insights/InsightsMoveRange.kt`) deliberately separate from `BtInsightPeriodKind`, because the two are different questions with different data sources: **Heute** = server `dayChangeEur`/`dayChangePct` (€, aggregatable); **1W/1M/1J** = `GET /assets/{id}/history?range=1W|1M|1Y` last-close-over-first-close (**percent only, never €**); **Seit Kauf** = server `unrealizedPnlEur` (€). Two honesty rails drive that table and are documented in the file: (1) a € contribution per position over a period needs the holding's quantity *through* the period and **no endpoint states it**, so ranged cards print percent and say so; (2) "all time" is NOT a MAX close series — that series predates ownership for most positions, so the honest all-time answer is the recorded-cost-basis result. Server range vocabulary verified as exactly `1D|1W|1M|3M|6M|1Y|5Y|MAX` (required param, unknown = 400) and mapped through the existing `AssetRange` enum (`MarketRepository.kt:124-139`), so no invented wire value is reachable. Percentages are taken inside the asset's trading currency (the history wire carries `currency` and no FX rate) — the ratio is unaffected but it is a price move, not a euro result, and `bt_insight_movers_price_note` says so. **Three defects fixed en route**: (a) the Drive-mode source ignored `range` and returned the whole cache — a card captioned "1 Woche" would have printed a multi-year move; (b) an "Andere" bucket was **summing percentages** (−4,69 % + −4,39 % → "−9,08 %"); (c) a coverage string said cost basis where price history was meant. Plus a cold-start wiring gap. **Follow-up filed, not done here**: the predecessor's "no batch endpoint" premise was false — `GET /portfolios/{id}/history?overlay=true` returns every held asset's series in ONE call, so the current per-asset fan-out can be collapsed; deliberately not rewired mid-round.

> 🔴 **COORDINATOR TASK 2026-08-19 - three source files are BINARY to grep and therefore exempt from every source-scanning discipline test.** Confirmed by inspection, and the NUL bytes are **deliberate, not corruption**: `ui/social/ItemThreadSection.kt` uses `commentId + <NUL> + emoji` as a composite busy-key (2 occurrences: the KDoc and the expression), `ui/components/BtRangeSegmented.kt` uses `labels.joinToString(<NUL>)` as a remember-key separator, and `ui/components/BtProfileIconTest.kt` feeds a literal NUL as hostile input. NUL is a *correct* separator choice - it cannot occur in an id, a label or an emoji. The defect is only that all four are written as **raw bytes instead of the `\u0000` escape**, so `grep`/`file` classify the files as binary and skip them silently. Consequence: `BtThemeDisciplineTest` (raw `Color(` literals banned in `ui/**`), `SheetStateScrollGuardTest`, `SettingsTaxonomyTest` and every other source-reading test have **never once examined two production UI files**. Fix = replace each raw NUL byte with the literal escape `\u0000` (identical semantics; same class as the `CashExport.kt` BOM fix, where a literal BOM byte tripped lint `ByteOrderMark` and was replaced by the `\uFEFF` escape). **Deferred deliberately to a quiet lane** - applying it while builders hold the tree would hand them a failure that is not theirs. Expect the newly-visible files to possibly FAIL a discipline test for the first time; that is the point of the fix, not a regression.

> 🟢 **DONE 2026-08-19/20 - SETTINGS-PARITY ROUND 1 + FEEDBACK v1 LIVE (three parallel builders + coordinator quiet-lane fixes; final gate over the combined tree: 3669/0 BOTH flavors, lint 0 errors).**
> **(a) Feedback v1 flipped ON** - both flags (`FeedbackFlags.enabled`, `FEEDBACK_SCOPE_ENABLED`; authorize now requests 20 scopes). New `feedbackEntryVisible(mode)` gate: rows require a server account (Drive-autonomous shows nothing - a composer whose Send can only fail is worse than no row). New `FeedbackEntryDisciplineTest`, `FeedbackFailureCopyTest`; HTTP-429 copy without inventing a server code. **Smoke test result: `403 INSUFFICIENT_SCOPE` on a genuinely pre-existing bearer** (whole-log proof: zero token refreshes fired) - the platform's "existing consents already carry it" claim is FALSE on prod; evidence + two questions posted to the board. Ship anyway: failure mode is one clean translated sentence, draft preserved, self-heals at next login.
> **(b) Connections round** - Google-link deep link `bettertrack://oauth/google-link` verified THREE ways (live-server probe of the public callback -> `?error=google_state` redirect; contract; platform #81 answer). Manifest filter added; `MainActivity` now path-discriminates (`/callback` vs `/google-link`; previously a successful link would have surfaced as `STATE_MISMATCH`), proven on device via logcat (link-status 200, zero auth lines). Connect flow wired (`POST /auth/google/link/start` -> `{authorizationUrl, expiresAt}`, Custom Tab, return -> re-read `link-status`; success = absence of `error`, never the `google=linked` spelling - rename-proof). Authorized Apps live (probe now 200): own grant = gold "Dieses Geraet"-row, revoke suppressed (platform #1390 will add `firstParty`/`current`; DTO already decodes them tolerantly), paranoid scopes struck-through "(inactive)", `google_state`/`google_verify`/`google_failed` copy added EN+DE. Logout self-revocation now really works and is platform-blessed.
> **(c) Passkeys + Trusted devices** - two new native screens under Sign-in & security (web-link row replaced; registration keeps its OWN labeled web hand-off row per the granularity rule). 6 endpoints (`GET/PATCH/DELETE /auth/passkeys*`, `GET/DELETE /auth/remembered-devices*`), DELETE-with-body + `X-Bt-No-Reauth: 1` on mutations, base64url handle asserted unencoded on the wire, every revoke re-reads the list (idempotent-no-op honesty). Device: both screens load live data - both EMPTY on the owner's account (zero passkeys; zero remembered devices, consistent: no account PIN => mint route unavailable), so populated rows/sheets are unit-test-proven only (13 tests) - **needs a non-production account for a visual pass someday**. DE subtitle overflow found+fixed on device.
> **(d) Coordinator quiet-lane** - `feedback:write` plain-language scope label EN+DE (map+test now 20; was about to render as a raw wire string after next login). **NUL-byte fix**: the 4 deliberate raw NUL separators in `ItemThreadSection.kt` (busy-key), `BtRangeSegmented.kt` (join separator), `BtProfileIconTest.kt` (hostile input) rewritten as `\u0000` escapes - semantics identical, but the three files were BINARY to grep and thus invisible to every source-scanning discipline test until now (pre-checked: no violations surfaced).
> **Open defects filed here, not fixed:** (1) `AuthRepository.revokeGrantBestEffort()` picks `firstOrNull { clientId == ours }` - with the app on TWO devices, logout on one may revoke the OTHER's grant; dormant while the call 403'd, LIVE since 2026-08-19; one-line fix once #1390's `current` ships. (2) Connections success snackbar may not render on `btSheet` routes (pre-existing; failure path unaffected - renders inline). (3) go-live table wording `{passkeyId}` vs deployed openapi `{id}` - cosmetic, but anything else built off that table should re-check. (4) openapi marks remembered-device `expiresAt` required non-nullable; we type it nullable defensively (missing key must degrade to a dropped clause, not a blanked screen).
> **Board state:** #81 answered (deep link verbatim, `{devices}` envelope, listGrants = annotate-not-hide as #1390, web trusted-devices panel = #1391, server-candidate widening recorded); #82 posted (mobile-dev.bettertrack.at/app infra for the KMP wasm build: static hosting, CORS-or-proxy, separate PKCE web client). Feedback-403 evidence posted. **KMP Phase W0 in flight** (separate worktree, `kmp-ios`): wasmJs target bring-up + honest per-layer web-port plan into `docs/KMP_PLAN.md` (owner 2026-08-19: real KMP for future iOS AND web; emulator shortcut rejected).

> 🟢 **DONE 2026-08-19 ~23:10 - FIRST-RUN WIZARD NATIVE (last piece of settings-parity round 1; gate 3691/0 BOTH flavors, lint 0, +22 tests).** Closes the real gap: the web's FirstRunGate exempts /oauth/authorize, so app-created accounts NEVER saw setup anywhere and stayed firstRunCompletedAt=null forever. Signal is a strict tri-state (`FirstRunStamp` Absent/Never/At via a PROPERTY-bound serializer - the non-nullable-property trick is what lets a custom serializer see JsonNull at all; nullable properties swallow it before the serializer runs): key missing -> UNKNOWN (never gate - older servers must not shove established users through setup), null -> PENDING, timestamp -> DONE, malformed -> UNKNOWN. Wizard = sequencing shell over the REAL screens (SecurityScreen incl. all 5 sub-screens, LanguageScreen, TaxSettingsScreen, PublicProfileScreen) on a local editor stack - nothing forked; step ids/order mirror the web's stepMeta.ts literally, test-pinned. **Deliberate divergence from web, flagged for the owner:** web's "Do this later" stamps the server flag; ours records a LOCAL account-scoped dismissal only, so the account stays PENDING and the Settings "Einrichtung abschliessen" escape row stays visible - only the terminal step calls complete. Gate sits below RootGate + app-lock (RootGateOrderingTest +3), server-account modes only. Device: six cold-start captures with NO wizard flash, Settings byte-identical, and the new PII-free `firstRun=DONE` log line proving the field genuinely decoded (UNKNOWN and DONE render identically everywhere - without the log a pass would be indistinguishable from the field never arriving). **PENDING path never rendered on a device** (his account is established; creating accounts is forbidden) - unit-test-proven only; the first fresh signup through the app exercises it for real.

> 🟢 **DONE 2026-08-20 - MOVERS BATCH-FETCH VIA PORTFOLIO OVERLAY (gate 3711/0 both flavors, lint 0, +20 tests).** All three price spans (1W/1M/1J) of the Bewegungen card now come from ONE `GET /portfolios/{id}/history?range=...&overlay=true` per portfolio instead of one call per held asset - measured on device: 11 asset calls -> 1 overlay call per span, 0 asset calls left. **Two dossier claims corrected en route** (dossier updated): the portfolio endpoint accepts `1D|1W|1M|6M|1Y|5Y|MAX` (so no 1W hybrid was needed), and overlay rows are daily carry-forward closes in native currency for every asset ever transacted. **PRODUCT CHANGE, coordinator call, owner may veto:** the displayed percentages on 1W/1M (and slightly on 1J) moved, because the old path's first point was an INTRADAY candle while the overlay is the same daily close-to-close series the portfolio curve is built from (e.g. ONDS 1M +34,14 % -> +29,55 %; closed-market names on 1J are bit-identical). Rationale for keeping the new basis: it is the series the server itself builds the portfolio curve from (consistency), close-to-close is the standard convention, and coverage improved 10/11 -> 11/11 (the custom asset has a daily series but no intraday one). Byte-identical old numbers are only reachable via the N+1 fan-out, which survives untouched as the Drive-mode/interface default - a one-line revert if the owner prefers the old basis. Failure honesty: overlay error -> existing error state, exactly 1 request, no silent N+1 fallback; empty `assets` is a real answer, missing `assets` is an Err. ETag store unaffected (plain call byte-identical). Fetch cap lifted only on the batching source; fan-out path keeps it.

> 🟢 **DONE 2026-08-20 early - PARANOID VAULTS A-WAVE + S1 (four builders; final gate 3842/0 BOTH flavors, lint 0 errors; everything behind `ParanoidVaultsFlags.enabled = false`, live v1 rail untouched, `VaultContract.FORMAT_VERSION` unchanged).**
> **A1 - §13 QR both legs** (`vault/pv/` + `ui/vault/qr/`, 54 tests): binding `btvault1:m=&v=[&n=][&f=]` parser that NEVER throws on camera input (fuzz-proven), every reject reason case-covered incl. the v2-collision discriminated by JSON body shape; UUID case STRICT (vaultId is HKDF info input - lowercasing would derive different keys); `f` fetch-then-compare only (offline check impossible - KDoc'd); CameraX+zxing scanner (NO Play services - github flavor safe), camera permission declared never prompted; sender = parameterized `BtQrCode` (byte mode/UTF-8/EC M) with the TOTP call site proven byte-identical TWICE (module-diff vs pre-change hints + on-device screenshot decoded via OpenCV); 60s blank+re-show, FLAG_SECURE on both legs; `VaultHeaderProbe` stubbed (E1-gated), a designed "can't verify yet" state - never a stored dead phrase.
> **A2 - §12 device custody** (`vault/pv/custody/` + `ui/vault/custody/`, 45 tests): per-endpoint keystore `{vaultId, custody: wrapped|plain, payload}` over mnemonic ENTROPY (words re-derive via the v2 wordlist import - one checksum for mint/manual/QR paths, asserted); wrapped = Argon2id(device pw, per-endpoint salt, the imported `VAULT_ARGON2_PARAMS` profile) -> AES-GCM with vaultId as AAD (replant fails the tag, tested); plain custody BUILT per owner ruling (calm one-sentence warning, still under EncryptedSharedPreferences); lockout 5->30s doubling cap 300s (computed, monotonic, overflow-proven, injectable clock); session ends at process death / lock() / the EXISTING app-lock hook (flow-shaped like `linkVaultLockToAppLock` - deliberately NOT wired into AppGraph while the flag is off); `PvCustodySourceDisciplineTest` bans payload-bearing logs (caught 3 of the builder's own during dev); custody prefs in their OWN file surviving logout wipe - deliberate: phrases are not session data.
> **S1 - E0 envelope v2 + vectors + dormant Room** (32 tests): ALL 25 platform vectors ported literally and green (per-AAD-field anti-swap, bit-flip+control, canonicalization: key-shuffled header -> byte-identical wire AAD; real AES-256-GCM through the app's own primitives); header mapping v2->E0 documented (portfolioId->docId, blobVersion->docVersion, +keyId/keySlots/accountBinding, +'header' docKind; iv/wrappedKc/accountBinding switch to CANONICAL base64url - own strict codec); **one real contract delta found**: E0 moves `cashBudget`+`cashBudgetFire` common->portfolio vs the v2 partition - expressed as a named delta constant, asserted both directions, `v2Partition` family replays against the pv location; Room v11->v12 dormant (`vaults`, `vault_docs` PK (vaultId,docId), `portfolios.vaultId/vaultAlias` nullable) with **`exportSchema` finally ON** (long-flagged defect), committed 11.json+12.json, 11 historical entry points replayed against real SQLite + Room's own validator, migration proven purely additive.
> **#4-ambiguity verdicts from the E0 contract** (board follow-up posted): Q1 fingerprint = first 16 CHARS of base64url (pinned by regex+length), Q3 seed = standard 64-byte BIP39 seed, Q5 accountBinding = 43-char unpadded base64url (fixture digests recomputed in Kotlin); Q2 K_wrap=32B implied-only (one-word confirm requested); **Q4 HKDF salt genuinely OPEN and blocks E3**. Two more recorded in code, not guessed: retirement-proof key schema, step-up MAX_PASSWORD_LENGTH.
> **Flake fix**: `BtCrashGuardTest` deliberately crashes an unguarded coroutine; kotlinx-coroutines-test's service-loaded `ExceptionCollector` (armed permanently by the first runTest in the fork) parked it and rethrew as `UncaughtExceptionsBeforeTest` into the NEXT runTest class (rotating victims, ~50%, latent until +99 test classes reshuffled ordering). Root-caused against the 1.10.2 sources jars, reproduced via forced-ordering probe, fixed with an `@After` drain through the only public entry point that empties the collector (structural match on suppressed causes - anything not the planted exception RETHROWS, so the cleanup can never hide a real leak). Test's assertions untouched; 6 consecutive filtered runs green; KDoc explains why wrapping in runTest would bypass the very path the test exists to prove.

> 🟢 **DONE 2026-08-20 ~08:10 - FEEDBACK v2 READ HALF: "Meine Einreichungen" + `feedback:read` flipped + RE-SMOKE 201 (gate 3883/0 both flavors, lint 0, +41 tests).** Status list built against the deployed openapi (NOT the tick - one drift found: the caller row has NO `updatedAt`, admin-only; modelled per openapi with an ignore-tolerance test). Display names chosen per the owner's "make up better names": Eingegangen / Angesehen / In Arbeit / Fuer spaeter vorgemerkt / Nicht umgesetzt (+Begruendung, NIE rot - test-pinned) / Umgesetzt (+Versions-Chip verbatim). Unknown future statuses render as neutral raw-wire chips, never vanish; `unreadReplyCount` badge built but `count>0`-gated (reserved until threads ship). **Scope flip evidence-based**: the owner's PRE-EXISTING bearer answered `GET /feedback/mine` 200 before the #1393 tick appeared -> the grant-widening is live; ceiling carries `feedback:read` as 21st scope in the deployed openapi -> `FEEDBACK_READ_SCOPE_ENABLED = true` (coordinator, same evidence standard as the write flag; NOT flipping was the identical works-today-breaks-on-relogin latent bug as yesterday). Authorize now requests 21 scopes; OAuthScopeTest fully re-pointed.
> **RE-SMOKE (the platform's asked-for one-shot): `POST /feedback` -> 201 in 177ms** over a provably un-refreshed pre-existing bearer (full logcat capture: the app's ONLY network traffic was 6 chat polls, the POST, the /mine GET - zero /oauth/token, zero 401). Finding for the record: #1393's widening is RETROACTIVE to already-issued tokens - no refresh, no re-login needed. Yesterday's 403 on the same token generation stands as the disproof of the original "already carried it" claim. On-device end-to-end: success card -> Meine Einreichungen shows the row (Sonstiges - Heute - Eingegangen). Screenshots + full logcat under /Users/cwiesi/bt_scratch/feedback-resmoke-2026-08-20/. Phone restored (logged in, stayon false, dozing).

> 🟢 **DONE 2026-08-20 ~09:00 - PARANOID A3+A4: key core (public half) + §21 creation ceremony (gate 3947/0 both flavors, lint 0, +64 tests, flag-gated).** A3: mnemonic issuance (SecureRandom 128 bit -> 12 words via the ONE wordlist path - custody's entropy<->words helpers are CALLED, not copied; discipline test pins the import AND the absence of a second path); BIP39 seed via PBKDF2-HMAC-SHA512 through Bouncy Castle (JCE providers disagree on char->byte) pinned against the FULL vendored Trezor english vector set **24/24 green** (+ a production-parameter test: empty passphrase, since Trezor's rows use "TREZOR"). **E3 stop line honoured**: `pvVaultWrapKey`/`pvKeyFingerprint` fully shaped with E0's answered literals, but their shared `pvDerivationSalt()` THROWS naming board ask #83 Q4 verbatim; `PV_E3_PINNED=false`; three vector tests skip visibly naming the ask, a fourth fails the moment the flag flips without the work, a source-scan pins exactly-one-throw. A4: ceremony state machine NAME->MEDIA->WORDS->VERIFY->ACKNOWLEDGE->CUSTODY->DONE (pure, WizardState idiom); ONE-word verify per the owner's Q2 ruling (injectable index, QR-path normalization, wrong answer -> back to words, NO lockout, ten wrong == one wrong); ack unskippable (proved twice); Drive/Both render as designed disabled cards (E5-gated), never dead buttons; FLAG_SECURE across the WHOLE flow; 2x6 numbered word grid, no clipboard, no rememberSaveable, no logs; §16 contrast beside the warning (device password loses nothing / phone loses nothing while a copy exists / phrase loses that vault). DONE persists NOTHING - §12 binds a stored phrase to its vault id as GCM AAD and E1 hasn't minted one; storing under a client-invented id would be the dead-words failure §13 exists to prevent. 44 strings EN+DE. Device: regression-only pass clean (cold start 1608ms, no crashes, no pv surface reachable, feedback screens live incl. the Eingegangen row).

> 🟢 **DONE 2026-08-20 ~13:15 - §4 KEY CHAIN COMPLETE + TAX YEARS DE-LOCKED (gate 3998/0 both flavors, lint 0, +51 tests).**
> **Keys**: `pvDerivationSalt()` = new named `VAULT_HKDF_EMPTY_SALT` on the SHARED v2 HKDF primitive (no second HKDF, no second spelling of empty); `pvVaultWrapKey` (empty salt, wrap-info+vaultId, L=32, IKM=64B BIP39 seed) and `pvKeyFingerprint` (16 base64url chars) live; key-slot wrap/unwrap with AAD `bettertrack-vault-key-slot-v1:vaultId:keyId`, wrappedKc=base64url(IV||CT||TAG), exact 60B. **Vector honesty**: RFC 5869 A.1-A.3 vendored+independently recomputed (A.3 pins the zero-salt ruling; empty==32 zero bytes proven, !=any other salt); full-chain self-fixture generated via Node WebCrypto (independent second implementation, cross-checks Kotlin/BC) marked `self-derived pending platform E3 fixture - replace, never merge` (string asserted); `PV_E3_PINNED` stays false = "platform vectors not here yet", 3 tests still visibly SKIPPED on that. Retirement-proof check tightened to exact 44B DER-SPKI Ed25519 (X25519 near-miss as the negative - one OID byte apart); deliberately NOT tightened on the read path (refusing to send costs a retry; refusing to open costs the vault - asymmetry documented). MAX_PASSWORD_LENGTH=200 recorded, not enforced.
> **Tax years (#1425)**: openapi verified (no lock field anywhere, no unlock/relock routes; tick matches). `locked` dead -> nullable `lastChangedAt` through DTO/domain/screens; Closed/Still-open badges + both note-footnotes REMOVED (4 strings dropped from both languages); new quiet "Zuletzt geaendert <MEDIUM date>" clause that degrades to SILENCE on null/unparseable (never "never changed", never raw ISO). Device: his three real years (2026/2025/2016) render badge-free; server returns lastChangedAt=null for all three, so the caption path is unit-proven only - honestly reported, no test data created.

> 🟢 **DONE 2026-08-20 ~19:00 - FEEDBACK WIDENING (#1400) CONSUMED (gate 4025/0 both flavors, lint 0, +27 tests).** Five categories (wire `feature|bug|other|help|improvement` verified against openapi - no drift this time): DE Feature / Verbesserung / Bug / Hilfe / Sonstiges, each with subline+glyph; TWO forced copy fixes beyond the new labels - `feature` lost "/Verbesserung" (a label covering both would file improvements under the wrong wire value while LOOKING right) and `other`'s subline counted "neither of the two" with four neighbours now (both test-pinned). Rows driven by `FEEDBACK_CATEGORY_ORDER` with a permutation test so a sixth wire value cannot ship unpickable. DELETE flow built (confirm sheet, re-read-decides, 204-tolerant via unitApiCall since a bodyless 204 would map to APP_EMPTY_RESPONSE through apiCall); `FEEDBACK_OPEN_LIMIT` catalogued EN+DE with the REAL remedy, and the failure-copy ordering fixed so a 429-status cap refusal can never render as "try again later" (false advice for a cap that clears only by triage/deletion).
> 🔴 **PLATFORM BUG FOUND + REPORTED: `DELETE /feedback/{id}` answers 500 INTERNAL deterministically on prod** (4 attempts, 2 builds, 64-byte envelope, catalogued INTERNAL copy = well-formed error body, not a validation refusal). Target: the re-smoke row - whose only non-default property is being ALREADY TRIAGED (`working_on_it`), so a soft-delete path handling only `new` would look exactly like this; hypothesis on the board. Client behaved perfectly: German copy + Retry, row STAYS (the app claimed nothing), re-read confirms. The re-smoke row therefore still sits on the live account until the platform fixes their route.
> 🔴 **REAL UI DEFECT CLASS FOUND: shell-Scaffold snackbars are INVISIBLE on `btSheet` destinations** (the sheet layer composes over the snackbarHost). The widening builder hit it live - the first delete failure showed the user NOTHING - and fixed the submissions screen inline (banner above the state `when`). Two more instances flagged by earlier builders now confirmed as the same root cause: TrustedDevicesScreen's "Vergessen." confirmation and ConnectionsScreen's link-success snackbar. Sweep builder dispatched.

> 🟢 **DONE 2026-08-20 ~19:30 - SHEET-SNACKBAR ROOT-CAUSE FIX (gate 4030/0 both flavors, lint 0, +5 tests).** The audit changed the answer: not 2-3 instances but **21 of 60 btSheet destinations** reported outcomes through the shell snackbar and EVERY one was invisible (only 3 non-sheet surfaces use the mechanism at all). Root cause was double: the sheet layer is a later sibling of the Scaffold in the shell Box, AND BtOcclusion clips the shell's draw to the visible strip - the host was never even rastered. **Fix = move the ONE host above the sheet layer** (last child of the shell Box, BottomCenter, offset = measured bottom-bar height while sheets are closed / nav inset while covered - bar height measured via onSizeChanged, never hardcoded) instead of deleting the app's single feedback idiom across 17 files. FeedbackSubmissionsScreen deliberately STAYS inline with its rationale corrected: a deletion verdict must stay readable against the list; a faded snackbar cannot be re-read. New `SheetSnackbarVisibilityDisciplineTest` (5 rules: no Scaffold slot, host after BtSheetHost in source order, BottomCenter+sheetsClosed-dependent offset, controller provided over the sheet layer, allowlisted hosts only) - **verified to bite**: deliberate breakage made 4/5 fail, then reverted. Device-proven with a clever non-destructive case: the DEBUG dev-unlock hint snackbar rendered OVER the settings sheet (exactly the class that was invisible), and repositioned correctly above the nav bar after closing the sheet. Trusted-devices "Vergessen." + Connections link-success now ride the proven host (text unit-proven; his empty device list / not unlinking = honestly unverifiable live).

> 🟢 **DONE 2026-08-21 ~01:07 - DELETE RE-VERIFY PASSED (device, one attempt as agreed): `DELETE /feedback/{id}` -> 204 in 294ms, automatic re-read 8ms later, row GONE, inline "Geloescht." rendered (the sheet-snackbar fix proving itself live).** The re-smoke test row is off the live account - cleanup complete. Notable: the owner's list contained a GENUINE submission ("Make Tax Work in webapp Like in phone", Feature, In Arbeit) - the feedback pipeline is in real use by the owner himself; the deleted row was verified by body text before deletion, nothing else touched.

> 🟢 **DONE 2026-08-23 - E1 BLOB-STORE CLIENT + E3 VECTORS PINNED + BUILD-INFRA FIX (one gate over the combined tree: 4097/0 BOTH flavors, lint 0, +123 tests, ZERO daemon OOMs for the first time in weeks).**
> **E3 - zero mismatches, the whole chain:** every platform value reproduces byte-exact incl. the FULL 921-byte envelope, the 632-byte wire header, both IVs, the BIP39 seed and accountBinding. Ported from their `keys.test.ts` (fetched at `970a5f1f`, not from tick prose): the canonicalization case they flagged as most-likely-to-drift (header shuffled at EVERY level -> their exact wire bytes, pinned against THEIR string not our own output, through both entry points), the mirror rule (a non-canonical header is authenticated as literal AAD, never reserialized), their 6-case tamper set, and the deflate stream isolated at 261 bytes - which independently proves our hand-ported fflate is byte-identical to the web's and NOT `java.util.zip` (a future "simplification" to the stdlib would have made every vault silently unreadable across clients). **Real security gap found and closed by the port:** our `pvRequireContentKey` checked only LENGTH, not the all-zero wiped-K_c sentinel - a caller using a key after lock would have encrypted real rows under a publicly known constant. Self-derived fixture DELETED; the tripwire re-pointed to defend "platform bytes, named file+commit, never self-described as self-derived, and no selfderived file may exist" - it fired for real mid-run on the builder's own provenance wording.
> **E1 client (first S2 slice)**: full route surface + DTOs + `PvBlobStore` with typed outcomes. `docId` IS the portfolio uuid made structurally unfakeable (`@JvmInline value class`, one field, source-scan bans mapping-shaped helper names). **"Write without an ETag" made unrepresentable in three layers**: a 2-inhabitant `PvDocPrecondition` (no None, no nullable), a `BtApi` with no unconditional PUT (wildcard baked into `@Headers`, `If-Match` a non-null `String` so omission is a COMPILE error), and a reflection test reading the interface back. **The two `412`s are disambiguated client-side** because the wire cannot do it (no published code): a local `(vaultId,docId)->(writeId,sha256)` ledger consulted BEFORE the request refuses same-writeId-different-bytes locally with the real remedy - the livelock is structurally impossible, not merely reported. Two `explicitNulls=false` traps defused (required-AND-nullable request fields would be silently OMITTED -> a 400 naming nothing).
> **THREE DRIFTS FOUND BY BUILDING, board-posted:** (1) BLOCKING - `DELETE /vaults/{id}` is bearer-reachable but `GET /vaults{,/{id}}` is session-cookie ONLY, so a phone can delete a vault it cannot read AND cannot obtain the `headerDocId`/`commonDocId` a QR-provisioned phone needs - the §13 flow dead-ends; two equivalent fixes offered, no workaround invented. (2) the tick's candidate route is `DELETE` but the deployment declares `GET` (openapi wins, we built GET). (3) `304/412/413/428` are prose-only, absent from every response map.
> **COORDINATOR BUILD-INFRA FIX**: (a) my own lane-lock recipe, handed to every builder for days, used a pgrep pattern matching NOTHING on this machine (the daemon is `GradleDaemon`, not `GradleMain`; the wrapper runs as `-jar gradle-wrapper.jar`) - and had it matched, it would have waited on the PERSISTENT idle daemon forever. Only the `mkdir` mutex ever worked; the broken half is removed. (b) `kotlin.daemon.jvmargs` 3g -> 5g: the 3g figure was measured at ~1/4 today's suite size and OOMs reproducibly at ~4100 tests whenever both flavors compile in one invocation - masquerading as "Internal compiler error"/GC-overhead, i.e. reading like CODE failures. The documented remedy was "kill daemons and retry" = paying the full compile twice; it cost this project hours across many rounds. This gate: both flavors in ONE invocation, 12m42s, zero OOMs.

> 🟢 **DONE 2026-08-23 - QR CROSS-CLIENT CONVERGENCE (gate 4110/0 both flavors, lint 0, +13 tests).** Their 16 E7 conformance vectors transcribed byte-for-byte (from PR #1451 branch `task/1417` @ `e1882d3a` - E7 is UNMERGED, vectors are branch-only, not on their main) and replayed against our shipped parser, each fixture row carrying `web`/`android`/`agreement` side by side. **13/16 identical decision AND identical parsed payload; 3 mismatches.**
> **Their prediction about us was wrong on the case they called most likely to diverge**: they expected an Android scanner to strip a trailing `#frag` via `Uri.parse` and ACCEPT. We do not use `Uri.parse` (our decoder is self-contained precisely because Android's `URLDecoder` and OpenJDK's disagree), `#` stays glued to the `v` value and breaks the UUID - **both clients reject, same reason.**
> **Real divergences:** (a) **duplicate `m`/`v`** - they reject, we accept first-wins; §13 is SILENT (their reject rule lives in their README, not the spec). Builder recommended converging on REJECT and deliberately did NOT change our behaviour unilaterally: §13's own rationale is that form-urlencoded "parses identically everywhere", and duplicate keys are exactly where form parsers do NOT agree (first-wins/last-wins/collect-all all exist in the wild). (b) **leading `?`** - they accept (URLSearchParams leniency leaking into spec), we REJECT, and §13's letter backs US (`?` is the URL's query delimiter, never form data). (c) **`n` length unit** - subtle and real: they count CODE POINTS, we count UTF-16 CODE UNITS. Their own vector cannot see it (U+00E9 is both). **64 emoji: they accept, we refuse.** Pinned by a test. Builder also flagged that OUR KDoc's stated rationale for choosing code units is factually WRONG about their code - and left the behaviour alone rather than picking a side.
> **Case 4 (control/bidi chars in `n`) - the asymmetry that matters:** inert for them (they never render the name), NOT inert for us - `VaultQrScanScreen` renders it verbatim as the answer to "which vault am I adopting?", i.e. attacker-controlled text inside a security decision. Recommendation: **sanitize at render, not reject at parse** - rejecting throws away a phrase transfer over a cosmetic hint on the one screen whose job is getting words onto the phone, and only protects clients that implement the rejection; one shared untrusted-label treatment protects every consumer regardless of who parsed it.
> **§13 `f` correction (#1500) confirmed already-correct on our side** - no code or string anywhere claims an offline pre-check. Three doc-only fixes: the ruled ORDER made explicit (compare `f` BEFORE decrypting the body; our old text had it reversed), and a now-false claim removed (`VaultQrScanState` said the E3 derivation chain "does not exist in this package" - it landed at `7fa57c2`; only the probe seam is still missing).
> **Six items §13 leaves under-specified**, all board-posted: duplicate required keys, the `n` length unit, `n` normalization (they preserve exactly, we trim+blank-as-absent), control/bidi chars, `f` parse-time validation (they reject at parse, we carry verbatim since nothing offline can judge it), and the rejection vocabulary (7 outcomes vs our 8, no 1:1 map, §13 defines none).

> 🟢 **DONE 2026-08-26 - ALL EIGHT QR RULINGS CONSUMED, our four items shipped (gate 4134/0 both flavors, lint 0, +24 tests, flag still off).** The platform ruled 4-against-itself/4-against-us on the interop items and admitted both briefing errors in writing ("our README describes our implementation and I quoted it as though it were the specification"). Our render-sanitizer recommendation was adopted AS NORMATIVE text; our emoji length-cap find and the update-required/not-our-code UX split both won.
> **The sanitizer (the one LIVE item, built first per their flag)**: `ui/format/UntrustedLabel.kt` - STRIP the bidi controls AND THEN ISOLATE the remainder in an FSI/PDI fence we own; neither half alone suffices (isolation only bounds the reorder's scope, but the label IS the security answer - a fenced RLO still spoofs inside its own fence; stripping alone would leak the label's directionality into surrounding UI). Both properties proven against the real Unicode bidi algorithm (`java.text.Bidi`), with the attack asserted to WORK on raw text first so the test proves something: RLO spoof contained, Hebrew+Arabic names preserved char-for-char with their run still RTL and the neighbours at level 0. ZWJ/ZWNJ deliberately kept (family emoji, Persian); all-controls name -> "" -> caller falls back to the trusted placeholder; idempotent; code-point-safe ellipsis. Discipline test: every read of `payload.name` in the scan UI must pass through the sanitizer (count equality).
> **The three parser flips**: DUPLICATE_KEY for any repeated KNOWN key (order-independent, identical repeats too - the ambiguity is the defect); the name cap counts CODE POINTS (64 emoji now fit; our KDoc's false claim about the web's code corrected in writing); FINGERPRINT_INVALID = 16-char base64url shape at parse, value still only after fetch, and the builder judiciously extended the shape check to the SENDER (round-trip tests had been emitting an `f` our own parser now rejects). New reasons collapse into the generic rejection copy BY DESIGN (naming "m twice"/"wrong length" would tell a shoulder-surfer how close the code was) - pinned by a whole-enum property test. Fixture re-recorded WITHOUT rewriting history (`rulingsApplied` block; per-vector notes state what each verdict was on 2026-08-23).
> **Board**: vocabulary pushback posted before the freeze (invalid-mnemonic = the most common real failure with a unique remedy; name-too-long; the legacy-code decision; the missing-mnemonic vs missing-required-key inconsistency inside their own #1508). E7+E8 are LIVE on web (they proved our transcription still byte-exact across two rebases, unasked); their #1508 counter-flips are OPEN not merged -> `n=a&n=b` temporarily diverges in the RULED direction, recorded in fixture+KDoc rather than left implicit.
> **Infra**: SESSION_DOSSIER recreated at a DURABLE path (`~/bt_scratch/SESSION_DOSSIER.md`) after the OS purged the /tmp original - updated to current truth (4134 baseline, mkdir-only lock, 5g Kotlin daemon, paranoid program state); `org.gradle.jvmargs` 2g->3g (the GRADLE daemon - distinct from the Kotlin one - logged heap exhaustion on combined gates). Known cosmetic debt: pre-existing RAW bidi characters in `VaultQrE7ConformanceTest` string literals (previous round) should become \u escapes in a quiet moment - NOT in raw """ strings where escapes don't process.

> 🟢 **DONE 2026-08-26 - REJECTION ENUM ALIGNED TO THE FROZEN 12 (gate 4141/0 both flavors, lint 0, +7 tests).** The platform's "your fold splits with one line" was wrong about what the work IS: the mechanical edit was ~4 net lines, but the fold had hidden a DECISION the one-value version never faced - what a body missing BOTH required keys answers. Resolved against the web parser's actual check order (m first) rather than by statement accident, written down as contract, pinned by its own test. 1:1 mapping to the frozen vocabulary total+injective+onto by property test, plus a source-reading guard that each enum value's KDoc names its counterpart (proven to bite: one mutated KDoc failed exactly that test). No renames - `PHRASE_INVALID` stays because "phrase" is this app's word for the mnemonic throughout vault.v2; renaming would be LESS consistent internally.
> **Two findings**: (1) the freeze is AHEAD of the web's merged code and in OUR direction - their shipped parser still has only 7 outcomes; the frozen list adds four of ours (`not-a-bettertrack-code`, `legacy-code`, `malformed`, `duplicate-key`) and renames their `invalid-name` to our `name-too-long`; the `bareString` fixture row stays a real difference until they ship. (2) NEW pre-existing cause-difference surfaced by the split: whitespace-only `m` is missing-mnemonic here (isNullOrBlank) but invalid-mnemonic there (=== ''); same reject, different frozen outcome, no vector covers it, no ruling - left unchanged, on the record.

> 🟢 **DONE 2026-09-02 - THREE TICK-CONSUMPTION ROUNDS + S2 SLICE 1 (combined gate over the isolated snapshot: 4207/0 BOTH flavors (+66 vs 4141), lint 0 errors; commits `9becc82` QR, `c86377e` E9, `5754022` S2).** Context: the coordinator session was restarted 2026-09-01 22:00 by the owner; nine unconsumed platform ticks (2026-08-26..29) were on the board; the 5h session limit killed all builders twice (22:30 and 03:10) and they were resumed from disk each time.
> **QR replay (#1508 + #1513 merged): 38/38 vectors identical on decision, value AND outcome** - both divergence lists in `pv-qr-e7.fixture.json` empty for the first time (was 15 same-cause + 1 same-decision-different-cause on 16 vectors). Eight verdicts moved on our side per the rulings: canonical version token `^btvault([1-9][0-9]*):$` compared as a STRING (before: `startsWith("btvault1:")` + a `[0-9]+` family regex answered `UNSUPPORTED_VERSION` for `btvault01:` - never opened, but the wrong prompt), leading `?` -> `MALFORMED` in both key orders, BOM-only name -> absent, padded 64-name accepted (trim before cap). Trim predicate `isVaultQrTrimCodePoint` = `Cc u Zs u Zl u Zp u {FEFF}` from the Unicode category tables, deliberately NOT a `\p{IsWhite_Space}` regex (Android's ICU regex rejects OpenJDK property spellings - a JVM test would prove what the device does not do); used by parser, blank test and builder alike. Four uncovered cases recorded in the fixture (`openUncoveredDifferences`) and board-posted, not fixed locally: `n=%zz` accept/reject split, `{not json` legacy-vs-malformed reason, `f=` empty absent-vs-invalid, `v` version-nibble shape. Cosmetic debt cleared in the two QR test files (raw bidi/NUL -> `\u`); same debt remains in `BtProfileIconTest`, `BtQuickLinksTargetingTest`, `StandingOrdersScreen`, `CashLedgerFilters` (raw U+FFFF/U+001F) - follow-up.
> **E9 fresh-start notice built, FLAG OFF (`FreshStartNoticeFlags.enabled = false`) on a route-policy blocker**: `MeResponse.paranoidFreshStartPending` decoded (optional, per openapi). The tick's `POST /account/fresh-start-ack` does not exist; the real route is `POST /api/v1/auth/fresh-start-notice/acknowledge` (no body, returns `MeResponse`) and its security is `sessionCookie` ONLY - `resolveAuthPolicy` in the platform's `bearerAuth.ts` carves out `/auth/first-run/complete` but not this route, so our bearer is refused. Ask posted (one allowlist line). The sheet: compact `BtPickerSheet`, calm section-21-ruling-4 copy EN+DE, ack rebuilds the session from the returned `MeResponse`, in-memory "shown this session" only (no persisted seen-flag can mask the server), labeled web link row to `/control/privacy`, outcomes inline. **Import ack**: the app has NO import DTOs/routes (web-only wizard); `ImportAdditiveKeyToleranceTest` pins the shared `AppGraph.json` against the real `understanding` object + `resolvedBy` row field and carries a tripwire for the day an import client appears.
> **S2 slice 1 - per-vault sync engine `vault/pv/sync/` (9 files, +40 tests, dormant)**: `PvDocMedium` seam (server adapter now, Drive next), `vault_doc_cursors` table PK `(vaultId, docId, medium)` via additive migration 12->13 (`schemas/13.json` exported, DDL matches character for character; v1's "validator + body as one unit" re-keyed: `If-None-Match` only when `cursor.docVersion == local.docVersion`, cursors forgotten whenever local state is discarded), section-6 entity rules CALLED not restated (`mergePvDocs` maps portfolio/common docs onto `VaultMerge`'s v1/v2 shapes; `PvSyncDisciplineTest` bans the v1 merge internals from the package), per-vault gate (lock + pending flag + job) with coalescing, `bt-pv-vault-push:<vaultId>` unique work (v1's `bt-vault-push` pinned untouched), `StateFlow<Map<vaultId, PvVaultSyncState>>`. Six provisional decisions board-posted (header-doc merge rule, third-412 via `PvSentWrites`, 404-on-Replace = absent -> recreate at local version, +1 on commit, retry bounds 3/4, wiring absent by design). Slice 2 = Drive medium (E5) + Room-backed local store over `vault_docs` + section-14 chip rows.
> **Process lessons (dossier updated)**: three builders gating in parallel corrupted `app/build` (concurrent dexing -> `NoSuchFileException`) - builders now run scoped one-flavor tests only; the coordinator runs ONE combined gate on an isolated `git worktree` snapshot before committing. Kotlin nested-comment trap: a `/*` glob inside KDoc opens a nested block comment (~1000 cascading errors). Lock acquisition: install the EXIT trap only after `mkdir` succeeded. `advanceUntilIdle()` skips `backgroundScope` work - debounce tests must pass the `TestScope` as the engine scope.

> 🟢 **DONE 2026-09-02 - OWNER P0 "I keep getting kicked out" ROOT-CAUSED + FIXED, LOGIN WORDMARK FIXED (combined gate 2 on the isolated snapshot: 4238/0 BOTH flavors (+31), lint 0; commits `6d687a5` wordmark, `0d4b497` auth).**
> **Forced sign-outs - the mechanism, both halves proven**: the server rotates refresh tokens with ZERO grace (`oauthRepository.ts:646-700` consumes in the issuing transaction; no `grace|leeway|reuse window` anywhere) and answers a re-presented consumed token by REVOKING THE GRANT (`oauthService.ts:919-930`); one grant per (client, user) (`:869-884`) so that signs out every install; the concurrent variant lets the LOSER revoke the WINNER's fresh pair (`oauth.test.ts:1144-1170`, asserted as intended). App side: `TokenManager.doRefresh` returned null on a transport failure and left the unrotated pair on disk; the next queued caller re-sent the SAME refresh token within milliseconds (SessionInitializer fires 5-6 parallel calls at cold start, 30 s read timeout). Field conditions confirmed in the 9 h logcat window: hourly rotation (access TTL 3600 s), one Cloudflare 520, five 429s. Two further independent forced-logout paths found: `SecureStore.openPrefs` DELETED the prefs on ANY `EncryptedSharedPreferences.create` exception (a transient Keystore failure = permanent silent logout); `isAuthHardFailure` wiped on any 400/401 although the token endpoint's only documented 400 is `VALIDATION_ERROR` and a 401 there can only be the bearer middleware. Ruled out with evidence: bearer header on `/oauth/token` (bare client, now pinned by MockWebServer test), any app call to `/auth/logout`, password/2FA/email change, web logout, session caps (none exist), refresh TTL (30 d, re-minted hourly), multi-process. **Fix**: at-risk quarantine (`requestMayHaveBeenDelivered` separates connect failures from sent-then-silence; the latter parks the token 10 min - test asserts the HTTP call count stays 1), `refreshRefusalIsDefinitive` = `400 && INVALID_GRANT` only, definitive refusal re-reads storage first, rotated pair `commit()`ed before any caller proceeds, refresh skew 60 s -> 300 s, `sessionInvalidated` replay=1 (an early rejection was silently dropped by the late subscriber), tri-state `TokenRead` (Present/None/Unavailable) with 3x reopen backoff and NO destructive read, `grantToRevoke` follows the server's `current` marker (#1390 is LIVE on prod; DTO comments stale). New: `SignOutLedger` (app-private prefs `bt_signout_ledger`, last 20, newest first, no secrets; reasons incl. `REFRESH_REJECTED_AFTER_LOST_RESPONSE` = the smoking gun), `AuthDiagnosticsScreen` reachable login gear -> pre-login sheet -> "Diagnose"/"Diagnostics" -> "Abmeldeverlauf"/"Sign-out history", `AuthReconnectBanner` ("Reconnecting to your account..." + retry, offline-banner slot) for the state that used to be a logout. +22 tests (`ForcedSignOutTest`), 20 EN/DE pairs. Grant/session inventory NOT taken (Cloudflare 1010 on the cookie login, curl retry classifier-denied - no session/grant created on the owner's account). Board asks posted: rotation grace window 30-60 s with same-successor re-issue; loser-does-not-revoke; per-token revocation (RFC 7009) or per-device grants; prune outstanding refresh tokens on re-authorize; `INVALID_GRANT` absent from the deployed spec. Follow-up queued: cold-start `/assets/{id}` fan-out trips the 60-req/10 s burst limiter (five 429s) - find and batch.
> **Wordmark (owner: "Better" must be the opposite of the background)**: TWO bugs. `LoginScreen` (and `PasswordChangeRequiredScreen`) painted NO background, so the SYSTEM day/night `windowBackground` showed under APP-theme ink (owner: system light + app dark => near-white "Better" on white); the monogram was the raw splash raster with a baked WHITE "B". Fix: both roots paint `bt.bg`; new `BtBrandmark` composes two colourless alpha masks (`bt_brandmark_ink.png` silhouette tinted `textPrimary` + `bt_brandmark_gold.png` "T" in `gold`; `tools/brandmark/split_glyph.py` regenerates byte-identically); `Wordmark` KDoc pins "Better" = on-background ink. `WordmarkInkTest` (9): polarity per palette, contrast light 17.81:1 / dark 17.96:1 / true black 19.38:1, `textPrimary` == M3 `onBackground` in every palette, no in-app screen references `splash_bt_glyph`, every out-of-shell root paints `bt.bg`. Splash left as-is (window manager, pre-Compose). Note for the owner: the monogram keeps the raster's safe-zone framing (~29x21 dp inside 72 dp) - colour-only change; re-crop is one line in the tool if he wants it larger. Visual verification on device pending (QA builder held the phone).
